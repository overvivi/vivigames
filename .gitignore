# Claude Code のローカル設定(端末ごとに異なるためコミットしない)
.claude/settings.local.json

# OS が勝手に作るファイル
Thumbs.db
desktop.ini
.DS_Store

# 旧版の背景。images/bg1-hell.png に置き換わったが、元データとして手元に残す
ヘルランナー背景.png

# ローカルの自動テストが生成する一時結果
node_modules/
test-results/
playwright-report/

# ポータル素材の生成原本。世代違いの中間生成物が大半で42MBあるため履歴には残さない。
# 公開に使う images/portal/*.webp は追跡している。原本はディスク上に保持する。
images/portal/source/

# HELL RUNNER 2 素材の生成原本。36MBあり、公開には使わない。
# 公開で使う images/hell-runner-2/{icons,frames,ui,collectibles} は追跡している。
images/hell-runner-2/source/

# 本日の最強決定戦の立ち絵・ドット絵原画。公開ページは圧縮済みWebPだけを使う。
images/todays-champion/source/

# ブラウザが吐くGPUのエラーログ
debug.log
