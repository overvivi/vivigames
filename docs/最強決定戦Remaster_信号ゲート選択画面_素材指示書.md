# 本日の最強決定戦 Remaster
# ネオンゲート選択画面 — 素材指示書

最終更新: 2026-08-27
対象: `projects/todays-champion-remaster/` の次期 `FIGHTER SELECT` 画面

---

## 1. この画面で作りたいもの

仮称 **NEON GATE LINEUP（ネオンゲート式）**。

雨のネオン街に立つ、7本の縦長キャラクターゲートから挑戦者を選ぶ画面。旧版の「縦長5人選択」のスタイリッシュさを残しつつ、7人を無理なく扱えるようにする。選択中の1人だけが明るく、前へせり出し、他の6人は暗く沈む。上部は暗い空と街明かりだけにし、選択に合わせた光の演出をPhaserで出す。

目標は、携帯で開いた瞬間に「これはちゃんとした対戦ゲームのキャラセレクトだ」と感じられること。PCはその横展開であり、**最初に優先するのは縦持ちスマホの見栄えと押しやすさ**。

### 絶対に避けること

- 現在のようなPC向け一覧を縮小しただけのグリッド
- 横長に引き伸ばされたキャラ画像
- キャラ名・肩書き・枠・背景が重なって読めない構成
- 画像生成時の意味不明な文字をそのままUIへ使うこと
- 信号の丸だけをCSS／DOMで背景の上に重ねる実装
- 小さすぎて携帯で直接押せないキャラカード

---

## 2. 実装上の前提（素材づくりで守ること）

### 2.1 選択演出はPhaserで完結させる

信号機は採用しない。無印版のように背景上へCSSの丸や別画像を重ねることも禁止する。

- 選択変更時だけ、選択キャラの固有色がゲート枠・背景の雨粒・小さな光へ短く走る
- 連打で何度も演出をリセットしない。選択が変わった時だけ再生する
- 選択ボイスがない／読み込み中でも、光の演出で画面が静止して見えない状態を避ける

### 2.2 文字は画像に焼き込まない

キャラ名、肩書き、番号、`START DUEL`、操作説明、画面タイトルはコードで表示する。素材内に必要なのは装飾だけ。

理由:

- 日本語・英語表記を後から直せる
- 端末ごとに文字が潰れない
- 画像生成の誤字を持ち込まない
- アクセシビリティと操作説明を保てる

### 2.3 タップ判定は画像の見た目とは別に確保する

スマホでは、見えている細いゲート画像だけを押す仕様にしない。各キャラは最低でもおおむね **44px × 44px以上** のタップ判定を持つ。

画面上は7本の縦ゲートが並んで見えてよいが、実際の操作は次を満たす。

- カードをタップして選択
- 左右スワイプでも選択変更
- 選択中のカードをもう一度押しても、ボイス・選択演出を最初から再生しない
- PCではクリックに加え、左右キーでも選択できるようにする
- STARTボタンは画面下部に固定し、カード操作と重ならない

---

## 3. 素材一覧（制作するもの）

### A. 選択画面専用背景 — 2枚

| ファイル名 | 用途 | 原画の目安 | 配信用の目安 |
| --- | --- | ---: | ---: |
| `select-bg-mobile` | 縦持ち携帯 | 2160 × 3840px | WebP、1440 × 2560px前後 |
| `select-bg-desktop` | PC・横長 | 3840 × 2160px | WebP、1920 × 1080px前後 |

内容:

- 雨の夜の日本風ネオン都市
- 濡れた路面の反射、都市建築の輪郭、遠景の看板、薄い霧
- 上部にゲーム名とネオン演出を置ける暗く読みやすい空間
- 下部にSTARTボタンを置くため、細かすぎる看板・人物・強い光を避ける
- キャラゲートの背後は暗めにして、カードの発光が主役になるようにする
- 背景の中央に大きな人物、読める文章、既存キャラに似た顔を描かない

背景は「カードを置く舞台」。カード用のキャラ絵と競合しないことを最優先する。

### B. 選択演出用の追加画像 — 現時点では不要

選択時の発光、カード枠、雨粒の反射はPhaserの図形・パーティクルで実装する。状態別の信号画像、信号柱、配管、ランプ素材は作らない。

### C. ネオンゲート用・縦長キャラアート — 7枚（最重要）

この7枚が画面の核。7人の絵柄、遠近感、照明、縦長比率を揃える。各キャラを別々の世界で描いた感じにせず、同じ都市・同じ対戦ゲームのロスターに見せる。

共通仕様:

- 画角は縦長、**幅:高さ = 1:3.2前後**
- 原画: 1200 × 3840px前後。配信用: WebP、幅768〜900pxを目安
- キャラは頭から足先まで入れる。足元は切らない
- 背景込みの完成絵。ただしカードの外枠、名前、肩書き、番号、文字、ロゴは描かない
- キャラの頭頂と足元に約5〜8%の余白を残す。実装で拡大しても頭・靴が切れないようにする
- キャラの顔・胸・武器がカード中央寄りに来る構図。横端へ重要な部位を逃がさない
- 光源は正面または斜め前から。暗転させても輪郭が残るようにする
- 色はキャラ固有色を強く出すが、背景の明度はキャラより抑える
- 画面内に読める文字、英単語、数字、ロゴ、透かしを入れない
- 既存のバトル用透過モーション素材とは別物。戦闘ポーズを流用しない

| ID | ファイル名 | キャラ | 固有色 | 必須モチーフ |
| --- | --- | --- | --- | --- |
| `raven` | `gate-raven` | RAVEN / ZERO BLADE | シアン、黒 | 長い黒コート、青い刀身、雨の青い路地 |
| `mika` | `gate-mika` | MIKA / PINK RIOT | ピンク、マゼンタ | ローラー／ストリート感、軽快な戦闘姿勢、ネオンの反射 |
| `brick` | `gate-brick` | BRICK / IRON FIST | 橙、鉄、金 | 巨体、工業作業服、重い腕、熱い工場灯 |
| `noise` | `gate-noise` | NOISE / BEAT BREAKER | 紫、電気青 | ヘッドホン、音波・ビート、夜のクラブ街 |
| `kiri` | `gate-kiri` | KIRI / GHOST SIGNAL | 白紫、深紫 | 大人っぽい美形、長い上着、幽霊信号・薄い霧 |
| `vivi` | `gate-vivi` | VIVI / TWO-FACED | 白、黒、氷青、赤紫 | 15歳前後の白髪美少年、天使と悪魔の混在、壊れた光輪、静かな狂気 |
| `tomega9` | `gate-tomega9` | TΩ9 / DESTROYER | 赤、黒、炉心橙 | 最大級の人外男性型キメラ、肥大した両腕、クラッシュ、赤い炉心街 |

#### キャラごとの構図メモ

**RAVEN**

- 刀を低く構え、こちらを静かに見据える
- 余計なポーズで暴れず、クールで厨二的な危険さを優先

**MIKA**

- 明るく挑発的。ピンクの勢いはあるが、可愛いだけではなく対戦者として強そうに見せる
- 足元の反射やローラーを見せる

**BRICK**

- 身体の大きさと鉄拳の重量が即座に分かる
- ポスター内で小さく見えないよう、肩・腕・胴を太く明確にする

**NOISE**

- 音楽キャラだと分かるが、DJの立ち絵で止めず、対戦者としての圧を出す
- 紫の音波は背景演出として扱い、輪郭を食わないこと

**KIRI**

- 落ち着いた大人のイケメン。中性的・少年寄りにしすぎない
- 白紫の霧や信号のモチーフを静かに入れる

**VIVI**

- 大人の美形ではなく、15歳前後の「整いすぎた美少年」
- 白い天使性と黒い悪魔性が一つの身体で不安定に混在する
- 叫んだ時の狂気を想像できる顔。ただし最初から絶叫顔にはしない
- 壊れた光輪、白黒の羽根、氷青と赤紫の異物感を入れる
- 白主体でも背景に埋もれないよう、黒側と輪郭光で形を保つ

**TΩ9**

- 全ロスターで明確に最大。人間の格闘家より一段大きい化け物
- ただの機械・ロボットではなく、低い男声で日本語を話す「生きた人形／キメラ」
- 赤黒の炉心、異常に重い両腕、破壊者の圧を見せる
- 画面が赤一色で潰れないよう、黒鉄の面積と赤い発光を分ける

### D. キャラ紋章 — 7枚（推奨）

ゲート上部や、選択中キャラの情報部に使う小さなマーク。名前が読めなくても誰を選んでいるか補助できる。

- 512 × 512pxの透過PNGまたは透過WebP
- 1色または2色まで。小さくしても判別できる太い形
- 文字・イニシャルは不要

| ID | 紋章の方向性 |
| --- | --- |
| RAVEN | 割れた刀身／鳥の翼を思わせる刃 |
| MIKA | ローラーの車輪＋稲妻 |
| BRICK | 鉄拳／六角ボルト |
| NOISE | 波形／砕けたスピーカー |
| KIRI | 霧の中の信号波／細い剣閃 |
| VIVI | 割れた光輪＋二対の羽根 |
| TΩ9 | 押し潰す両腕／炉心の亀裂 |

### E. ゲートの金属装飾 — 2〜3枚（任意だが推奨）

フレームそのものはCSS／Phaser図形で作れる。ただしC案のような金属の傷、細い配線、金の縁を出したい場合は次の薄い透過テクスチャを用意する。

| ファイル名 | 用途 | 備考 |
| --- | --- | --- |
| `gate-frame-metal` | 通常カードの黒鉄テクスチャ | タイル可能または縦長枠用 |
| `gate-frame-selected` | 選択中の白金〜金の縁・微発光 | キャラ絵を隠さない細さ |
| `ui-metal-plate` | STARTボタンや下部プレート用 | 任意。ボタン文字は入れない |

この素材は主役ではない。複雑なイラストにせず、カードアートの色を邪魔しない黒鉄・鈍金・浅い傷に留める。

### F. 選択時の短い装飾VFX — 3枚程度（任意）

静止画だけで十分に完成させてから追加する。実装時の優先度は低い。

- `select-scanline`：選択カードを走る細い走査光
- `select-shards`：VIVIやTΩ9にも使える小さな破片・火花
- `select-rain-glint`：濡れた地面の反射が一瞬強くなる光

全キャラ共通で使える抽象素材にし、キャラごとの専用VFXは作らない。キャラ固有の派手さは戦闘中の攻撃エフェクトへ残す。

---

## 4. 今回、画像として作らないもの

以下はコードで作る。画像素材を増やして重くしたり、端末差で位置を壊したりしない。

- `FIGHTER SELECT`、`TODAY'S CHAMPION / REMASTER`などの見出し
- キャラ名、英語の肩書き、番号、選択状態の説明
- `START DUEL`ボタン内の文字
- 左右のスワイプ矢印、ページインジケーター
- カードの暗転、選択中の拡大、発光、影、揺れ
- 背景上へ別座標で重ねる演出素材
- 操作中に変わる説明文

フォントはゲーム側で読み込み、素材の画像文字には頼らない。

---

## 5. 画面構成・安全領域

### 縦持ち携帯（最優先）

1. 上部 0〜25%: 小さいゲーム名・選択中キャラの情報
2. 中央 20〜78%: 7本のネオンゲート。選択中だけ明るく前へ出る
3. 下部 78〜94%: 選択キャラ名・肩書き・STARTボタン
4. 最下部: スワイプヒント。iPhoneのホームインジケータに重ねない

生成モックのように7本を同時に見せるのは見た目の目標。ただし実装では、携帯でカードが押せないほど細くなる場合に限り、左右スワイプで中央の選択カードを動かし、隣接カードを一部だけ見せる方式へ切り替える。見た目より操作性を優先する。

### PC

- 同じ素材を横に広げる
- 背景だけはPC専用の横長版を使う
- ゲート・文字・選択演出はPhaser内の同一カメラ座標で拡縮する
- 画像ごとの別CSS位置調整を作らない

### 原画の安全領域

- どのキャラ絵も左右8%、上下5〜8%は重要部位を置かない
- 顔、武器の先端、拳、靴、壊れた光輪は切れやすい端へ寄せない
- UIが重なる可能性のあるカード下15%には、読まれるべき細部を置かない

---

## 6. 容量・形式・配信ルール

### 原画と配信用は分けて保存する

- 原画: 高解像度PNGを`source/`相当の非公開保管先へ残す
- 配信用: WebPをゲームの`public/assets/`へ置く
- PNG原画をそのままGitHub Pagesへ置かない
- 紋章・VFXなど透明が必須の素材は、alpha対応WebPをまず検討する。背景は不透明WebPでよい

### 初回読み込みの目標

| 区分 | 合計の目安 |
| --- | ---: |
| 背景2枚 | 0.8〜1.2MB |
| ゲート7枚 | 2.1〜3.0MB |
| 紋章・装飾・UI | 0.2〜0.4MB |
| 選択画面の合計 | **3.1〜4.6MB程度** |

これは初回に短い`PREPARING...`演出を許容すれば、携帯でも現実的な範囲。さらに大きくなる場合は、次の順で削る。

1. キャラ絵のWebP品質を微調整する
2. 背景の細かいノイズを減らす
3. PC背景を携帯初回ロードから分離する
4. 選択中の高解像度版を作る場合は、選んだキャラ分だけ先読みする

画質が必要なキャラゲートを、安易に小さくしすぎない。最初に削るのは背景・装飾側。

### 読み込み順

1. 選択画面の背景（端末に合う1枚）
2. 7人のゲートカード
3. 紋章、装飾
4. 選択キャラのボイス・戦闘開始時に必要な戦闘素材

選択中キャラのボイスを鳴らす前に必要な素材だけ読み込み、ボイス終了と戦闘開始演出の間を不自然に止めない。

---

## 7. 納品・確認チェックリスト

### 選択演出

- [ ] 背景上に別DOM・CSS・状態別画像を重ねていない
- [ ] 選択変更時の光がゲートと雨の反射として自然に見える

### キャラゲート

- [ ] 7枚の縦横比が同じ
- [ ] 全員の頭頂・足元がカード内に収まる
- [ ] VIVIが大人に見えず、白黒の不安定さがある
- [ ] TΩ9が他キャラより明確に大きく、人外の重量感がある
- [ ] BRICKが小さく細く見えない
- [ ] KIRIが少年寄りではなく、大人っぽいイケメンに見える
- [ ] 既存5人と新規2人で、絵柄・照明・密度がバラバラに見えない
- [ ] 読める文字、ロゴ、透かし、誤字が絵の中に入っていない

### 携帯での実機確認

- [ ] iPhone Safari、Android Chrome、PC Chromeで選択UIの位置が崩れない
- [ ] 縦持ちでキャラカードを無理なくタップできる
- [ ] スワイプしてもページ全体が意図せずスクロールしない
- [ ] 選択中のキャラが一目で分かる
- [ ] 全員が暗転しても誰のカードか最低限分かる
- [ ] 画像読み込み後にカードがカクつかず、STARTを押せる

---

## 8. 作成順

1. 背景の世界観を決めるため、`select-bg-mobile`のラフを先に作る
2. 7人の`gate-*`を同一テンプレートで揃える
3. PC背景、紋章、金属装飾を追加する
4. 低解像度の仮素材をPhaserへ接続して、携帯でタップ・スワイプ・配置を先に確認する
5. 操作と画面構成が確定してから、高解像度WebPへ差し替える

画像を全部完成させてから初めて画面へ置くのではなく、**背景＋2〜3人の仮ゲート**で早期に操作確認する。これなら、素材を7人分作り直す事故を防げる。
