# PROJECT STATUS — Codex / Claude Code共通引き継ぎ

最終更新: 2026-08-19 / Codex

このファイルが作業状況の正本。Codex・Claude Codeとも、作業前に読み、作業後に更新する。
詳細な制約は `AGENTS.md` を優先する。特に巨大HTMLの全体読み込み・全体整形は禁止。
git操作は通常行わず、ユーザーから明示的に許可された場合のみ可能。

## 現在の状態

### HELL RUNNER 2（`games/hell-runner-2.html`）

- 再開時は [HELL-RUNNER-2引き継ぎ.md](docs/HELL-RUNNER-2引き継ぎ.md) を最初に読む。完了範囲、未確定の数値、実機確認手順を現状へ整理済み
- HELL RUNNER 1とは完全に別ファイルのローグライト版。ラン中XP・3択・メイン2枠／補助3枠／LEGEND1枠、育成完了後のLucky!報酬、開発者ツールを実装中
- ポーズ画面のHOMEでタイトルへ戻る時、直前のCOIN COMBO HUDが残る不具合を修正。HOME遷移でコンボ値を0へ戻し、コンボ／SOUL BANKの表示判定を更新する。専用Playwrightテストで再発防止済み
- 収集物の基準値は実機確認済みのサイズ2.20x、発光1.80x、高所高さ1.50x、コイン40%、オーブ20%、複数オーブ5%、高所コイン40%、高所オーブ20%、間隔56pxへ設定。コイン価値1.50xはコンボ可視化後に再調整予定
- 高所アイテムは奈落上のみ。1層目は常時、2層目は20,000m以降、3層目は60,000m以降に出現する
- デバッグ地点移動は能力ビルド・XP進行を保持し、古い能力選択オーバーレイだけを確実に解除する
- 追従する死神は距離ギミックではなく、LEGEND「デスイーター」取得中だけ表示する。XP到達が空中なら、着地してからLEVEL UPを開く
- XPバーはオーブ取得ごとに即時更新。コインコンボはプレイ中常時、外装なしのコンボ数・倍率・残り時間ゲージだけを表示し、未継続時は`00`。連続数で青→緑→金→赤紫へ変化し、8コンボ以上では数字だけが流れる虹色になる。`?debug=1`で基本持続時間、本体・ゲージのサイズと位置を個別調整できる。LEGENDのみの候補も見送れる
- コンボUIの確定初期値は、基本持続3.00秒、表示位置X84% / Y22%、本体・ゲージ倍率1.00x、ゲージオフセット0px
- HELL RUNNER 2専用Playwrightは最新9件成功。通知はXPバー下、ゲームオーバー時はXP・能力・コンボ・通知をすべて隠すことを自動確認済み
- `?debug=1`にスコア完成ビルドと変換器完成ビルドを追加。同じ完成後状態からLucky!回数と得点を比較でき、ラン統計も距離・合計スコア・通常コイン・Lucky!・通常オーブXP・変換器XPへ分解表示する
- 同じ出現率・距離帯で比較するため、`10K比較を開始`→10,000m走行→`10K結果をコピー`を追加。地点移動を挟むと計測を破棄し、混在した結果を残さない
- 比較プリセットは、完成ビルド表示なのに必要XPだけ初期24のまま残る問題を修正。最初のLucky!確認後は、完成時の能力取得数に対応する終盤XPしきい値へ戻る
- 変換器取得後もスコアに無関係なコインコンボ表示だけが反応する不整合を修正。変換後のコインはXPだけへ変換し、既存コンボも即時`00`へ戻す
- 変換器の完成後価値を追加。完成後の必要XPは最大800XPで頭打ちにし、変換XPは魂BANKへ蓄積、次の`SOUL LUCKY!`で`変換XP×5.20点`を基礎Lucky!へ加算して還元する。両値は開発者ツールで調整可能
- 変換器専用の`SOUL BANK` HUDを追加。取得中だけXPバー下に表示し、魂BANK量と800XP基準の充填ゲージを視覚化。約80%以上で発光し、SOUL LUCKY!後は空へ戻る。ゲームオーバー時の非表示も専用テストで確認
- SOUL BANKを横バーから透過魔導瓶へ刷新。`images/hell-runner-2/ui/soul-bank-vessel.png`に紫の悪魔金属フレーム・透明な中心瓶を生成し、CSSの魂火が内側を下から満たす。原本は`images/hell-runner-2/source/ui/`。1254×1254 RGBA・アルファ0〜255を確認済み
- 魔導瓶の充填演出を追加。約80%から脈動し、満タンでは瓶口から魂が漏れる。SOUL LUCKY!では白紫の割れフラッシュ・揺れ・放射粒子のあとにBANKを空へ戻す
- 変換器中のCOIN COMBOを非表示化し、SOUL BANKを同じ右上領域へ差し替え。素材の透明な胴内に合わせて魂火の幅・高さ・底位置を再調整し、フレーム外へ漏れないようにした
- SOUL BANKの文字とBANK数値は能力アイコンに重なっていたため、瓶の下へ移動。瓶そのものの位置はコンボ差し替え位置のまま維持
- 100K帯10K比較で、スコア完成14,110（COIN +4,064 / Lucky +46）対変換器完成13,850（Lucky +92 / SOUL +3,758）を確認。差約1.8%で目標±10%内のため、完成後XP上限800・魂換金5.20点/XPを初期採用
- スコア・変換器の数値調整フェーズを完了。次は`docs/HELL-RUNNER-2実機テスト項目.md`の基本感触、抽選、全LEGEND、PC/携帯HUDの実機確認を行い、指摘箇所だけを最終調整する
- 修羅・ギャンブラー倍率がコイン/Lucky!だけにしか掛からず、距離スコアに反映されていない不整合を修正。総スコア倍率として修羅1.30x、ギャンブラー1.05xを初期候補にし、開発者ツールへ個別スライダーを追加
- `?debug=1`へ修羅・ギャンブラー完成ビルドを追加。修羅は3段ジャンプ・滑空＋コイン倍率／コンボ／持続、ギャンブラーは3段ジャンプ・滑空＋コイン倍率／コンボ持続／経験値倍率の混成で、転生を取らない同条件の10K比較ができる
- 修羅・ギャンブラーの総スコア倍率は取得後の得点だけへ適用するよう修正。終盤で取った時に過去の距離まで遡及してスコアが跳ねる抜け道をなくし、デバッグ地点移動でも移動後地点を新しい倍率起点にする
- 100K帯10K実機比較（修正後）は修羅完成+17,032、ギャンブラー完成+13,643。既存のスコア完成+14,110／変換器完成+13,850と併せ、修羅は早期取得で大化けする高リスク枠、ギャンブラーは選択不要の代償に少し弱い枠として初期倍率を維持する
- ギャンブラーは上記比較後にさらに弱めた。最高倍率は従来の1.05xのまま、`0.85〜1.05x`を0.05刻み・均等抽選（平均0.95x）に変更。取得時と10K結果に確定倍率を表示するため、選択不要の気軽さと下振れを両方明示できる
- LEGENDの通常出現率を17%から25%へ変更。さらに未取得のまま90,000mへ到達した場合、最初の安全な着地でLEGEND候補を必ず出す天井を追加した。XP到達を待たずに起動するため、100,000mまでに必ず一度はLEGENDを見られる。100K比較用のデバッグ地点移動では天井を消化済みにして、完成ビルド操作を妨げない
- LEGEND候補を一度でも見送った後の再出現率を35%へ設定。さらにLEVEL UPの各カード下に1回限りの`⟳`を追加し、同ティアの未提示能力へだけ引き直せるようにした。LEGEND待ちの理不尽さを減らしつつ、無限リロードによる確定取得は防ぐ。再出現率は開発者ツールでも調整できる。HR2専用テストは引き直しケースを追加して最新10件成功
- LEGENDの初回出現率はユーザー調整により25%から30%へ引き上げ。90K天井・見送り後35%・同ティア1回引き直しは維持
- 開発者ツールの「能力選択を表示」がクリックEventを強制LEGEND引数として渡しており、率に関係なく毎回LEGENDになる不具合を修正。通常のLEVEL UPと同じ抽選へ統一し、率1%・乱数固定でLEGENDなしを確認する専用テストを追加。HR2専用テストは最新11件成功
- ユーザー確定候補を初期値へ反映: 修羅速度1120px/s、修羅得点1.30x、ギャンブラー最高1.05x、LEGEND初回/再出現率35%/35%、変換XP0.70x、Lucky1.00x、完成XP上限800、魂5.20点/XP、収集物2.20x・発光1.80x・高さ1.50x、コイン40%・オーブ20%・複数オーブ5%・高所コイン40%・高所オーブ20%・56pxグリッド、コイン価値1.50x、コンボ3秒・表示84%/22%
- PC/携帯とも取得・Lucky!などの通知が上部能力アイコンと重なっていたため、通知をLEGEND枠を含む全スロットの下（top 218px / 単行222px）へ統一。両表示幅で通知とLEGEND枠が重ならない専用テストを確認。HR2専用テストは最新12件成功
- `?debug=1`の左側ローグライト調整を整理。確定済みの能力・収集物・コンボ値は折りたたみ「確定済みバランス値」へ格納し、通常表示は通知調整を優先。ライバル、デスイーター、GAMBLE、RANDOM GET、Lucky、SOUL LUCKY、LEGEND CALL、復活の8通知を即時表示するボタンと、全通知共通のサイズ・X・Yスライダーを追加。コピー値へも通知値を含めた。全種と位置反映を自動確認し、HR2専用テストは最新13件成功
- 通知のユーザー確定値を初期値へ反映（サイズ1.40x、X=51%、Y=264px）。転生後の「復活！」通知を削除し、通知プレビューも7種へ整理。HR2専用テスト13件・構文/素材検査は最新成功
- メインの3段ジャンプ・滑空をLv3までの段階能力へ拡張。両方ともLv1は既存より弱く、Lv3で既存能力と同等へ到達する。体感差を明確にするため、3段ジャンプは3段目だけを70%→85%→100%、滑空は重力0.65→0.47→0.32倍へ段階強化し、既存の1・2段目操作感を維持。転生はLv1固定。完成ビルドのプリセットも各Lv3の選択回数・終盤XPしきい値（スコア23、その他22）へ更新。HR2専用テスト13件、`npm run verify`、`git diff --check`成功
- 携帯実機確認用に、LAN内限定の一時サーバーをport 4174で起動中。終了時はNodeプロセスPID 18972を停止する。通常の127.0.0.1:4173開発サーバーには変更なし
- 携帯Safariでは全体のタッチ操作無効化が結果画面とDEV TOOLSのスクロールまで止めていたため修正。ゲームオーバー画面は常に上端から開き、ランキング登録時も縦スワイプでSIGN UP・RETRYまで到達可能。携帯のDEV TOOLSは展開後に縦スクロールでき、PCで左側へ出すローグライト調整を同一パネル末尾へ統合した。モバイル用の表示・タッチ許可もHR2専用テスト13件で確認
- 携帯のゲームオーバーは、ランキング登録ありでも1画面へ収まる専用密度へ調整（ロゴ・スコア余白・登録枠・画像ボタンを縮小）。引き直し`⟳`はカード高を増やさず40×24px・高輝度シアンへ拡大し、見落としと押しづらさを改善。新記録時の登録枠を表示した状態で1画面内に収まること、引き直しサイズをHR2専用テスト13件で確認
- 上記の圧縮を新記録時だけへ限定。登録欄のない通常ゲームオーバーは従来のロゴ・余白・ボタンサイズを維持し、寂しく見えないようにした。HR2専用テスト13件、`npm run verify`、`git diff --check`成功
- LEVEL UPの引き直し記号を、`C`に見えやすい`⟳`から矢印の向きが明確な`↻`へ変更。40×24pxの高輝度シアン表示は維持し、カードの高さを変えずに判別性を上げた。HR2専用テスト13件、`npm run verify`、`git diff --check`成功

### HELL RUNNER (`games/temple-run-clone.html`)

- 距離に応じた6背景、画像の足場、ステージ別OGG BGMと3000mクロスフェードを実装済み
- PC・スマホとも390:844の縦長比率と3倍の描画密度へ統一済み
- カメラは公開版を含め常時10%引き
- HUDは10000以上を `10K`、100000を `100K` のように短縮表示
- 30000mから緩やかに加速し、100000mで最高速度の1.4倍に到達
- 20000mで「デスイーター降臨」。死神が左上へ追従し、コイン取得範囲が広がる
- 旧オーラ・色エフェクト・十倍界王拳・修羅モードは廃止済み。復活させない
- `?debug=1` で距離ジャンプ、録画、スクリーンショット、BGM・ジャンプ・コイン・ライバル音の個別調整と設定値コピー、落下時に安全な足場へ即時復帰する `NO FALL` が使用可能。PCではゲーム外の右余白へ表示し、`−` / `+` で縮小可能
- タイトル画面右端に見える細線は、内容が縦に収まらない時のスクロールバー。動作上は問題なし
- 大小ジャンプの試遊版を実装。短押しは小ジャンプ、長押しは従来相当の大ジャンプ。2段目・3段目にも適用し、長押しから滑空へ自然に接続
- `?debug=1` の `JUMP HEIGHT` で `RELEASE CUT` を0.45〜0.90まで実機調整可能。滞空時間・水平距離・高さ・モードを実測表示し、設定値をコピー可能
- `JUMP_RELEASE_CUT = 0.50` に確定。PC・携帯の両方で実機試遊し、小ジャンプと大ジャンプのバランスが良いことを確認済み
- 最速タップを取りこぼさない毎フレーム状態判定を採用。携帯は最初のpointerIdだけを操作元にし、2本目の指を離しても大ジャンプ・滑空を解除しない
- 一時停止機能を実装。HUDのⅡ、既存START/HOME素材を流用した停止メニュー、3秒カウント再開、全サウンドON/OFF、自動停止に対応
- 停止中と再開カウント中は距離・物理・粒子・コウモリ・演出時間を完全静止し、空中停止時も大小ジャンプの軌道を維持する
- HUDのⅡ文字を正式な透過PNG `images/pause-button.png` へ置換。PC・携帯の実機確認で `PAUSE_BUTTON_SIZE = 96` に確定。デバッグモードでは28〜96pxの調整・直接入力・値コピーが可能
- PCタイトル画面へ3行の操作案内を表示。Space短押し＝小ジャンプ、長押し＝大ジャンプ、落下中長押し＝滑空、クリック併用可、P/Escape＝一時停止を明記。遊び方にも追記し、携帯ではPC向け案内を非表示
- ポータルのHELL RUNNERパッチノートへv0.4を追加し、NEWSとゲーム内タイトルのバージョン表記もv0.4へ更新
- 一時停止メニューへプレイヤー用のBGM・効果音音量ゲージを追加。実機調整済みの音量を100%基準とし、2系統を別々に端末保存。サウンドON/OFFは設定値を変えない一括消音として維持
- 音量設定が未保存の端末で `null` が0扱いになっていた初回無音を修正。未設定時はPC・携帯ともBGM・効果音100%、明示的に保存した0%は維持する
- 背景スナップショットテストでは新設のポーズボタンを隠し、背景素材の変更だけを比較するよう分離。背景基準画像そのものは更新していない
- PC・携帯の最終試遊に使用したLAN一時サーバー（port 8001）は作業終了時に停止済み

### 討伐2048 (`games/boss-battle-demo.html`)

- チャレンジランキングの得点を「最大タイル」から「ゲーム終了時の盤面全タイル合計」へ変更済み
- プレイ中の記録も現在の盤面合計を表示し、詰んだ時点で自己ベスト判定・ランキング送信を行う
- 新しい自己ベスト保存キーは `bossDemoChallengeBest_v2`。旧方式の記録とは分離
- 既存Supabaseテーブル `boss_battle_scores` は継続使用。旧2048点の投稿は残るが、新方式の得点が上回る想定
- 全タイルへ一律の移動方向オフセットをかける旧演出を廃止し、各タイルの実際の移動元セルから移動先まで滑らせる方式へ変更。静止タイルと新規タイルは動かず、合体時は遠い側が滑ってポップする
- 戦闘画面左上（HP帯の下）へ青い魔法陣の停止ボタンを追加。一時停止中は盤面入力・演出・攻撃タイマー・BGM・効果音を停止し、PLAY／RETRY／HOMEを選択可能
- 一時停止メニューへBGM・効果音の個別音量ゲージを追加。従来の音量を100%基準として端末保存し、未設定時は両方100%で開始
- `?debug=1`へSIZE TUNERを追加。停止ボタンはサイズ/X/Y、勇者は共通サイズ/X/Y、モンスターはミミック・ドラゴン・魔王ごとのサイズ/X/Yをスライダーまたは直接数値入力で調整し、JSONをコピー可能。全3ボスも保存進捗を変更せず一時解放する
- 実機調整値を本番へ反映。停止ボタン97px・(0px,86px)、勇者117px・(20.1%,90.7%)、ミミック155px・(73.4%,33.4%)、ドラゴン155px・(68.2%,34%)、魔王180px・(77.9%,34%)
- ダメージ数字、攻撃画像、被弾・回復・撃破パーティクル、背景光を各ボスの現在位置とサイズへ追従。チャレンジスコアはHELL RUNNERと同じ10K／10.5K／1M形式で短縮表示

### HEXAMINE（`games/hexamine.html`）

六角形の盤面から原子の配置を論理だけで復元するパズル。3本目の完成作。
**あてずっぽうは要らない。** 生成器は「推測なしで解ける」ことを検査してから盤面を出す。

- どの画面からでも`← GAME BASE`でポータルへ戻れる。タイトル・選択中は左上、プレイ中はMENU/HINTと重ならない左下へ自動配置
- **ソルバーが正本**。`<script type="javascript/worker" id="solverSource">` に1つだけ置き、
  Worker はこのテキストから Blob で作る。テストも同じ場所から取り出して動かす
  （写しを持つと必ず食い違うため）
- **チュートリアル6面**。規則を1つずつ解禁する。教えていない記法は前の面に出さない
  （`notation` / `omitBlue` / `silentRatio` で制御）。盤面は固定の種で、説明とずれない
- **手順つきの誘導**。ソルバーが辿る手順をそのまま台本にし、根拠→理由→操作の順に示して
  押すマスを光らせる。台本は毎回いまの盤面から作り直すので、違う順に開けても破綻しない
- **間違えたマスは開かない**。本家と同じで、ミスだけ増えて盤面は進まない。
  黙って開くと「いつの間にかミスしていた」になり、最後の1手だと結果画面へ直行してしまう
- **長押しの誤爆対策**。押している間に輪郭を1周描いて進み具合を見せ、
  中途半端な長さ（LONG_MS の55%以上・未満）で離した場合はどちらとも取らない
- **∞モードとデイリーは不定形の盤面**。種から輪郭を作る。穴は「6方向すべて埋まったマス」
  からしか抜かないので分断されない。デイリーは種だけで決まるため全員が同じ形
- **デイリーランキング**（Supabase `hexamine_scores`）。順位はミスの少ない順、
  同じならタイムの短い順。送れるのは1日1回だけ
- 一発勝負だが、抜けて入り直せる。**経過時間とミスは引き継ぐ**ので、
  やり直しても有利にならない。誤操作で1日を失わせないため
- 日付は**日本時間で固定**。端末のローカル時刻だと時差のある人だけ別の盤面になり、
  「全員が同じ盤面」が崩れる。GitHub Actions（UTC）と揃える意味もある
- 送信に失敗しても `submitted` を立てず、フォームも残す。通信の失敗で記録を捨てないため
- テスト `npm run test:hex`（108件）。solver.test.mjs が `games/hexamine.html` から
  ソルバーを取り出して検証する
- Discordのランキング通知は共通の`tools/ranking-to-discord.mjs`へ登録済み。1時間ごとに当日の1位更新だけを通知し、最初の実行では既存記録を基準値として保存する。HELL RUNNER 2（β）も同じ仕組みで総合1位更新を通知する


### ポータル (`index.html`)

**2026-08-21に全面刷新。以前の「アーケード筐体を並べる」方式は廃止した。**
筐体は3D・陰影・パース・金属質感を持つ絵で、生成AIに同じ型を再現させられず、
新作のたびにデザインが揃わなかった。旧筐体用のPORTAL TUNER、筐体・タイトル・PLAYの画像、
位置の確定値表は削除済み。現行の携帯機レイアウト用に、2026-08-21から新しいPORTAL TUNERを再導入した。

現在の形:

- 中央に**スケルトンの横長携帯機1台**。カセットを選ぶと本体へ吸い込まれ、
  差込口が光り、内部がそのゲームの色に灯って画面へ絵が出る
- **カセットはCSSで描く**（画像ではない）。形も色も光り方もコードから変えられる
- ロゴは**CSSの文字**。画像ロゴは、それを載せる暗い板ごと背景を覆っていたため廃止
- 世界観は地下の秘密基地 `VIVI GAME BASE`（旧 VIVI GAME ARCADE）。
  `INSERT COIN` `GAME FLOOR` は筐体前提の言葉なので
  `SELECT YOUR GAME` `CARTRIDGES` へ変更済み
- カセット置き場は**折り返さず横スクロール**。PCはドラッグでも動かせる。
  10本並べても1段のままで、ページは横に伸びない（実測確認済み）
- `?portalDebug=1`ではPORTAL TUNERを表示。ロゴ、ステータス、カセット見出し／列／名前、ゲーム機、情報カウンターのX/Y/倍率を個別スライダーで調整できる。値はその端末の開発表示だけに保存され、コピーした値を本番CSSへ反映する
- 倍率の表示単位`x`をCSSの`scale()`へ渡して変形全体が無効になる不具合を修正。位置・倍率スライダーの値がCSS変数へ即時反映されることを実機ブラウザで確認済み
- 携帯の初期レイアウトは実機調整値を採用（見出しY -78px／1.09x、カセット列Y -82px、名前X -15px、本体Y -127px、情報Y +37px）。携帯の挿入カセットは本物と近い25%幅へ拡大。タグのX/Y/倍率も調整対象へ追加し、コピーが使えないHTTP携帯環境では`値をコピー`でツール内に確定文字列を表示して長押し／スクショ共有できるようにした
- `?portalDebug=1`のPORTAL TUNERへ、携帯機へ挿入済みのカセット専用のX/Y/倍率を追加。挿入アニメーション中から着地後まで同じ調整値を使うため、サイズだけを変えても差込口からずれない
- 背景はカセット名の下端まで延長し、その直後から本体の黒い展示領域へ切り替える。カセットと背景の切れ目が重ならないよう、PC/携帯ともヒーロー高さとセクションの重なり量を対で調整済み
- CSSロゴは一部の文字だけが壊れた蛍光灯のように不規則に明滅。`SELECT YOUR GAME`はゆっくり点滅する。`INFORMATION COUNTER`は外側の大きな装飾枠を廃止し、各情報ボタンだけを残す
- 蛍光灯演出は`VIVI GAME`内のIとEだけに限定し、`BASE`は常時表示。携帯ではカセット列をさらに35px上げ、ゲーム名を幅100%の中央揃え・折返し禁止へ統一して`HELL RUNNER 2`も1行で表示する

**新作を1本足すときは、絵1枚とコード数行だけ。**
手順は `docs/ポータル実機デザインの仕様.md` の「新作を1本足すときの手順」が正本。

**ゲームの絵には安全地帯がある。** 1枚を「カセット（正方形のまま全体）」と
「本体の画面（横長に切り取り）」の2か所で使うため、画面では上から10〜69%しか映らず、
その下半分にはタイトルの帯が重なる。**主役は上から10〜45%へ置くこと。**
数字と図は同仕様書に記載。

素材（すべて `images/portal/`、原本は `source/` で .gitignore 対象）:

| ファイル | 用途 | 何回作るか |
|---|---|---|
| `console-body.webp` | 携帯機本体 | 一度だけ |
| `arcade-bg-desktop/mobile.webp` | 秘密基地の背景（PC/携帯） | 一度だけ |
| `cart-label-*.webp` | ゲームの絵 | ゲームごと1枚 |

- `vivi-game-base-logo.webp` は残してあるが**未使用**。CSSロゴを採用したため
- 素材の縮小とwebp変換は `npm run portal:images`（`tools/portal-images.mjs`）
- 確認用サーバー`tests/lan-static-server.cjs`は既定でLAN公開（4174）。`VIVI_PREVIEW_HOST=127.0.0.1`と`VIVI_PREVIEW_PORT`を指定すると、そのPCだけで見られるローカル確認へ切り替えられる


### 素材と道具（2026-08-21時点）

| 道具 | 用途 |
|---|---|
| `npm run portal:images` | ポータル素材の縮小＋webp変換（`tools/portal-images.mjs`） |
| `node tools/game-images.mjs <html>` | ゲームHTMLの埋め込みPNGをWebPへ。大きい絵は非可逆、ドット絵は可逆 |
| `node tools/game-audio.mjs <html>` | 埋め込みBGMのビットレートを下げて入れ直す |
| `tools/hooks/pre-commit` | コミット前に `npm run verify` を走らせる（`git config core.hooksPath tools/hooks`） |

- 依存に `sharp`（画像）と `ffmpeg-static`（音声）を追加済み。どちらも devDependency
- **PowerShellでは `npm` が実行ポリシーで弾かれる。`npm.cmd` を使う**
- `docs/ダークネオンアーケード仕様書.md` は筐体方式の記録。現行仕様ではない

## テスト環境

**巨大HTMLを変更したら、最後に必ず `npm run verify` を実行する。**
構文・`<script>`の開閉と`</html>`終端・素材参照の実在を1コマンドで検査する。
以前は `tail -c 40` などを手で叩いていたが省略されやすく、素材の欠落は目視でしか
気づけなかった（実際にポータルで参照切れを4件見落としていた）。手作業の構造確認は不要。

テストは**変更した領域だけ**に絞る。全件は複数領域にまたがる変更と公開前確認だけ。

| コマンド | 対象 | 件数 |
|---|---|---:|
| `npm run verify` | 構文・構造・素材参照 | — |
| `npm run test:runner` | HELL RUNNER(debug-mode + 背景比較) | 25 |
| `npm run test:boss` | 討伐2048 | 10 |
| `npm run test:portal` | ポータル | 10 |
| `npm run test:hex` | HEXAMINEのソルバー | 108 |
| `npm test` | 全件（Playwright分） | **45** |

- 背景の見た目を意図的に変えた場合のみ `npm run test:update-backgrounds`。通常は基準画像を更新しない
- HELL RUNNER(無印)は凍結済みのため、`test:runner` と背景比較は**回帰確認としてのみ**必要。
  無印を変更していないなら実行しなくてよい
- 2026-08-21時点で `verify` / `test:portal`(10) / `test:hex`(108) / `test:boss`(10) / `test:runner`(25) すべて成功
- `test:hex` はPlaywrightではなくNode単体で動く（`tools/hex/solver.test.mjs`）

## 未完了・次にやること

### 公開前にやること（最優先）

1. **`hexamine_scores` の動作確認用データを消す。**
   名前「接続確認」で2件入れてある。消さないと公開後のランキングに混ざる
   ```sql
   delete from hexamine_scores where name = '接続確認';
   ```
2. **HEXAMINEを一通り遊ぶ。** チュートリアル6面と∞・デイリーを通しで触ってから公開する
3. **GitHubへpush。** 今日の作業はまだ一度もpushしていない

### ポータル

- `vivi-game-base-logo.webp` は未使用のまま残してある。CSSロゴで確定したので削除してよい
- HELL RUNNER 2 は β として掲載済み。完成したら説明とタグを見直す
- HELL RUNNER 2 の完成後に `node tools/game-images.mjs games/hell-runner-2.html` を通す。
  無印と同じ **未使用の `UI_B64`（2.5MB）** が残っているので、まずそれを消すと効果が大きい

### HEXAMINE

- `tools/ranking-to-discord.mjs` へ追加済み。初回実行時は記録するだけで通知しない
- 盤面の `0` の直線ヒントが多いと感じたら `silentRatio` などで調整の余地あり

---

- 既存HELL RUNNER 1は凍結。ユーザーから明示的な修正指示がない限り、コード変更もHELL RUNNER 1専用テストも実行しない。HELL RUNNER 2の作業で共通構文検査が1まで巻き込む場合も、2単体検査を優先する
- HELL RUNNER 2は、可視化済みのコインコンボを基準にしたコイン価値・変換器・Lucky!報酬の数値調整が次の主作業。現時点のコイン価値1.50xは仮固定
- 詳細な優先順・確認項目は `docs/HELL-RUNNER-2引き継ぎ.md` を正本とする
- ゲームオーバー時の能力アイコン・XPバー・コンボ・通知の非表示、通知がXPバーより下に出ることは専用テストで確認済み。今後は実機で配置の細部だけ確認する

- ローグライト能力版は既存HELL RUNNERへ上書きせず、ゲーム部分を流用した独立作`HELL RUNNER 2`として制作する方針に変更。新HTML・保存キー・ランキング・素材フォルダを分け、完成まではポータルへ掲載しない
- HELL RUNNER 2素材工程6の前半を完了。通常・邪悪・幸運コインと通常・高密度XPオーブの透過スプライト列を`images/hell-runner-2/collectibles/`、生成マスターを`source/collectibles/`へ保存。5素材とも四隅アルファ0・0〜255を検査済み。変換器演出とゲーム内置換は未実装
- HELL RUNNER 2素材工程5を完了。メイン3種・補助5種・レジェンド5種の全能力アイコンを`images/hell-runner-2/icons/`、生成マスターを`source/icons/`へ保存。全て1254×1254px、左上アルファ0・0〜255を検査済み。一覧は`docs/mockups/hell-runner-2-ability-icons-contact.png`
- HELL RUNNER 2素材工程6を完了。変換器演出`effect-coin-to-xp-spritesheet.png`を追加。通常コインから粒子を経てXPオーブになる8フレーム、1536×192px、四隅アルファ0・0〜255を検査済み。ゲーム内置換は未実装
- HELL RUNNER 2素材工程7の一部を完了。文字なし透過のLEVEL UP角装飾を`frames/level-up-header-ornament.png`へ追加し、選択画面へ統合。390×844・PCの専用UIテストと構文検査成功
- HELL RUNNER 2は旧版の開始前能力選択入口を非表示化。開始時は必ず2段ジャンプから始め、ラン中のXP・3択でのみ能力を得る仕様へ移行する。XP取得・抽選・実効果は次の実装段階
- HELL RUNNER 2の最小XPループを実装。通常・高密度XPオーブを足場上へ生成・描画・回収し、XPバーを更新する。しきい値到達で走行を停止してLEVEL UPへ入る。選択カードの候補抽選・取得効果・スロット反映は次段階。`window.__hellRunnerDebug.giveXp()`でdebug時のみ高速検証可能。専用UIテストと構文検査成功
- HELL RUNNER 2のメイン3種・補助5種を実候補化。メイン2枠・補助3枠の空きがある未取得能力、または上限未満の取得済み能力だけを3択へ出す。選択時に3段ジャンプ・滑空・転生は既存プレイ処理へ反映。補助の数値効果・通常プレイHUDスロットの動的反映・LEGENDは次段階。専用UIテストと構文検査成功
- HELL RUNNER 2の補助5種を実効果化。コイン倍率、コンボ倍率、コンボ持続（Lvごと+0.15秒）、経験値倍率、XP吸引を実処理へ接続。通常HUDのメイン2枠・補助3枠は現在の取得ビルドとLvを動的に表示する
- HELL RUNNER 2のLEGEND 5種を候補化。デスイーター（補助上限+1）、スロット解放（メイン+1）、修羅（固定960px/s・得点1.10倍）、変換器（コインをXPへ）、ギャンブラー（停止なしランダム取得・得点1.12倍）を実処理へ接続。レジェンド枠は1つのみで、候補出現率は5%
- ビルド内で取得可能な能力が尽きた以後は選択画面を出さず、内部の3択相当のスコア報酬（18 / 28 / 46）を抽選し`Lucky!`で通知する。数値は100000m帯の実機計測後に調整予定
- 通常のLEVEL UP選択後だけ0.65秒の静止＋`GO!`へ変更。旧3秒再開カウントは通常ポーズ専用として残し、ギャンブラー・育成完了後報酬は完全ノンストップ
- コインを既存の手描き円から、通常・邪悪・幸運の透過スプライトへ置換。変換器取得中は画面上のコインもXPオーブ表示へ即時見せ替えし、回収地点にはコイン→XPの8フレーム変換演出を出す。専用UIテスト3件・`npm run verify`・`git diff --check`成功
- `?debug=1`のROGUELITE BALANCEで、修羅速度（760〜1120px/s）、レジェンド率（1〜100%、本番初期17%）、変換器XP量（0.40〜1.20倍）、育成完了後Lucky!報酬（0.50〜2.00倍）を走行中に調整・コピー可能。通常URLには表示しない
- LEVEL UP下部の能力欄はダミー表示を廃止し、メイン→補助→LEGENDの順で現在のビルドとLvをHUDと同期して表示する
- `?debug=1`のROGUELITE BALANCEへラン統計を追加。通常コイン得点、変換器XP、通常オーブXP、能力取得数、Lucky!報酬をリアルタイム表示し、変換器とXP系ビルドの価値を比較できる
- コイン出現を地上区画の約80%から55%へ減らし、通常列も3〜5枚へ減らして広間隔化。COMMONは抽選重み1、メインRAREは0.45として、早期メイン取得を幸運なビルド分岐にした。実機確認項目は`docs/HELL-RUNNER-2実機テスト項目.md`
- XP吸引は取得範囲だけでなく、近くのコイン・XPオーブがプレイヤーへ滑らかに引き寄せられる視覚効果へ変更。XPオーブは30%を高所配置とし、吸引ビルドの回収価値を上げた。コイン／XPオーブは表示サイズと発光も強化。実機テストで吸引の分かりやすさ・高所の取りやすさ・明るさを確認する
- XP吸引の初期範囲が広すぎて全回収になる問題を修正。Lv1の開始範囲を168px相当から88px相当へ縮小し、追尾速度を上げて並走せず素早く吸収するよう変更。Lv上昇で遠方・高所まで届く設計は維持
- 次の地形・当たり判定修正: (1) 飛び越える段差と走行可能な段差を高低差で明確化、(2) 空中で段差の横壁へ衝突した際に通り抜けて落下する問題の解消、(3) コイン／XPオーブの高所・横位置を吸引Lvとジャンプ判断に見合うよう再調整。実機では高所配置がまだ十分に出ていないとの報告あり
- 段差の地形生成を小段差10〜28pxと大段差68〜120pxへ二極化し、最大高さを156pxへ拡張。走行可能かジャンプ必須かを視認しやすくした。空中の横壁判定から着地補助の通過例外を削除し、必要高度がない段差をすり抜けて落下する問題を修正。高所XPは45%・+130〜230pxへ強化
- 高所コイン配置を追加。地上弧コインを+112〜224px、ラッキーコインを+160〜230px、奈落越え報酬を最高+258pxへ上げ、ジャンプ精度またはXP吸引ビルドによる回収判断を作った
- 跳ぶ必要がある大きな上り段差だけ、横壁へ朱色の面と金色の上縁を追加。高低差だけでは見落としやすい場面でも、走行可能な小段差との判断を補助する
- `?debug=1`にWALL TESTを追加。届かない高さの横壁へ空中衝突する状況を固定再現し、押し戻されて通過しないことをHR2専用UIテストで検証。通常URLには公開しない
- ギャンブラー取得中と育成完了後は、1回のXP取得で複数しきい値を越えた余剰XPも即時に連続処理するよう改善。通常の3択だけは最初の到達時に停止し、残余XPを保持する。専用テストで200XPの連続処理を確認
- 育成完了後のLucky!報酬は上部通知に加え、プレイヤー頭上へ`Lucky! +点数`を浮かせる演出を追加。走行中の報酬取得だと直感的に分かるようにした
- `?debug=1`にCOMPLETE BUILDを追加。スロット解放＋メイン3種＋補助3種Lv5の完成ビルドへ即移行し、XPをあと1にして次の取得でLucky!報酬ループを確認できる。通常URLには公開しない。専用テストでも完成ビルド遷移とLucky!を確認済み
- PC幅1200px以上の開発者ツールはゲーム右外側へ横展開し、地点移動・録画・音量・ジャンプ・ローグライト調整を2列で配置。各操作とローグライトの全スライダーを日本語化し、用途説明も付けた。ゲーム画面と開発者ツールが重ならないことを専用PCテストで検証
- 開発者ツールを再整理。確定済みの音量・小ジャンプ調整は非表示化し、右側を地点移動・再現と録画、左側をローグライト調整専用に分割。サイズ0.60〜2.20倍、高所アイテム高さ0.50〜2.50倍、高所オーブ率0〜100%、コイン出現頻度10〜90%を走行中に調整可能
- 収集物生成を整理。コインは横列・ピラミッド・高所アーチ・高所逆ピラミッド・高所一点物へ限定し、紫（邪悪）・白金（幸運）・金を混ぜた当たり列も追加。既存のコイン／XPオーブと重なる生成を抑止し、十分な高低差の二層配置だけを残す
- XPオーブは通常42px・高密度52px基準へ拡大し、変換器表示もサイズ調整へ連動。表示サイズに応じて回収判定も変わる。高所コイン率0〜100%を開発者ツールへ追加し、XP吸引は前方の遠距離吸引を抑え、遠方でゆっくり・近くで加速する追尾へ変更
- 高所コイン・高所XPオーブは、着地できる地面上へは生成せず奈落上だけに限定。3段ジャンプなしで垂直高度を要求される不公平をなくし、強制ジャンプ中の寄り道報酬として機能させる
- 収集物の基礎出現を単体中心へ再調整。地上コインは単体62%・3連22%・ピラミッド16%、地上XPオーブは単体72%・3連18%・ピラミッド10%。混色コイン列も通常8%／高所22%へ下げた。コイン／オーブの3連とピラミッドは横幅・段の高さを同じ値に統一
- 全コイン（通常・紫・白金）と全XPオーブ（通常・高密度）の基準表示を42pxへ統一し、回収判定も共通化。横3連、ピラミッド、奈落上の3連は全て56pxグリッドで配置する
- 開発者ツールへオーブ出現頻度5〜80%、複数アイテム間隔42〜112px、コイン価値0.50〜5.00倍を追加。コイン価値の初期値は1.50倍へ上げ、距離だけでスコア差が付きにくい状態を緩和
- 複数オーブ率0〜70%を開発者ツールへ追加。高所報酬は奈落ごとに共通の単体／3連パターンを選び、最大2層まで同じ形で生成する。低層は単体ジャンプ、高層は2〜3段ジャンプの選択肢となる。XPオーブ回収には澄んだ短い上昇音を追加
- HELL RUNNER 2素材工程1を完了。COMMON・RARE・LEGENDの透過能力カード枠を`images/hell-runner-2/frames/`、生成原本を`images/hell-runner-2/source/frames/`へ保存。四隅・中央の実アルファ0を検査済み。表示初期基準は約354×128px
- HELL RUNNER 2素材工程2を完了。COMMON・RARE・LEGEND・EMPTYの小型透過スロット枠4種を同フォルダへ保存。全て1254×1254pxで四隅・中央の実アルファ0を検査済み。表示サイズは工程3のUI試作で確定する
- HELL RUNNER 2素材工程3を完了。仮アイコン3個（3段ジャンプ、コイン倍率、経験値吸引）を制作し、独立した`games/hell-runner-2.html`へXPバー・左右スロット・縦3択プレビューを実装。旧版とはランキング名・保存キーを分離
- 390×844の専用テスト`tests/hell-runner-2-ui.spec.js`成功。完成画面は`docs/mockups/hell-runner-2-ui-prototype.png`。カード内文字の重なりも実画面確認で修正済み。能力抽選・取得ロジックは未実装

- HELL RUNNERのローグライト能力システム方針を確定。メインRARE 2枠、補助COMMON 3枠、特殊LEGEND 1枠とし、枠が埋まった後は未取得能力を候補から除外する
- メインは3段ジャンプ・滑空・転生から2種類だけ取得。転生はレベル制にしない。ライバルは能力化せず、現在のギャグ系ステージギミックとして残す
- 特殊能力案はデスイーター（補助上限+1）、スロット解放（メイン枠+1）、修羅（固定高速+取得後得点倍率）、変換器（コインをXPへ変換）、ギャンブラー（停止なし自動取得+取得後得点倍率）の5種類
- レベルは無制限。全能力完成後は選択画面を出さず、内部でコイン報酬を抽選して頭上通知する。通常3択後も3秒カウントを使わず0.5～0.8秒程度の短い再開猶予を試作する
- 変換器の完成後報酬は通常の10000mあたり平均コイン得点を実測し、失う直接コイン点の70～80%程度を回収する初期目標で調整する。詳細は `docs/ゲーム企画・HELL-RUNNER次期アップデート案.md` 3.8～3.10
- 能力選択画面、通常プレイ中の6スロット、経験値バーをまとめた完成予想図を `docs/mockups/hell-runner-roguelite-ui-concept.png` に保存。選択カードは携帯向け縦3枚、通常スロットは中央視界を塞がない左右分割案

### HELL RUNNER(無印)は完成品。触らない

`games/temple-run-clone.html` は **v0.5で機能凍結**。ユーザーの明示的な指示がない限り
一切変更しない。入れてよいのは致命的なバグ修正だけ。

シンプルにスコアを狙う今のゲーム性をそのまま残したいというユーザーの判断による。
**ローグライト化はHELL RUNNER 2の機能**で、無印へは持ち込まない。
2本はコードの大半が同じため、片方だけ直すと必ず食い違う。横展開しないこと。

### 進行中

- **HELL RUNNER 2 ローグライト能力システム**。正本は `docs/HELL-RUNNER-2ローグライト仕様書.md`。無印向けの旧指示書 `docs/HELL-RUNNER-ローグライト能力システム指示書.md` は参照禁止
  - XP、候補抽選、能力取得、XPオーブ、メイン／補助／LEGENDの実効果、完成後Lucky!、開発者ツールまで実装済み
  - 今後はコインコンボの視覚表示と得点バランス、ゲームオーバー時HUD、通知位置を仕上げる

### 参考

- 新作3案（複合型音楽ゲーム、AIストーリー生成型テキスト放置ゲーム、テトリスブラスト）は `docs/ゲーム企画・HELL-RUNNER次期アップデート案.md` に整理済み。いずれも未着手
- 「旧十倍界王拳を選択式へ戻さない」はタイトル画面の旧アビリティ選択へ戻さない意味。ラン中に停止して3択ポップアップから取得する新ローグライト方式は別仕様
- 大ジャンプ(長押し)と滑空(落下中の長押し)が同じ操作になる件はユーザー確定済み。「そのまま滑空へつながる」を仕様とし、滑空したくない時は頂点付近で指を離す。入力を分ける仕組みは追加しない
- PC・携帯のポータル位置調整値は実機確認を経て本番へ反映済み。一時LANサーバーも最終確認後に停止済み
- 音量の本番値はiPhone実機試聴でBGM 0.30、ジャンプ 2.80、コイン 1.50、ライバル 2.00に最終確定
- ポータルの主要な品質改善は実装済み。今後は実機確認で気になる細部が出た場合のみ、光量・余白・文字サイズを微調整する
- ユーザーが両ゲームを公開し、プレイヤーの感想を集める段階
- 次回は寄せられたプレイ感・ランキング推移を見て調整する

### 新しい会話で最初に進める内容

1. `AGENTS.md` とこのファイルを最後まで読む
2. `docs/ダークネオンアーケード仕様書.md` と `docs/mockups/dark-neon-arcade-concept.png` を確認する
3. 実装済みのPC・スマホワイヤーフレームを確認し、必要なら配置や文言を調整する
4. 実画面でロゴ・背景・キービジュアルの重なりとトリミングを確認する
5. PC・スマホ実機で操作盤とCOMING SOON筐体を確認し、必要な場合のみ細部を微調整する

重要: モックアップ一枚を背景として貼らない。背景、ロゴ、キービジュアル、筐体装飾、HTML文字へ分解して実装する。

## 作業履歴の追記形式

作業後はこの下へ新しい日付を上にして追記する。

### 2026-08-21 — Codex（ポータル携帯版の位置確定）

- ユーザー実機調整値を携帯幅（599px以下）の本番初期値へ反映。挿入カセットはX=28px / Y=15px / 1.80x、タグはY=-141px、情報カウンターはY=-138pxを含む全21値を明示した
- 情報カウンターを上へ移動したことで残るフッター前の空白は、携帯限定で情報セクションの下余白と負の下マージンを対にして解消。390×844の実表示で情報カウンター末尾からフッターまで18pxに収まり、詰まりすぎないことを確認
- ポータルのPATCH NOTESへ2026.08.21の携帯レイアウト調整を追記。Discord通知はGitHubへpushした差分を自動送信する構成だが、ローカルにはWebhookが無いため、公開許可待ち
- 変更: `index.html`、`PROJECT-STATUS.md`
- 検証: `npm.cmd run test:portal`（10件成功）、`npm.cmd run verify`（4ファイル／インラインスクリプト7件、構文・構造・素材参照すべてOK）

### 2026-08-21 — Claude Code（ポータル刷新・HEXAMINE公開準備・軽量化）

**ポータルを筐体方式から実機＋カセット方式へ全面刷新した。**

- 中央のスケルトン携帯機1台へCSS製カセットを挿す形へ。差込口が光り、内部が
  ゲームの色に灯り、画面へ絵が出る。新作で増えるのは絵1枚だけになった
- サイト名を `VIVI GAME BASE` へ。背景は地下の秘密基地、ロゴはCSSの文字
- カセット置き場は折り返さず横スクロール。PCはドラッグ可。10本で実測確認
- HEXAMINE と HELL RUNNER 2（β）を掲載。稼働4本
- 筐体まわりのCSS71規則、デバッグコード52行、未使用画像10枚を削除

**HEXAMINEを公開できる状態にした。**

- 手順つきの誘導チュートリアル、間違えたマスを開かない仕様、長押しの誤爆対策
- ∞モードとデイリーを不定形の盤面へ。デイリーランキング（Supabase）を実装
- Discord通知へ追加。日付は日本時間で固定

**約20MB軽量化した。**

| | 前 | 後 |
|---|---|---|
| 討伐2048 | 7.2MB | 3.2MB（BGMを80kbpsへ） |
| HELL RUNNER 1 | 6.2MB | 1.8MB（未使用2.5MB削除＋WebP化） |
| ポータル素材 | 8.9MB | 359KB |

- HELL RUNNER 1 は凍結中だが、ユーザーの明示的な指示による。素材の再圧縮と
  未使用データの削除のみで、挙動は変えていない

**環境整備**

- `sharp` / `ffmpeg-static` を追加し、変換をスクリプト化
- コミット前に `npm run verify` を走らせるフックを追加（壊れたHTMLで実際に止まることを確認）
- git の `core.quotepath=false`。日本語ファイル名が読めるようになった
- 権限リストの使い捨て25件を汎用28件へ整理

**踏んだ罠（同じ事故を繰り返さないため）**

- **試作を既存ページへ持ち込むとき、クラス名の衝突が最大の事故要因。**
  `.shell` がページ幅コンテナと同名で、ヘッダーとヒーローが崩れた。`cs-` を付けて解決
- **発光の層は本体の絵より後に置く。** 同じ z-index ならDOM順が描画順になり、
  前に置くと不透明な絵に隠れて一度も見えない
- **ドラッグ追跡に `setPointerCapture` を使うと click が奪われる。** 追跡は window で行う
- CSSアニメーションはブラウザペインが非表示だと進まない。`resize_window` で復帰する

- 検証: `npm run verify` / `test:portal`(10) / `test:hex`(108) / `test:boss`(10) / `test:runner`(25) すべて成功

### 2026-08-19 — Codex（HELL RUNNER 2 収集物・開発者ツール調整）

- PCの開発者ツールを左右のゲーム外余白へ分割し、固定済みの音量・小ジャンプを非表示化。未確定のローグライト値だけを日本語説明付きで大きく調整できるようにした
- コイン／XPオーブの生成重複を防止し、整理されたコインパターン、高所コイン頻度、混色コイン列、サイズ連動の回収判定を実装。XP吸引は前方からの違和感を抑え、近距離加速へ変更
- 検証: HELL RUNNER 2専用Playwrightテスト3件、`npm run verify`、`git diff --check` 成功

### 2026-08-19 — Codex（HELL RUNNER 2 高所報酬・デバッグ移動修正）

- コンボ表示をXPバー内の小表示から、赤青のネオン枠・大きい連番・得点倍率・残り時間ゲージを持つ独立パネルへ置換
- `?debug=1`へ、コンボ本体のサイズ／X／Y、コンボゲージの幅／X／Y、実コインを待たずに表示を出す確認ボタンを追加
- メイン・補助枠が埋まった後、LEGEND抽選に外れたXP到達はLucky!コイン報酬に変更。LEGENDだけが出た時に見送った場合も同じ報酬を得て、デスイーター待ちのスコア稼ぎを成立させた
- 検証: HELL RUNNER 2専用Playwrightテスト7件、`npm run verify`、`git diff --check` 成功

### 2026-08-19 — Codex（HELL RUNNER 2 コンボ表示の初期実装）

- デスイーター取得時だけ、横方向を広げず上方向のアイテム吸引・取得判定を+210px補正。奈落上3層目を狙う用途に限定
- XPオーブを取るたびXPバーと数値を即時更新するよう修正。コイン連続取得はXPバー右端へ`COMBO x数`と残り時間ゲージを表示
- LEGENDだけが候補に残る場合は、欲しくないLEGENDを「今回は見送る」で取得せず通過可能。レジェンド抽選に外れたLEVEL UPは停止せず走行を継続
- LEVEL UPを開く際は、携帯の開発者ツールを自動で縮小してカードと見送りボタンを覆わないよう修正
- 検証: HELL RUNNER 2専用Playwrightテスト6件、`npm run verify`、`git diff --check` 成功

### 2026-08-19 — Codex（HELL RUNNER 2 高所報酬・デバッグ移動の初期修正）

- 3層目は経験値吸引の所持有無に関係なく、60,000m以降に出現するよう変更。2層目は20,000m以降のまま
- デバッグの距離移動で、能力ビルド・能力レベル・XP進行・ラン統計を引き継ぐよう修正。死神は距離到達ではなくLEGEND「デスイーター」取得中だけ追従する
- 空中でXPが満ちた場合は、その場で停止せず、次の着地を確認してからLEVEL UPを表示するよう変更
- XPバーとXP数値は内部XP自体は加算されるが、閾値到達時にしか描画更新していない未完了状態を確認。コインコンボの可視化と得点調整の工程で同時修正予定
- 検証: HELL RUNNER 2専用Playwrightテスト5件、`npm run verify`、`git diff --check` 成功

### 2026-08-19 — Codex（HELL RUNNER 2 初期高所報酬・デバッグ移動修正）

- XPオーブ取得音をコインと同じ基準音量へ合わせた。音色は魂を吸い込む上昇感を維持
- 収集物の基準を、サイズ2.20x・発光1.80x・高所高さ1.50x・コイン40%・オーブ20%・複数オーブ5%・高所コイン40%・高所オーブ20%・56pxグリッドへ更新。コイン価値1.50xは保留のまま
- 奈落上の報酬を最大3層化。2層目は20,000m、3層目は60,000mかつ経験値吸引Lv6以降だけに限定
- 地点移動時に古いLEVEL UP候補・一時停止状態を残さないよう修正し、レジェンド所持状態でも移動後に選択できる自動テストを追加
- 検証: HELL RUNNER 2専用Playwrightテスト4件、`npm run verify`、`git diff --check` 成功

### 2026-08-18 — Codex（HELL RUNNER 2 ローグライト基盤・地形判別）

- HELL RUNNER 2だけに、ラン中XP、3択、メイン／補助／LEGENDスロット、5種のLEGEND、完成後Lucky!報酬を実装。通常版HELL RUNNERは変更していない
- XP吸引を実際の追尾として見える化し、初期吸引範囲を狭めて、Lv上昇で高所アイテムに届くよう調整。コイン・XPオーブの表示、配置高、出現間隔も見直した
- 地形を小段差とジャンプ必須の大段差へ二極化。大段差の上り壁には朱色・金色の縁取りを追加し、空中横壁の通過例外をなくした
- `?debug=1`へ完成ビルド、横壁再現、ローグライト数値調整を追加。通常URLには露出しない
- 検証: HELL RUNNER 2専用Playwrightテスト3件、`npm run verify`、`git diff --check` 成功

### 2026-08-17 — Codex（討伐2048配置確定・エフェクト追従）

- ユーザー実機調整の停止ボタン、勇者、ミミック、ドラゴン、魔王のサイズ・X・Yをデバッグ初期値と通常プレイへ反映
- ボスの接地点と表示高から共通の中心座標を算出し、攻撃エフェクト、ダメージ数字、被弾・回復・撃破パーティクルを3ボスそれぞれへ追従させた
- 勇者の位置調整に足元の床、ボス位置調整に背景光も追従。攻撃画像とパーティクルの広がりは各ボスの拡大率に合わせる
- HELL RUNNERと同じ `formatHudNumber` を導入し、チャレンジ中・ゲームオーバー時のスコアとベストをK／M表記へ統一
- 検証: 討伐2048専用テスト10件成功、確定3ボスのエフェクト中心座標とK／M境界値を確認、PC目視・`npm run test:syntax`・`git diff --check` 成功

### 2026-08-17 — Codex（討伐2048サイズ調整デバッグ）

- `?debug=1`限定のSIZE TUNERを追加し、停止ボタンはサイズ/X/Y、勇者は共通サイズ/X/Y、ミミック・ドラゴン・魔王はそれぞれ独立したサイズ/X/Yをリアルタイム調整可能にした
- 全15項目のスライダーと直接数値入力を双方向同期し、COPY VALUESで対象別に整理した共有用JSONを表示。Clipboard APIが使えない携帯でも手動コピー可能
- PCではゲーム右側、携帯では初期縮小表示とし、通常URLではパネルと調整APIを非公開
- デバッグ中は全3ボスを一時解放するが、`bossDemoUnlockedStages`へ書き込まず通常プレイの進行を汚さない
- 検証: 討伐2048専用テスト9件成功、PC目視で全ボスと調整パネルを確認、`npm run test:syntax`・`git diff --check` 成功

### 2026-08-17 — Codex（討伐2048タイル移動エフェクト改善）

- `slideAndMergeGrid()` が出力タイルごとの元位置を返すようにし、合体時は遠い側の元位置を採用
- 盤面全体の移動元を `gridMoveFrom` へ保存し、描画時に各タイル固有の距離を計算。静止タイルと新規出現タイルには移動オフセットをかけない
- 既存のアニメーション速度・ease-out・合体ポップ・スコア計算は変更せず、使われなくなった全体移動方向の状態を削除
- デバッグURL限定で合体処理と方向座標変換を検査可能にし、通常URLでは引き続き非公開
- 検証: 討伐2048専用テスト4件成功、4方向の座標変換と合体元追跡を確認、`npm run test:syntax` 成功、HTML末尾・script 2/2・`git diff --check` 成功

### 2026-08-17 — Codex（討伐2048一時停止・音量設定）

- `images/stop-button-magic-blue.png` を戦闘画面左上へ配置し、HPバーと重ならない64×64pxの停止操作を追加
- PAUSE画面へPLAY／RETRY／HOMEと、BGM／効果音の0〜100音量ゲージを追加。設定は端末へ個別保存
- 一時停止中はグリッド入力、Canvas演出、合体後の攻撃・回復・勝敗タイマー、AudioContextを停止。P／Escapeとアプリ非表示時の自動停止にも対応
- PCと390×844の携帯表示を目視確認し、停止メニューが1画面に収まることを確認。ブラウザの警告・エラーなし
- 検証: 討伐2048専用テスト7件成功、`npm run test:syntax` 成功、HTML末尾・script 2/2・`git diff --check` 成功

### 2026-08-16 — Codex（HELL RUNNER一時停止機能）

- プレイ中だけ表示する48×48pxのⅡボタンと、既存START/HOME画像を流用した一時停止メニューを追加
- PLAY後は停止地点を維持したまま3秒カウントし、完了後に再開。P/Escapeも同じ経路へ統一
- タブ・アプリが非表示になった場合は自動停止し、復帰しても自動再開しないよう対応
- 停止中はBGMの再生位置を保持し、カウント開始時に同じ位置から復帰。ゲーム全体のサウンドON/OFFを保存可能にした
- `clearJumpInput()` と `jumpCanBeCut = false` を併用し、空中停止後に大ジャンプが勝手に小ジャンプへ変わらないよう保護
- `paused` / `pauseCountdown` 中は物理だけでなく粒子・コウモリ・`animT` も停止し、現在距離の背景を維持
- 正式な128×128透過ポーズボタンへ差し替え、デバッグモードに28〜96pxのサイズ調整・直接入力・コピーを追加。PC・携帯共通の本番値は96pxに確定
- PCの主操作をSpaceキーとしてタイトル画面と遊び方へ明記し、クリック操作も併用可能なまま維持。携帯にはPC向け文言を表示しない
- 検証: `tests/debug-mode.spec.js` 17件成功、ポーズサイズ調整の関連テスト成功、`npm run test:syntax` 成功

### 2026-08-16 — Codex（HELL RUNNER大小ジャンプ試遊版）

- 上昇中に入力を離すと残り上向き速度をカットする方式で、短押しの小ジャンプと長押しの従来相当大ジャンプを実装
- イベント順に依存せず毎フレーム `holdingInput` を見ることで、`pointerdown → pointerup → update()` の最速タップも小ジャンプにした
- pointerとkeyboardの押下元を分離し、携帯では最初のpointerIdだけを採用。2本目の指のpointerupで意図せずカットされないよう修正
- 2段目・レッドブルの3段目へ同じ大小判定を適用。長押しだけで次段を自動消費しない既存キーリピート対策を維持
- DEV TOOLSへ `JUMP HEIGHT` を追加。`RELEASE CUT`、実測AIRTIME/DISTANCE/HEIGHT、JUMPS、MODE、設定値コピーを実装
- LAN内HTTPで自動コピーできない携帯向けに、手動コピー用の選択済みテキスト欄へフォールバック
- 携帯では項目増加後もタイトル画面のPLAYを塞がないよう、デバッグパネルを初期縮小
- 遊び方とタップヒントを短押し／長押しへ更新し、滑空説明を「落下中の長押し」へ明確化
- `JUMP_RELEASE_CUT` はPC・携帯の実機試遊結果を反映し、0.50で確定
- 検証: `tests/debug-mode.spec.js` 14件成功、`npm run test:syntax` 成功、HTML末尾 `</html>`、script 2/2、`git diff --check` 成功

### 2026-08-16 — Claude Code（成果物の保全・指示書レビュー・キーリピート修正）

- Git管理外だったポータル刷新の成果物を取り込み。`images/portal/` の公開素材14点、`tools/build_portal_titles.py`、`tests/portal.spec.js`、一時停止機能指示書。**素材が欠けると実機で確定した位置調整値を再現できないため**
- `images/portal/source/`(42MB)は `.gitignore` へ追加。世代違いの中間生成物が大半で、一度履歴へ入れると消せないため。**原本はディスク上に残している**
- 筐体の一体型画像化で使われなくなった `.control-panel` / `.coming-screen` / `.coming-controls` を削除。これらが参照する `control-deck-*.webp` と `coming-soon-cabinet.webp` はすでに存在しないファイルだった。適用されないルールの `url()` は取得されないため実害は無かったが、素材の欠落と誤認されるため除去
- `keydown` に `e.repeat` ガードを追加し、キーリピートで2段・3段ジャンプが勝手に消費されるバグを修正。滞空約0.69秒に対しリピート開始が約0.5秒後のため、空中で実際に発火していた。タイトル・ゲームオーバーでの `startGame()` 連打も同時に防止
- 一時停止機能指示書へ「12. 実装アンカー」を追記。**パーティクルとコウモリの扱いが指示書と既存コードで矛盾していた**(既存は転生カウント中も動かす意図的な作り)。指示書側を優先し転生時の挙動は変更しない方針を明記。あわせて `animT` の加算条件も直さないと背面が静止しない点、`reviveCountdown` が変数ではなく `state` の値である点、巨大 `dt` 対策が `Math.min(0.033, ...)` で既に入っている点を記録
- 大小ジャンプ指示書へ「15. レビュー結果」を追記。**「最低カット受付時間を経過済み」をカット条件にしていたため、速いタップほど大ジャンプになる欠陥**を指摘。ジャンプは `update()` 内で次フレームに発動するため、素早いタップでは押下時間が事実上0になり必ず下限を下回る。時間で門を作らず常にカットし高さの下限で守る方式を提案
- 同指示書に小ジャンプの設計意図を確定して追記。**本質は「高さ」ではなく「滞空時間＝着地点」の調整**。用途は(1)早く着地して `jumpsUsed=0` に戻し満タンで奈落へ入る、(2)高速域で760px跳ぶ2段目を小さく撃ち、190〜430px(狭い足場は56〜102px)の足場へ降りる、の2点。これに伴い4節の調整目標の単位が誤りと判明。カット率kに対し滞空時間と水平距離はk倍だが高さはk²倍のため、「頂点55〜65%」では水平距離が74〜81%までしか縮まず用途(2)に届かない。目標を滞空時間・水平距離で50〜60%へ定義し直した
- 検証: `git diff --check` 相当の空白エラーなし、`index.html` と本体の `<script>` 対応・`</html>` 終端を確認、ポータルが参照する全画像の実在を確認。**Playwrightと構文検査は未実行**(この環境から `node` が見えないため)。次に触る担当が `npm test` / `npm run test:syntax` を実行すること

### 2026-08-16 — Codex（ポータル位置調整モード）

- 筐体へ焼き込まれていた3タイトルを空看板へ戻し、BIZ UDゴシック Bold・固定文字高の独立透過WebPへ変更
- `?portalDebug=1` 限定のPORTAL TUNERを追加。HELL・討伐2048・COMING SOONのタイトルと、2作品のPLAYをX・Y・SIZEで個別調整可能
- 調整値をJSONで取得する `COPY VALUES`、初期値へ戻す `RESET`、携帯でも使えるパネル縮小ボタンを追加
- ユーザーがPCで確定したタイトル・PLAY値を、幅600px以上に限定した本番初期値へ反映
- HELL・討伐2048・COMING SOON各筐体のX・Y・SIZE調整を追加し、COMING SOONが上へ重なる負の余白を解除
- 各調整スライダーの横を直接編集できる数値入力欄へ変更し、両方を同期
- 同一Wi-Fi上のHTTP接続ではClipboard APIが無効になるため、COPY時に選択済みJSON欄を表示する手動コピー代替を追加
- 携帯の手書き調整値をスクリーンショットと照合し、3タイトルと2つのPLAYへ幅599px以下限定で反映
- PLAY画像だけを縮小する構造へ変更し、見た目を筐体差込口へ合わせつつ56pxのタップ領域を保持
- 旧PLAY反射レイヤーを画像ボタンでは完全に無効化し、筐体外へ四角い光がはみ出さないよう修正
- ステータス行を2段構成へ変更し、NOW PLAYINGとタグが中途半端に折り返されないよう固定
- PLAYの大きなタップ領域まで発光していた問題を修正し、hover・押下演出を実画像だけへ限定
- 透過タイトル3枚は1024×192、四隅アルファ0、完全透明画素ありを検査
- 検証: ポータル専用8件成功、筐体移動・数値入力同期・Clipboard APIなしでの手動コピー表示を確認、`npm run test:syntax` 成功、差分の空白エラーなし

### 2026-08-16 — Codex（ポータルワイヤーフレーム）

- `index.html` を `VIVI GAME ARCADE` のダークネオンアーケード構成へ刷新
- PCは2筐体横並び、スマホは1列とし、素材未配置時にもゲーム名・説明・PLAYが表示されるフォールバックを実装
- NEWS、PATCH NOTES、CREDITS、ABOUTをキーボード操作可能なモーダルへ整理し、既存の更新履歴とクレジットを移行
- 公開用素材フォルダ `images/portal/` を作成し、仕様書で定めた画像ファイルを置くだけで反映される参照先を準備
- 検証: `npm run test:syntax` 成功、差分の空白エラーなし、2作品のリンク先ファイルと4モーダルの対応を確認

### 2026-08-16 — Codex（ポータル背景素材）

- ダークネオンアーケードのPC用背景と、同じ世界観のスマホ専用縦構図を画像生成
- PNG原本を `images/portal/source/`、公開用WebPを `images/portal/` へ保存
- 公開用をPC 2560×1440・182KB、スマホ1080×1920・110KBへ変換し、中央上部のロゴ用暗部と左右の筐体列を確認
- ロゴ案は正確な文字で生成できたが、透過背景が実画像化されていたため不採用。誤った素材を本番へ入れずHTML文字を継続
- 背景PC版・スマホ版はユーザー確認を経て正式採用
- ロゴの指示書を改訂し、市松模様などの疑似透過を禁止。実アルファ値と重ね表示で合否判定する工程を明文化
- 改訂した指示でロゴを再生成し、1600×600のRGBA PNG、四隅アルファ0、完全透明画素58.9%を確認して正式採用
- `images/portal/vivi-game-arcade-logo.png` をポータルへ組み込み、読み込み失敗時にはHTML文字が残る構造を維持
- ロゴ画像の読み込み完了を検知して背面のHTML文字を隠すよう修正。PC・390pxスマホの実表示で二重表示なし、横スクロールなしを確認
- PC背景は入口ヒーローの680pxまで表示して暗くフェードし、それ以降はキービジュアルと情報を主役にする現在の構成を正式採用。下部用の別背景画像は追加しない
- HELL RUNNERの既存地獄背景・死神素材を参照し、右向きの骸骨ランナーと追跡する死神のキービジュアルを制作
- 討伐2048は数字を画像へ焼き込まず、魔法タイルと青紫の巨大ボスでゲーム内容を表現
- PNG原本を `images/portal/source/`、1600×900の公開用WebPを `images/portal/` へ保存。HELL RUNNER 255KB、討伐2048 193KB

### 2026-08-16 — Codex（ポータルUI仕上げ）

- 追加画像を増やさず、筐体画面下へ金属製コントロールデッキ風の区切りをCSSで追加
- PLAYボタンへ内側フレーム、左右ランプ、hover時だけ通過する弱い反射を追加し、ゲーム固有色を維持
- INFORMATION COUNTERを一体型の金属パネルにまとめ、各項目へ統一アイコン枠・走査線・矢印・フォーカス可能な操作反応を追加
- スマホでは装飾の余白を縮め、2列配置と56px以上のPLAYタップ領域を維持
- `prefers-reduced-motion` の既存設定により、反射移動を含むアニメーションは停止可能

### 2026-08-16 — Codex（ポータル文言・テスト）

- NEWSをリニューアル告知と2作品の稼働情報へ更新し、ABOUTへCOMING SOONの扱いを追記
- フッターへ制作者表記、GitHub、クレジット入口、サイトバージョンを整理
- `tests/portal.spec.js` を追加し、PCの2筐体比較、スマホ1列・横はみ出しなし・PLAY高さ56px以上を検証
- ロゴ画像の取得失敗時にHTMLタイトルへ戻ること、情報モーダル開閉後にスクロールが戻ることを検証
- 背景2枚、ロゴ、キービジュアル2枚のHTTP取得を検証
- テスト結果: ポータル専用5件成功、`npm run test:syntax` 成功、差分の空白エラーなし

### 2026-08-16 — Codex（公開前最終検証）

- ポータル、HELL RUNNER、討伐2048、背景比較を含む全25件を実行し、すべて成功
- `npm run test:syntax` 成功、差分の空白エラーなし
- ポータル刷新のローカル実装・素材制作・検証は完了。コミット・push・公開は未実施

### 2026-08-16 — Codex（完成予想図との再比較）

- PC実画面を `docs/mockups/dark-neon-arcade-concept.png` と再比較
- 背景・ロゴ・キービジュアル・基本配色・操作性は良好だが、筐体が平面的なWebカードに見え、完成予想図の物理筐体らしさが不足
- ジョイスティックとボタンを含む操作盤、筐体脚部、専用COMING SOON筐体、画面反射・金属質感、小アイコンの統一が未完了
- 現在版はPCで全高1855pxあり、完成予想図のように1画面内でロゴ・2筐体・COMING SOON・情報カウンターを俯瞰できない
- 自己評価は総合68/100。公開完成扱いを取り消し、追加素材とレイアウト再設計を次工程とする

### 2026-08-16 — Codex（ポータル品質向上）

- 2台のゲーム筐体へ立体的な操作盤、ジョイスティック、4ボタン、脚部を追加し、完成予想図のアーケード筐体らしさへ近づけた
- 消灯したCRT画面のCOMING SOON専用素材を生成し、操作部を備えたミニ筐体として配置
- PC版のロゴ・筐体・情報カウンターの余白と寸法を再設計し、全高を約1855pxから約1063pxへ圧縮。1440×1000では情報カウンター下端まで表示可能
- スマホ版は説明文とタグを維持し、390×844で横はみ出しなし、PLAY高さ60px、操作盤表示を確認
- 公開画像の取得テストへCOMING SOON素材を追加

### 2026-08-16 — Codex（操作盤の画像素材化）

- CSSの単純な図形で描いていたジョイスティック・ボタン・操作盤を廃止し、透過WebP素材へ置換
- HELL RUNNER用は傷のある黒鉄と赤熱光、討伐2048用は青紫の魔導金属、COMING SOON用は無点灯の黒鉄として3種類を個別制作
- PNG原本を `images/portal/source/`、公開用透過WebPを `images/portal/` へ保存。全素材でアルファ0〜255と完全透明画素を確認
- 透明余白を公開用変換時に切り詰め、PCでは筐体幅いっぱい、390pxスマホでは325×108pxで細部が読める寸法へ調整
- HTMLのPLAYボタンとリンクは画像へ焼き込まず維持し、操作性・フォーカス・レスポンシブを保った
- 公開画像取得テストへ3種類の操作盤素材を追加

### 2026-08-16 — Codex（筐体全体の画像素材化）

- HELL RUNNER、討伐2048、COMING SOONの3台を、看板・画面・操作盤・下部外装・脚まで含む一体型筐体画像として制作
- ゲーム名は筐体看板へ正確に収録。COMING SOONは青紫に発光するアナログ砂嵐CRTとして制作
- 2作品のPLAYは筐体画像へ焼き込まず、ゲーム別の独立した透過ボタン画像を制作。筐体の空き枠へ割合座標で重ね、画像全体をリンク領域にした
- 生成直後に市松模様を描いた原本は不採用。HELL RUNNER筐体、討伐2048筐体、COMING SOON筐体、PLAYボタン2種の全5素材で、生成原本の実アルファ・四隅0・完全透明画素・暗色/明色背景表示を検査
- 透過生成共通ルールを仕様書へ追加し、後処理で透明化した試作を「生成時点の透過合格」と扱わないことを明文化
- スマホではPLAY画像が小さくてもリンク領域を最低56pxへ拡張し、押しやすさを維持

### 2026-08-16 — Codex（筐体・PLAY寸法の統一）

- 透過後の素材切り詰めとCOMING SOON専用CSSにより、3筐体の実表示サイズとPLAY2種の比率が不揃いだったことを確認
- 生成原本は維持したまま、公開用3筐体を同一1200×1680キャンバス・同一1080×1580外形・同じ床面へ正規化
- PLAY2種も同一1024×256キャンバス・同一960×240外形へ正規化し、リンク配置を左22%・上69.5%・幅56%・比率4:1の共通値へ変更
- COMING SOONのPC表示幅をほか2台と同じ461pxへ変更
- 1440px PCで3筐体すべて461×645.39px、PLAY2種とも258.16×64.53px、横はみ出しなしを確認
- 筐体3台とPLAY2種の表示寸法が一致することをポータル自動テストへ追加

### 2026-08-16 — Codex（共通マスターから筐体を新規制作）

- 個別生成による外形差を解消するため、空看板・空画面・操作盤・PLAY差込口・脚を固定した共通透過マスターを新規制作
- HELL RUNNER、討伐2048、COMING SOONは同じマスター形状へ各画面と発光色だけを適用し、アルファマスク・原寸・外形・床面を完全共通化
- 生成編集時に透明が失われる問題が再発したため、合格済みマスターのアルファを3台へ共通適用する正式工程へ変更。最終原本3枚は1054×1493、四隅0、同一アルファ境界
- ゲームタイトルはBIZ UDゴシック Bold、文字高76px、同一中心座標で正確に合成。HELL RUNNER、討伐2048、COMING SOONのフォントとサイズ感を統一
- PLAYは筐体側の黒い角丸差込口へ収まる、外枠・ボルト・側面ランプなしの角丸ガラス板へ変更
- PLAY2種は同一1024×256、BIZ UDゴシック Bold、文字高90px、同一中心座標。テーマごとの差は赤・紫の発光色だけ
- PC実測は3筐体すべて461×645.39px、PLAYリンク2種184.39×46.09px。390px携帯は3筐体幅343px、PLAY画像137.19×34.30px、リンク領域137.19×56px、横はみ出しなし
- 旧v2/v3/v4筐体・旧PLAY・旧CSS操作盤・旧COMING SOON画面・旧比較画像の計21ファイルを削除。削除済み素材を公開取得テストから除外

### 2026-08-16 — Codex

- ゲーム置き場を `VIVI GAME ARCADE` として刷新するデザイン・レスポンシブ・素材サイズ・制作ルールを仕様書化し、PC版コンセプトモックアップを保存
- iPhone SafariでもBGM音量が効くよう、BGMをWeb Audio経由へ統一
- HELL RUNNERのデバッグモードへ3系統の音量スライダー・現在値表示・設定値コピーを追加
- 実機試聴でBGM 0.14、ジャンプ/コイン 1.5、ライバル 2.0に確定し、デバッグモードへ落下しても確認を継続できる `NO FALL` 切替を追加
- デバッグパネルをPCではゲーム外の右余白へ移し、携帯向けの縮小切替とJUMP・COINの個別ミキサーを追加
- 携帯でデバッグパネルを縦スクロールできず音量欄へ到達できなかった問題を修正し、画面高さいっぱいまで表示可能に変更
- iPhone実機の個別ミキサーで最終調整し、本番音量をBGM 0.30、ジャンプ 2.80、コイン 1.50、ライバル 2.00に確定
- 同じ項目を反復調整する場合、毎回コードを直す前にデバッグ機能で簡易化できないか提案するルールを `AGENTS.md` に追加
- 関連テストのみ実行し、構文検査とデバッグ非公開・BGMクロスフェード・音量スライダーの確認に成功

### 2026-08-15 — Codex

- 公開先を確認したところ、GitHubには旧3ファイルしかなく `games/`・`images/`・`audio/` が未配置。次回公開時はフォルダごと反映が必要
- HELL RUNNERのモバイルHUDを折り返し禁止・均等幅へ変更し、ゲームオーバーとホームではHUD自体を非表示化
- タイトル画面を上端基準のスクロール配置へ変更し、ロゴと3ボタンをモバイル向けに縮小
- 公開入口の `index.html` はルートに残し、2本のゲーム本体を `games/` フォルダへ整理
- ポータル、画像、BGM、ホーム導線、テスト、指示書の参照先を新配置へ更新
- HELL RUNNERの公開画面をスマホ比率・固定描画密度・10%引きへ統一
- HUDのK表記、デバッグ用スクリーンショット、30000〜100000mの緩やかな加速を追加
- 討伐2048のランキング得点をゲーム終了時の盤面合計へ変更
- 構文検査を両ゲームへ拡張し、Playwrightを16件へ追加
- テスト結果: `npm run test:syntax` 成功、`npm test` 16件成功
