# PROJECT STATUS — Codex / Claude Code共通引き継ぎ

最終更新: 2026-08-28 / Codex

このファイルが作業状況の正本。Codex・Claude Codeとも、作業前に読み、作業後に更新する。
詳細な制約は `AGENTS.md` を優先する。特に巨大HTMLの全体読み込み・全体整形は禁止。
git操作は通常行わず、ユーザーから明示的に許可された場合のみ可能。

## 現在の状態

## 新規開発の現在地（2026-08-28）

- 既存ゲームは不具合報告が来るまで凍結を継続。`games/temple-run-clone.html`（HELL RUNNER無印）は完成品のため、致命的不具合以外は触らない
- Steam販売を目標にする長期本命は、終末世界で瓦礫をマージし、廃墟を復旧して生活圏を再生するカード型・自動化サバイバル。Phaser 4で別プロジェクトとして進行する
- `本日の最強決定戦 Remaster`は、現行版を変えずPhaser 4で別実装中。実装の正本は`docs/最強決定戦Remaster_実装状況.md`、画面方針は`docs/最強決定戦Remaster_画面構成と演出方針.md`
- Remasterは雨のネオン交差点、透過画像の`3 → 2 → 1 → FIGHT`、7キャラ全ポーズと固有攻撃エフェクト、PC左右余白の`?debug=1`調整パネルまで実装済み。PC基準の全ポーズ・エフェクト初期値も反映済み
- Remasterの通常戦闘は正面左右対面。信号機を背景上へCSSで重ねる方式は機種差のずれが再発するため**採用禁止**。カウントはPhaserの同一キャンバス内の透過画像だけで表示する
- Remasterの次は、ネオンゲート式の最終選択画面、結果／ランキング画面、音・ボイス接続、PC／iPhone／Androidの実機確認。公開はユーザーの明示`push`時だけ

## 作業停止中の現状サマリー（2026-08-26）

ユーザー方針により、**既存ゲームは不具合報告が来るまで変更しない**。再開時は、報告されたゲームと症状だけを対象にする。見た目の改善・横展開・リファクタリングを自主的に始めない。

### 共通の再開手順

- 最初にこのファイルと`AGENTS.md`を読む。巨大HTMLは全読込せず、`rg`で場所を特定してから周辺だけを確認する
- 変更後は対象ゲームのPlaywrightテスト、`npm run verify`、`git diff --check`を実行する
- 公開はユーザーが明示的に`push`と言った時だけ。一時worktreeから対象ファイルだけを公開し、元フォルダで`git add .`／reset／checkoutをしない
- 通常作業フォルダはGitHub最新版へ同期済み。`tools/ranking-state.json`はランキング通知のローカル履歴なので、通常の未保存変更表示から除外している

### ゲーム別の到達点と保留事項

| 対象 | 現在の状態 | 次に触る条件 |
| --- | --- | --- |
| `games/temple-run-clone.html`（HELL RUNNER） | **完成・凍結**。致命的に遊べない不具合以外は変更禁止 | 明示的な致命的不具合報告のみ |
| `games/hell-runner-2.html`（HELL RUNNER 2） | ローグライト版。実装・実機調整は`docs/HELL-RUNNER-2引き継ぎ.md`が正本 | ユーザーの実機バグ報告または再開指示 |
| `games/boss-battle-demo.html`（討伐2048） | ランキング、停止、音量、SIZE TUNERまで実装済み | 実機バグ報告のみ |
| `games/hexamine.html`（HEXAMINE） | PCは左クリックだけで判定。右・ホイールクリックの誤答は防止済み | 実機バグ報告のみ |
| `games/todays-champion.html`（本日の最強決定戦） | 下記の最新仕様まで実装済み。公開画面のDEV導線は削除済み | 登録成否、PC／携帯表示、音声の不具合報告 |
| `games/rhythm-game-test.html`（音ゲー試作） | 未公開・保留。明示指示があるまで完全に対象外 | ユーザーから再開指示が来た時だけ |

### 本日の最強決定戦：確定済みの最新仕様

- 通常URLでは開発ツール・`OPEN DEV TUNER`リンクを出さない。調整は`games/todays-champion.html?debug=1`だけで行う
- 信号、結果ボタン、AUDIO MIX、キャラ別VOICEはPC／携帯で別プロファイル。PCは結果SELECT`(76,-50)`・KIRI VOICE`0.74`・信号GROUP`(4,1)`／RED`(1.5,8)`／YELLOW`(2.2,8)`／GREEN`(3,8)`／SIZE`2.68`。携帯は既存の別値を維持する
- ランキングは0〜3000msを許可。本番Supabaseでは、0ms対応の制約変更と`anon`／`authenticated`への公開INSERT権限・採番権限を**ユーザーがSQL Editorで実行済み**
- 登録失敗時はコードを表示する。`23514`は記録値制限、`42501`は公開権限。次の王者撃破時に実プレイヤー登録で成否を確認する
- 素材は対戦開始時にプレイヤーと相手の2キャラ分だけdecodeしてから開始するため、初回戦の縮小・反転崩れを防ぎつつ約4.6MBの一括先読みを避ける
- 接続済みボイスはRAVEN（選択・開始・勝利）、KIRI（選択・開始・勝利）、BRICK（選択）。MIKA／NOISEは未到着。ブリックの現行選択ボイスは録り直し待ち、キリ勝利は元録音由来のジリつきが残る仮版
- 専用確認コマンド: `npx playwright test tests/todays-champion.spec.js`（現行20件）、`npm run verify`

### HELL RUNNER 2（`games/hell-runner-2.html`）

- 再開時は [HELL-RUNNER-2引き継ぎ.md](docs/HELL-RUNNER-2引き継ぎ.md) を最初に読む。完了範囲、未確定の数値、実機確認手順を現状へ整理済み
- HELL RUNNER 1とは完全に別ファイルのローグライト版。ラン中XP・3択・メイン2枠／補助3枠／LEGEND1枠、育成完了後のLucky!報酬、開発者ツールまで実装済み。**2026-08-26に開発終了**（バグ報告があった時だけ触る）
- ポーズ画面のHOMEでタイトルへ戻る時、直前のCOIN COMBO HUDが残る不具合を修正。HOME遷移でコンボ値を0へ戻し、コンボ／SOUL BANKの表示判定を更新する。専用Playwrightテストで再発防止済み
- 収集物の基準値は実機確認済みのサイズ2.20x、発光1.80x、高所高さ1.50x、コイン40%、オーブ20%、複数オーブ5%、高所コイン40%、高所オーブ20%、間隔56pxへ設定。コイン価値1.50xはそのまま確定値
- 高所アイテムは奈落上のみ。1層目は常時、2層目は20,000m以降、3層目は60,000m以降に出現する
- デバッグ地点移動は能力ビルド・XP進行を保持し、古い能力選択オーバーレイだけを確実に解除する
- 追従する死神は距離ギミックではなく、LEGEND「デスイーター」取得中だけ表示する。XP到達が空中なら、着地してからLEVEL UPを開く
- XPバーはオーブ取得ごとに即時更新。コインコンボはプレイ中常時、外装なしのコンボ数・倍率・残り時間ゲージだけを表示し、未継続時は`00`。連続数で青→緑→金→赤紫へ変化し、8コンボ以上では数字だけが流れる虹色になる。`?debug=1`で基本持続時間、本体・ゲージのサイズと位置を個別調整できる。LEGENDのみの候補も見送れる
- コンボUIの確定初期値は、基本持続3.00秒、表示位置X84% / Y22%、本体・ゲージ倍率1.00x、ゲージオフセット0px
- HELL RUNNER 2専用Playwrightは最新9件成功。通知はXPバー下、ゲームオーバー時はXP・能力・コンボ・通知をすべて隠すことを自動確認済み
- `?debug=1`にスコア完成ビルドと変換器完成ビルドを追加。同じ完成後状態からLucky!回数と得点を比較でき、ラン統計も距離・合計スコア・通常コイン・Lucky!・通常オーブXP・変換器XPへ分解表示する
- 同じ出現率・距離帯で比較するため、`10K比較を開始`→10,000m走行→`10K結果をコピー`を追加。地点移動を挟むと計測を破棄し、混在した結果を残さない
- 比較プリセットは、完成ビルド表示なのに必要XPだけ初期24のまま残る問題を修正。最初のLucky!確認後は、完成時の能力取得数に対応する終盤XPしきい値へ戻る
- 変換器取得後もスコアに無関係なコインコンボ表示だけが反応する不整合を修正。変換後のコインはXPだけへ変換し、既存コンボも即時`00`へ戻す
- 変換器の完成後価値を追加。完成後の必要XPは最大800XPで頭打ちにし、変換XPは魂BANKへ蓄積、次の`SOUL LUCKY!`で`変換XP×5.20点`を基礎Lucky!へ加算して還元する。両値は開発者ツールで調整可能
- 変換器専用の`SOUL BANK` HUDを追加。取得中だけXPバー下に表示し、魂BANK量と800XP基準の充填ゲージを視覚化。約80%以上で発光し、SOUL LUCKY!後は空へ戻る。ゲームオーバー時の非表示も専用テストで確認
- SOUL BANKを横バーから透過魔導瓶へ刷新。`images/hell-runner-2/ui/soul-bank-vessel.png`に紫の悪魔金属フレーム・透明な中心瓶を生成し、CSSの魂火が内側を下から満たす。原本は`images/hell-runner-2/source/ui/`。1254×1254 RGBA・アルファ0〜255を確認済み
- 魔導瓶の充填演出を追加。約80%から脈動し、満タンでは瓶口から魂が漏れる。SOUL LUCKY!では白紫の割れフラッシュ・揺れ・放射粒子のあとにBANKを空へ戻す
- 変換器中のCOIN COMBOを非表示化し、SOUL BANKを同じ右上領域へ差し替え。素材の透明な胴内に合わせて魂火の幅・高さ・底位置を再調整し、フレーム外へ漏れないようにした
- SOUL BANKの文字とBANK数値は能力アイコンに重なっていたため、瓶の下へ移動。瓶そのものの位置はコンボ差し替え位置のまま維持
- 100K帯10K比較で、スコア完成14,110（COIN +4,064 / Lucky +46）対変換器完成13,850（Lucky +92 / SOUL +3,758）を確認。差約1.8%で目標±10%内のため、完成後XP上限800・魂換金5.20点/XPを初期採用
- スコア・変換器の数値調整フェーズを完了。次は`docs/HELL-RUNNER-2実機テスト項目.md`の基本感触、抽選、全LEGEND、PC/携帯HUDの実機確認を行い、指摘箇所だけを最終調整する
- 修羅・ギャンブラー倍率がコイン/Lucky!だけにしか掛からず、距離スコアに反映されていない不整合を修正。総スコア倍率として修羅1.30x、ギャンブラー1.05xを初期候補にし、開発者ツールへ個別スライダーを追加
- `?debug=1`へ修羅・ギャンブラー完成ビルドを追加。修羅は3段ジャンプ・滑空＋コイン倍率／コンボ／持続、ギャンブラーは3段ジャンプ・滑空＋コイン倍率／コンボ持続／経験値倍率の混成で、転生を取らない同条件の10K比較ができる
- 修羅・ギャンブラーの総スコア倍率は取得後の得点だけへ適用するよう修正。終盤で取った時に過去の距離まで遡及してスコアが跳ねる抜け道をなくし、デバッグ地点移動でも移動後地点を新しい倍率起点にする
- 100K帯10K実機比較（修正後）は修羅完成+17,032、ギャンブラー完成+13,643。既存のスコア完成+14,110／変換器完成+13,850と併せ、修羅は早期取得で大化けする高リスク枠、ギャンブラーは選択不要の代償に少し弱い枠として初期倍率を維持する
- ギャンブラーは上記比較後にさらに弱めた。最高倍率は従来の1.05xのまま、`0.85〜1.05x`を0.05刻み・均等抽選（平均0.95x）に変更。取得時と10K結果に確定倍率を表示するため、選択不要の気軽さと下振れを両方明示できる
- LEGENDの通常出現率を17%から25%へ変更。さらに未取得のまま90,000mへ到達した場合、最初の安全な着地でLEGEND候補を必ず出す天井を追加した。XP到達を待たずに起動するため、100,000mまでに必ず一度はLEGENDを見られる。100K比較用のデバッグ地点移動では天井を消化済みにして、完成ビルド操作を妨げない
- LEGEND候補を一度でも見送った後の再出現率を35%へ設定。さらにLEVEL UPの各カード下に1回限りの`⟳`を追加し、同ティアの未提示能力へだけ引き直せるようにした。LEGEND待ちの理不尽さを減らしつつ、無限リロードによる確定取得は防ぐ。再出現率は開発者ツールでも調整できる。HR2専用テストは引き直しケースを追加して最新10件成功
- LEGENDの初回出現率はユーザー調整により25%から30%へ引き上げ。90K天井・見送り後35%・同ティア1回引き直しは維持
- 開発者ツールの「能力選択を表示」がクリックEventを強制LEGEND引数として渡しており、率に関係なく毎回LEGENDになる不具合を修正。通常のLEVEL UPと同じ抽選へ統一し、率1%・乱数固定でLEGENDなしを確認する専用テストを追加。HR2専用テストは最新11件成功
- ユーザー確定候補を初期値へ反映: 修羅速度1120px/s、修羅得点1.30x、ギャンブラー最高1.05x、LEGEND初回/再出現率35%/35%、変換XP0.70x、Lucky1.00x、完成XP上限800、魂5.20点/XP、収集物2.20x・発光1.80x・高さ1.50x、コイン40%・オーブ20%・複数オーブ5%・高所コイン40%・高所オーブ20%・56pxグリッド、コイン価値1.50x、コンボ3秒・表示84%/22%
- PC/携帯とも取得・Lucky!などの通知が上部能力アイコンと重なっていたため、通知をLEGEND枠を含む全スロットの下（top 218px / 単行222px）へ統一。両表示幅で通知とLEGEND枠が重ならない専用テストを確認。HR2専用テストは最新12件成功
- `?debug=1`の左側ローグライト調整を整理。確定済みの能力・収集物・コンボ値は折りたたみ「確定済みバランス値」へ格納し、通常表示は通知調整を優先。ライバル、デスイーター、GAMBLE、RANDOM GET、Lucky、SOUL LUCKY、LEGEND CALL、復活の8通知を即時表示するボタンと、全通知共通のサイズ・X・Yスライダーを追加。コピー値へも通知値を含めた。全種と位置反映を自動確認し、HR2専用テストは最新13件成功
- 通知のユーザー確定値を初期値へ反映（サイズ1.40x、X=51%、Y=264px）。転生後の「復活！」通知を削除し、通知プレビューも7種へ整理。HR2専用テスト13件・構文/素材検査は最新成功
- メインの3段ジャンプ・滑空をLv3までの段階能力へ拡張。両方ともLv1は既存より弱く、Lv3で既存能力と同等へ到達する。体感差を明確にするため、3段ジャンプは3段目だけを70%→85%→100%、滑空は重力0.65→0.47→0.32倍へ段階強化し、既存の1・2段目操作感を維持。転生はLv1固定。完成ビルドのプリセットも各Lv3の選択回数・終盤XPしきい値（スコア23、その他22）へ更新。HR2専用テスト13件、`npm run verify`、`git diff --check`成功
- 携帯実機確認用に、LAN内限定の一時サーバーをport 4174で起動中。終了時はNodeプロセスPID 18972を停止する。通常の127.0.0.1:4173開発サーバーには変更なし
- 携帯Safariでは全体のタッチ操作無効化が結果画面とDEV TOOLSのスクロールまで止めていたため修正。ゲームオーバー画面は常に上端から開き、ランキング登録時も縦スワイプでSIGN UP・RETRYまで到達可能。携帯のDEV TOOLSは展開後に縦スクロールでき、PCで左側へ出すローグライト調整を同一パネル末尾へ統合した。モバイル用の表示・タッチ許可もHR2専用テスト13件で確認
- 携帯のゲームオーバーは、ランキング登録ありでも1画面へ収まる専用密度へ調整（ロゴ・スコア余白・登録枠・画像ボタンを縮小）。引き直し`⟳`はカード高を増やさず40×24px・高輝度シアンへ拡大し、見落としと押しづらさを改善。新記録時の登録枠を表示した状態で1画面内に収まること、引き直しサイズをHR2専用テスト13件で確認
- 上記の圧縮を新記録時だけへ限定。登録欄のない通常ゲームオーバーは従来のロゴ・余白・ボタンサイズを維持し、寂しく見えないようにした。HR2専用テスト13件、`npm run verify`、`git diff --check`成功
- LEVEL UPの引き直し記号を、`C`に見えやすい`⟳`から矢印の向きが明確な`↻`へ変更。40×24pxの高輝度シアン表示は維持し、カードの高さを変えずに判別性を上げた。HR2専用テスト13件、`npm run verify`、`git diff --check`成功

### HELL RUNNER (`games/temple-run-clone.html`)

- 距離に応じた6背景、画像の足場、ステージ別OGG BGMと3000mクロスフェードを実装済み
- PC・スマホとも390:844の縦長比率と3倍の描画密度へ統一済み
- カメラは公開版を含め常時10%引き
- HUDは10000以上を `10K`、100000を `100K` のように短縮表示
- 30000mから緩やかに加速し、100000mで最高速度の1.4倍に到達
- 20000mで「デスイーター降臨」。死神が左上へ追従し、コイン取得範囲が広がる
- 旧オーラ・色エフェクト・十倍界王拳・修羅モードは廃止済み。復活させない
- `?debug=1` で距離ジャンプ、録画、スクリーンショット、BGM・ジャンプ・コイン・ライバル音の個別調整と設定値コピー、落下時に安全な足場へ即時復帰する `NO FALL` が使用可能。PCではゲーム外の右余白へ表示し、`−` / `+` で縮小可能
- タイトル画面右端に見える細線は、内容が縦に収まらない時のスクロールバー。動作上は問題なし
- 大小ジャンプの試遊版を実装。短押しは小ジャンプ、長押しは従来相当の大ジャンプ。2段目・3段目にも適用し、長押しから滑空へ自然に接続
- `?debug=1` の `JUMP HEIGHT` で `RELEASE CUT` を0.45〜0.90まで実機調整可能。滞空時間・水平距離・高さ・モードを実測表示し、設定値をコピー可能
- `JUMP_RELEASE_CUT = 0.50` に確定。PC・携帯の両方で実機試遊し、小ジャンプと大ジャンプのバランスが良いことを確認済み
- 最速タップを取りこぼさない毎フレーム状態判定を採用。携帯は最初のpointerIdだけを操作元にし、2本目の指を離しても大ジャンプ・滑空を解除しない
- 一時停止機能を実装。HUDのⅡ、既存START/HOME素材を流用した停止メニュー、3秒カウント再開、全サウンドON/OFF、自動停止に対応
- 停止中と再開カウント中は距離・物理・粒子・コウモリ・演出時間を完全静止し、空中停止時も大小ジャンプの軌道を維持する
- HUDのⅡ文字を正式な透過PNG `images/pause-button.png` へ置換。PC・携帯の実機確認で `PAUSE_BUTTON_SIZE = 96` に確定。デバッグモードでは28〜96pxの調整・直接入力・値コピーが可能
- PCタイトル画面へ3行の操作案内を表示。Space短押し＝小ジャンプ、長押し＝大ジャンプ、落下中長押し＝滑空、クリック併用可、P/Escape＝一時停止を明記。遊び方にも追記し、携帯ではPC向け案内を非表示
- ポータルのHELL RUNNERパッチノートへv0.4を追加し、NEWSとゲーム内タイトルのバージョン表記もv0.4へ更新
- 一時停止メニューへプレイヤー用のBGM・効果音音量ゲージを追加。実機調整済みの音量を100%基準とし、2系統を別々に端末保存。サウンドON/OFFは設定値を変えない一括消音として維持
- 音量設定が未保存の端末で `null` が0扱いになっていた初回無音を修正。未設定時はPC・携帯ともBGM・効果音100%、明示的に保存した0%は維持する
- 背景スナップショットテストでは新設のポーズボタンを隠し、背景素材の変更だけを比較するよう分離。背景基準画像そのものは更新していない
- PC・携帯の最終試遊に使用したLAN一時サーバー（port 8001）は作業終了時に停止済み

### 討伐2048 (`games/boss-battle-demo.html`)

- チャレンジランキングの得点を「最大タイル」から「ゲーム終了時の盤面全タイル合計」へ変更済み
- プレイ中の記録も現在の盤面合計を表示し、詰んだ時点で自己ベスト判定・ランキング送信を行う
- 新しい自己ベスト保存キーは `bossDemoChallengeBest_v2`。旧方式の記録とは分離
- 既存Supabaseテーブル `boss_battle_scores` は継続使用。旧2048点の投稿は残るが、新方式の得点が上回る想定
- 全タイルへ一律の移動方向オフセットをかける旧演出を廃止し、各タイルの実際の移動元セルから移動先まで滑らせる方式へ変更。静止タイルと新規タイルは動かず、合体時は遠い側が滑ってポップする
- 戦闘画面左上（HP帯の下）へ青い魔法陣の停止ボタンを追加。一時停止中は盤面入力・演出・攻撃タイマー・BGM・効果音を停止し、PLAY／RETRY／HOMEを選択可能
- 一時停止メニューへBGM・効果音の個別音量ゲージを追加。従来の音量を100%基準として端末保存し、未設定時は両方100%で開始
- `?debug=1`へSIZE TUNERを追加。停止ボタンはサイズ/X/Y、勇者は共通サイズ/X/Y、モンスターはミミック・ドラゴン・魔王ごとのサイズ/X/Yをスライダーまたは直接数値入力で調整し、JSONをコピー可能。全3ボスも保存進捗を変更せず一時解放する
- 実機調整値を本番へ反映。停止ボタン97px・(0px,86px)、勇者117px・(20.1%,90.7%)、ミミック155px・(73.4%,33.4%)、ドラゴン155px・(68.2%,34%)、魔王180px・(77.9%,34%)
- ダメージ数字、攻撃画像、被弾・回復・撃破パーティクル、背景光を各ボスの現在位置とサイズへ追従。チャレンジスコアはHELL RUNNERと同じ10K／10.5K／1M形式で短縮表示

### HEXAMINE（`games/hexamine.html`）

六角形の盤面から原子の配置を論理だけで復元するパズル。3本目の完成作。
**あてずっぽうは要らない。** 生成器は「推測なしで解ける」ことを検査してから盤面を出す。

- どの画面からでも`← GAME BASE`でポータルへ戻れる。タイトル・選択中は左上、プレイ中はMENU/HINTと重ならない左下へ自動配置
- **ソルバーが正本**。`<script type="javascript/worker" id="solverSource">` に1つだけ置き、
  Worker はこのテキストから Blob で作る。テストも同じ場所から取り出して動かす
  （写しを持つと必ず食い違うため）
- **チュートリアル6面**。規則を1つずつ解禁する。教えていない記法は前の面に出さない
  （`notation` / `omitBlue` / `silentRatio` で制御）。盤面は固定の種で、説明とずれない
- **手順つきの誘導**。ソルバーが辿る手順をそのまま台本にし、根拠→理由→操作の順に示して
  押すマスを光らせる。台本は毎回いまの盤面から作り直すので、違う順に開けても破綻しない
- **間違えたマスは開かない**。本家と同じで、ミスだけ増えて盤面は進まない。
  黙って開くと「いつの間にかミスしていた」になり、最後の1手だと結果画面へ直行してしまう
- **長押しの誤爆対策**。押している間に輪郭を1周描いて進み具合を見せ、
  中途半端な長さ（LONG_MS の55%以上・未満）で離した場合はどちらとも取らない
- **∞モードとデイリーは不定形の盤面**。種から輪郭を作る。穴は「6方向すべて埋まったマス」
  からしか抜かないので分断されない。デイリーは種だけで決まるため全員が同じ形
- **デイリーランキング**（Supabase `hexamine_scores`）。順位はミスの少ない順、
  同じならタイムの短い順。送れるのは1日1回だけ
- 一発勝負だが、抜けて入り直せる。**経過時間とミスは引き継ぐ**ので、
  やり直しても有利にならない。誤操作で1日を失わせないため
- 日付は**日本時間で固定**。端末のローカル時刻だと時差のある人だけ別の盤面になり、
  「全員が同じ盤面」が崩れる。GitHub Actions（UTC）と揃える意味もある
- 送信に失敗しても `submitted` を立てず、フォームも残す。通信の失敗で記録を捨てないため
- テスト `npm run test:hex`（108件）。solver.test.mjs が `games/hexamine.html` から
  ソルバーを取り出して検証する
- 携帯の外側ヒントは、数字そのものではなく盤外側だけの透明な判定帯を押せる。タップ後は範囲を固定表示し、同じヒントをもう一度押すと閉じる。数字を狙い損ねてマスを誤タップする事故と、離した直後に範囲が消えて読めない問題を防ぐ。`npm run test:hex-ui`（1件）で判定帯の端でもマスを変えないことと固定表示を確認
- デイリーチャレンジのHINTは3回まで。HUDの`HINT（残り 3）`で残数を示し、実際に確定手を示せた時だけ消費する。残数・ミス・経過時間は同じ日なら入り直しても引き継ぎ、0回でボタンを無効化する
- Discordのランキング通知は共通の`tools/ranking-to-discord.mjs`へ登録済み。1時間ごとに当日の1位更新だけを通知し、最初の実行では既存記録を基準値として保存する。HELL RUNNER 2（β）も同じ仕組みで総合1位更新を通知する

### 制作中止・削除したもの（2026-08-26）

**ユーザー判断で2本の試作を打ち切り、ファイルごと削除した。** どちらもポータルには
一度も載せておらず、専用素材も無いため、削除による影響は無い。

| 消したもの | 内容 | 理由 |
|---|---|---|
| `games/rhythm-game-test.html` | RHYTHM LAB（複合型音楽ゲームの全モード比較試作） | 現在の開発環境・プレイ環境では満足のいくものが作れる見込みが立たないため |
| `tetris.html` | テトリス（土台のみ・未リンク） | これ以上進展させないため |

`tests/rhythm-game.spec.js` と `npm run test:rhythm` も削除し、`npm run verify` の対象からも
両方外した。過去の作業履歴（2026-08-21のRHYTHM LAB試作など）はそのまま残してある。
中身が必要になった時は git 履歴から取り出せる。

### 本日の最強決定戦（実装中）

近未来の路地裏を舞台にした、当日最速の反応速度を競う1ボタン対戦ゲーム。カービィの「刹那の見切り」の待機と一撃の気持ちよさを参考にするが、キャラクター・世界観・演出は完全オリジナルで制作する。

- プレイヤーは5人から1人を選ぶ。キャラクター性能は同一にして反応速度の公平性を守り、立ち絵・ドット絵・モーション・3種のボイスで愛着を作る
- 5人の初期案: レイヴン（ZERO BLADE）、ミカ（PINK RIOT）、ブリック（IRON FIST）、ノイズ（BEAT BREAKER）、キリ（GHOST SIGNAL）
- 各キャラに、選択時／敗北時／勝利画面の決め台詞を1つずつ用意する。選択画面はリアル寄りの縦長立ち絵を背景、同キャラの2Dドット絵を前景に置いた5枚の横並びカードにする
- 当日ランキング1位がNPC基準（初期案280ms）より速ければ、その記録のプレイヤー名・選択キャラ・反応時間を対戦相手として出す。空または基準以下なら通常NPCと対戦する
- 当日1位との対戦前は、黄黒テープ風の`CAUTION! CURRENT CHAMPION`で通常開始を遮り、今日の王者戦であることを明示する。日付が変われば新しいランキングで始める
- 監視ドローンが決闘を開始する合図役。待機中は赤いスキャン光、合図時は青白いライトと`ENGAGE!`、連動して街の看板・水たまりの反射が一瞬発光する。視覚と単発SEを同時に出し、端末差でも遊びやすくする
- ドット絵の最低限の演技は、待機、警戒、決着の踏み込み、被弾、勝利ポーズ、敗北ポーズ。勝敗時は立ち絵の表情も一瞬差し替える
- フライングは`FEINT!`としてそのラウンドを落とす。異常に小さい反応時間（初期案90ms未満）はランキング登録しないなど、不正・計測外の記録で王者が固定されることを防ぐ
- ボイスはユーザーが用意する。立ち絵とドット絵はCodexが制作する。選択カード用の立ち絵5枚を`images/todays-champion/portraits/`へ保存し、待機／警戒／勝利のドット絵企画原画を`images/todays-champion/source/pixel-character-poses.png`へ保存済み。`games/todays-champion-motion-preview.html`で、5人の選択と待機→警戒→勝利の自動切替を確認できる。本編、個別スプライト分割、決着／被弾は未着手
- 携帯実機確認用に、同一Wi-Fi限定の一時サーバーをport 4174で起動中。`http://192.168.0.8:4174/games/todays-champion-motion-preview.html`で確認できる。終了時は起動したNodeプロセスだけを停止する
- ポータル用ラベル絵は`images/portal/cart-label-todays-champion.webp`（640px正方形、71KB）を作成済み。レイヴンとミカの対峙、中央の監視ドローンを上寄りの安全地帯に置き、携帯機画面で下側が切れても主役が残る構図にした。`index.html`へ`本日の最強決定戦`のカセットをCOMING SOON手前に登録し、警告灯イエロー`#ffd34e`をカセット・本体内部・PLAYの共通色にした。PLAY先は`games/todays-champion.html`の開発中ページ
- 開発中ページの公開素材をWebPへ圧縮。立ち絵5枚とドット絵は合計14.1MB→1.8MB（87%減）で、原本PNGは`images/todays-champion/source/`にだけ残し.gitignore対象とする。再生成は`npm run champion:images`
- ドット絵の企画シートは各行・各キャラの余白が等しくないため、Canvasでの均等分割を廃止。ポーズごとの実際の上下端とキャラごとの横端を指定し、警戒中に次の勝利ポーズの一部が足元へ混ざる表示崩れを修正。390px携帯スクリーンショットで確認済み
- `games/todays-champion.html`はNPC練習決闘まで実装済み。選択後はカードを隠して、雨の近未来路地（`images/todays-champion/stage-bg.webp`）で2人のドット絵だけが向かい合う。START→ランダム待機→`ENGAGE!`へのステージタップで反応時間と勝敗を決め、合図前は`FEINT!`でNPC勝ちにする。勝者は素早く中央へ踏み込み勝利ポーズ、敗者は倒れ込む決着モーションを表示する。企画シートに焼き込まれた夜紺背景はCanvas上で透過処理するため、路地背景と二重にならない。日替わり王者・ランキング・ボイスは次工程
- 対戦背景の原画は`images/todays-champion/source/stage-bg.png`（gitignore対象）へ、公開用WebPは`images/todays-champion/stage-bg.webp`へ保存。`npm run champion:images`で立ち絵・ドット絵と同時に再生成する
- 試合中の等身は立ち絵と分離したちびドット絵へ変更。`source/chibi-duel-poses.png`は5列×6行（待機A／待機B／構え／走り／攻撃／結果）の透過原画、公開用は`chibi-duel-poses.webp`。選択中だけ待機A/Bを小さく往復し、開始後は構え→勝者の走り込みと衝突フラッシュ→敗者の倒れ込みを見せる。決着は`source/chibi-result-poses-v2.png`（上段=勝利、下段=敗北）／公開用`chibi-result-poses-v2.webp`へ切替え、既存5人の髪色・服・武器を一致させた
- 選択カードは立ち絵の前に同キャラのちびドット絵を重ねる。試合開始後は監視ドローンを表示し、待機中は赤いスキャン光、`ENGAGE!`では青白い発光と街路を照らすフラッシュへ切替える。ドローン自体はCSSで描いているため、表示密度を増やさず携帯でも読める
- 日替わり王者の読み込み・対戦導線・記録フォームを実装。`todays_champion_scores`から日本時間当日の最速（280ms未満）を読み、名前を敵表示、キャラIDを敵ドット絵へ反映する。王者戦は`CAUTION! CURRENT CHAMPION`を経由し、正常勝利かつ90ms以上の記録だけを名前・キャラID・速度とともに登録できる。**公開前に`supabase-todays-champion.sql`をSupabase SQL Editorで一度実行する必要がある**。実行前は通常NPCへ安全にフォールバックする
- 王者戦のCAUTIONは`hidden`属性を表示CSSが上書きして閉じず、対戦画面へ進んでいてもテープだけ残る不具合を修正。`caution[hidden]{display:none}`を明示し、テープの表示・非表示と通常NPC戦での非表示をPlaywrightで確認済み
- 対戦画面のちびキャラを一段引き（通常255px、決着245px）にし、背景の路地を広く見せる。勝者の踏み込みと敗者の倒れ込みも半歩分へ抑え、390px携帯画面で敗者を含む2人が決着後まで画面内に残ることを確認済み
- 対戦専用のWeb Audio効果音を追加。START後の赤スキャン、`ENGAGE!`、一撃、勝利、敗北を短い電子音で合成する。外部音源を増やさず、STARTタップでAudioContextを解禁してSafariの自動再生制限を回避する
- 選択カードのちび絵が名前・肩書きへ重なり、対戦中の切り抜きは足元を失っていた問題を修正。カード内のちび絵は名前領域より上へ縮小配置し、対戦用は切り抜き余白を8pxへ広げて足まで描画する。390px携帯スクリーンショットと`npm run verify`で確認済み
- 敗北専用シートには分離した靴・エフェクトなどの不要断片が混ざるため、敗者には専用シートを使わず全身が保証される通常ちび絵の構えポーズ＋倒れ込みCSSを使用する。勝者だけが勝利専用ポーズへ切替わる。390px携帯の決着スクリーンショットで両キャラが全身のまま画面内に残ることを確認済み
- 決闘ルールを再整理。二人は交差点の左右端で向かい、赤信号の待機から青緑のGOで中央へ踏み込んで一撃を交わす。決着後は勝者・敗者を左右へ戻して全身を残し、勝敗が読める構図にする
- 監視ドローン演出は廃止。雨の近未来交差点へ描き替えた`source/intersection-bg.png`を公開用`intersection-bg.webp`へ圧縮し、背景の横信号へHTMLの赤／青緑点灯を重ねる。`WAIT FOR GREEN`→`GREEN — GO!`で入力タイミングを視覚的に明示する
- 足元や別ポーズが混ざる`chibi-duel-poses.webp`は対戦描画から外した。5列×3行が揃った`pixel-character-poses.webp`のみにし、原画に焼き込まれた濃紺背景は`champion:images`で透明化して、決闘背景の上に四角を残さない
- 390px幅の決闘待機・勝利決着をスクリーンショット確認。二人の開始位置を広げ、信号・両キャラ・背景を同時に見せられることを確認。`npm.cmd run champion:images`、`npm.cmd run verify`成功
- キャラ素材は従来の共通シートから全面作り直し中。新規の正本は`source/sprites/<character>/idle.png`／`dash.png`／`attack.png`／`win.png`／`lose.png`の**1状態1ファイル**とし、Canvasでの切り出しを廃止する。レイヴンの5枚は透明PNG・全身収録を確認済み。残り4人を同形式で制作後、このパスを直接読む実装へ置換する
- 待機ループは不要。全キャラを試合進行に対応する5枚で統一する: (1)踏み込む前の戦闘構え、(2)武器を出しながら前へ走るダッシュ、(3)すれ違い後の攻撃を振り切った決め、(4)勝利、(5)敗北。キャラの横移動はCSSで行い、見せ場だけをこの5枚へ切り替える。構えは直立ではなく、前後へ開いた足・低い重心・攻撃へ移れる武器位置を必須にする
- レイヴンの`idle-sheet-5frames.png`／`guard-sheet-5frames.png`は採用せず、構図と加工方法の試作原画として`source/sprites/raven/`へ保管。ゲームには未接続
- 5人の構え→ダッシュ→振り切りの個別設計: レイヴン=刀へ手を置く抜刀／居合い、ミカ=ローラースケートで低く構える／横滑り飛び蹴り、ブリック=義手の拳を前に出す／肩からのストレート、ノイズ=ヘッドホンの音を溜めるDJ構え／低い回転からの音圧掌打、キリ=クリスタルナイフを逆手に隠す／滑り込む刺突。この個性を(1)構え、(2)ダッシュ、(3)振り切りの3枚へ明確に反映し、(4)勝利・(5)敗北は各攻撃の余韻につなげる
- ユーザーの切り抜き加工用に、5人ぶんの横シートを`source/sheets/`へ保存。`raven-action-sheet.png`、`mika-action-sheet.png`、`brick-action-sheet.png`、`noise-action-sheet.png`、`kiri-action-sheet.png`は、左から構え／ダッシュ／振り切り／勝利／敗北の順。各ポーズ間に大きな透明余白を取り、全員がRGBAであることを確認。ノイズは初版の単純な等身・絵柄を不採用にし、他4人の高密度ちびドット絵へ寄せた第2版を本体へ採用。市松付きの元出力は`noise-action-sheet-raw.png`、初版は`noise-action-sheet-v1.png`として残す。ゲームには未接続
- 攻撃エフェクトはキャラシートへ焼き込まない。切り抜き後のキャラと別レイヤーにし、振り切りの瞬間だけ重ねる。これによりキャラ本体を隠さず、サイズ・位置・色をゲーム上で調整できる。初期案はレイヴン=青い居合い弧、ミカ=ピンクのキック軌跡、ブリック=橙の拳圧、ノイズ=紫の音波、キリ=紫の結晶閃光
- ユーザーが切り抜く**編集原本**は、全ポーズ共通の640×640px・RGBA PNGにする。自動トリミングはせず、靴底／接地点をY=560pxへ固定し、上下左右には余白を残す。攻撃の武器先端もキャンバス内へ収め、エフェクトは含めない。編集確定後は公開用に透過WebP（劣化が見えない設定、必要ならlossless）へ変換し、ゲーム側は軽いWebPだけを読む
- ユーザーが5人×5ポーズを個別PNGへ切り抜き済み。`source/sheets/raven1〜5.png`、`mika1〜5.png`、`brick1〜5.png`、`noise1〜5.png`、`kiri1〜5.png`が、すべて640×640・RGBA。番号は構え／ダッシュ／振り切り／勝利／敗北の順。25枚を透明背景上で一覧確認し、武器先端・足元の欠け、別キャラの混入は見当たらない。次工程は公開用WebP化と、この個別素材を読む対戦画面への置換
- 次の素材工程は攻撃エフェクト5枚。各キャラの3番（振り切り）にだけ別レイヤーで重ねる。キャラ本体へ焼き込まないため、位置・大きさ・点滅をゲーム側で調整できる
- 攻撃エフェクト5枚を作成済み。`source/effects/raven-attack.png`（青の居合い弧）、`mika-attack.png`（ピンクのキック軌跡）、`brick-attack.png`（橙の拳圧）、`noise-attack.png`（紫の音波）、`kiri-attack.png`（結晶閃光）はすべて640×640・RGBA PNG。生成元は`source/effects/raw/`に保存。ゲームには未接続
- 個別素材を公開用WebPへ変換済み。`images/todays-champion/sprites/<character>-{guard,dash,attack,win,lose}.webp`の25枚と、`images/todays-champion/effects/<character>-attack.webp`の5枚を`npm run champion:images`で再生成できる。旧Canvasシートを対戦描画から外し、個別WebPを直接読む方式へ置換した
- 選択画面はゲームへ入った直後に画面全体を5枚の立ち絵で使う構成へ変更。START後は交差点ステージを全面表示し、赤信号待機→緑GO→双方ダッシュ→振り切り＋両者の攻撃エフェクト→勝者4番／敗者5番の順に演出する。決着時は左右のファイターを相手側へ通り越させ、開始位置と逆の側で止める
- 背景信号に重ねる要素は枠なしの赤・黄・緑の丸3灯だけへ簡略化。`?debug=1`限定の`DUEL TUNER`で、5キャラ×構え／ダッシュ／攻撃／勝利／敗北／エフェクトのX・Y・SIZEをスライダーと数値入力で個別調整できる。信号3灯もX・Y・SIZEを同様に調整可能。パネルは`−`で畳め、`COPY VALUES`で値を取得できる
- DUEL TUNERへ`MOTION SPEED`（0.10〜1.00倍、スライダー＋数値入力）を追加。ダッシュ・攻撃・逆側への着地を同じ倍率でスロー化するため、動きの途中にある素材・エフェクトを調整できる。合図は緑→黄→赤へ変更し、緑→黄は650ms固定、**黄→赤のみ700〜3000msのランダム**。赤が入力タイミングで、緑・黄でのタップはフライングになる。`npm.cmd run verify`成功、Playwrightでデバッグ数値入力7件と黄フェーズ遷移を確認
- 通常のキャラ選択画面下部に`OPEN DEV TUNER`を追加。押すと`todays-champion.html?debug=1`へ遷移し、調整パネルを開ける。Playwrightでリンク遷移とパネル表示を確認、`npm.cmd run verify`成功
- 実機調整値を5人の全ポーズ・攻撃エフェクトと信号3灯へ反映。通常プレイの動作速度は維持し、`MOTION SPEED=0.10x`はデバッグ時だけの初期値にした。決闘中に`← FIGHTER SELECT`でタイマーを止めて選択画面へ戻れるようにした。各素材を`FLIP IMAGE`で個別に左右反転でき、NPC側にはさらに逆向きに反映される。信号は外枠なし、赤・黄・緑を個別X/Y、共通SIZE（最大5.00）で調整・コピーできる。位置確認用の全幅基準線をデバッグ時だけ追加し、結果表示の上から高さを調整できる。`npm.cmd run verify`、`git diff --check`成功
- キャラ／エフェクトはX位置・画像反転とも敵側で反対向きになるよう修正。キャラの手前へ出すミカの蹴り軌跡だけを例外とし、レイヴン・ブリック・ノイズ・キリの攻撃エフェクトは武器や腕を隠さない背面レイヤーへ移した。ユーザー実機の再調整値（信号SIZE 3.23xを含む）を反映。`npm.cmd run verify`、`git diff --check`成功
- ユーザー提供の`信号１・２.mp3`をGREEN開始時とYELLOW移行時の2回、`信号３.mp3`をRED合図時に再生するよう組み込み。START操作中に無音で各音源を解禁してから待機中に鳴らすため、Safariの自動再生制限へ対応する。`npm.cmd run verify`、`git diff --check`成功
- 新記録時のランキング登録フォームを画面中央の専用レイアウトへ変更。RETRYを隠し、結果文・入力欄・REGISTERが重ならないようにした。`supabase-todays-champion.sql`はテーブル、RLS、公開SELECT/INSERTポリシー、anon/authenticatedへのテーブル／ID採番権限まで定義済み。`npm.cmd run verify`、`git diff --check`成功
- Supabaseの`todays_champion_scores`を実際に読み出し、HTTP 200／現時点は記録0件を確認。`tests/todays-champion.spec.js`を追加し、当日最速（150ms・キリ・TEST CHAMPION）の応答で敵名・敵スプライト・`CAUTION!`・王者戦導線が反映されることを自動確認した。`npx.cmd playwright test tests/todays-champion.spec.js`（1件）、`npm.cmd run verify`、`git diff --check`成功
- ユーザー提供の`プレイヤー同士が重なる瞬間.mp3`を、両者が中央で重なるアタック切替の瞬間へ組み込んだ。`?debug=1`の`AUDIO MIX`で信号1/2・信号3・衝突・WIN・LOSEを0〜1.50で個別調整し、`COPY VALUES`へも含める。王者戦と音量調整のPlaywrightは2件成功、`npm.cmd run verify`、`git diff --check`成功
- 王者戦の敵は名前・キャラだけでなく、当日最速記録の`ms`を反応時間へ使用するよう修正。実データではミカ184msが当日最速で、後続の190ms／269msは遅いため王者が変わらないことを確認。Playwrightで150ms王者が`NPC 150ms`で決着することを確認した。`npx.cmd playwright test tests/todays-champion.spec.js`（2件）、`npm.cmd run verify`、`git diff --check`成功
- ユーザー提供の`ランキング1位いるときの音.mp3`をCAUTION画面専用BGMとして追加。画面開始で再生しGREEN開始時・選択画面帰還時に停止する。CAUTIONには王者名／記録と`BREAK THE RECORD. TAKE THE CROWN.`を表示し、挑戦の意味を明確化。`AUDIO MIX`へCHAMPION音量を追加。`npx.cmd playwright test tests/todays-champion.spec.js`（2件）、`npm.cmd run verify`、`git diff --check`成功
- CAUTIONが0.95秒で切り替わり文字とBGMを認識できなかったため、3秒へ延長。BGMをほかの音源の事前解禁処理から分離し、CAUTION開始時の再生を直後に止めないよう修正した。`npx.cmd playwright test tests/todays-champion.spec.js`（2件）、`npm.cmd run verify`、`git diff --check`成功
- CAUTIONの見出しを大きくし、`← GAME BASE`を決闘中に常設。音源は`audio/todays-champion/`へ英語名で整理し、初期音量を信号1/2=1・信号3=1・衝突=0.5・王者BGM=0.5・WIN/LOSE=1へ確定した。未使用の旧ちび対戦シートを公開素材から外して約1.7MB削減し、変換スクリプトも再生成対象から除外した
- Discord通知は、すでにランキング専用Webhookを設定済みのGitHub Actionsへ一本化。`todays_champion_scores`を5分ごとに当日最速と照合し、同日中に短い記録へ更新された時と日付が変わった最初の王者をDiscordへ投稿する。初回だけは現在の王者として通知し、通知先で機能追加を確認できる。通知ロジック自体を更新したpushでも即時実行される。Webhookをブラウザやリポジトリへ置かないため、追加のSupabase設定は不要
- モーション速度は携帯・PCとも初期値を`0.20x`へ変更。立ち絵ではなく、決闘中に作り込んだダッシュ／攻撃／勝敗ポーズを読めるテンポを優先する
- 携帯の`DUEL TUNER`は上下半画面の`TOP VIEW`／`BOTTOM VIEW`へ切替可能にし、調整パネル内のスクロールを維持したまま反対側のゲーム画面を常に見られるようにした。ヘッダーもスクロール中に固定し、表示位置の切替と折り畳み操作をすぐ使える
- 実機で再調整したレイヴン／ミカの構え／ダッシュ／攻撃／勝利／敗北／エフェクトと、信号3灯（共通SIZE 2.84x・各X=3/Y=7）を初期値へ反映。コピー値に含まれる未調整のほか3人・音量・速度は変更していない
- `DUEL TUNER`へ`FORCE RESULT PREVIEW`を追加。`PLAYER WIN`／`NPC WIN`で選択中キャラをそれぞれ左右の勝者として静止表示し、ランキング王者の反応速度・待機を経由せずWIN／LOSEポーズを調整できる。`SELECT SCREEN`で通常状態へ戻る
- `POSE LOOP`は選択中キャラの構え→ダッシュ→攻撃→WIN→LOSEを左右同時に自動再生し、ポーズ切替時のサイズ差・足元のずれを確認できる。`MIRROR CHECK`は選択中のASSETを同じキャラのプレイヤー側／NPC側へ静止表示し、敵側へ反転した時のX位置・画像向きを確認できる
- 背景画像を`cover`で表示する際の実拡大率・中央トリミングを算出し、信号3灯の親位置と倍率へ反映するよう変更。390×844で決めた現在の信号調整を保ったまま、携帯の縦横比やPCモニター比率が変わっても背景内の信号機へ追従する
- **次回の残作業:** ゲーム本体の実機調整は`OPEN DEV TUNER`と`COPY VALUES`で継続できる
- 検証: `npm.cmd run champion:images`成功、`npm.cmd run verify`成功。390px幅の待機、攻撃エフェクト、勝利決着をスクリーンショットで確認。開始時の離れた対峙、攻撃時の青居合い／紫音波、決着後に逆側へ残る2人を確認済み
- ユーザーの実機調整値を初期値へ反映: BPM 115、AUDIO OFFSET +0.14秒、BEAT START 2.40秒
- 開始時は音声の`play()`成功後にだけノーツを進める。Safariがユーザー操作後の待機を自動再生として止めないよう、押下と同じ同期区間で再生を要求する。失敗時は`AUDIO ERROR`として画面に理由を出し、黙って無音のまま始めない


### ポータル (`index.html`)

**2026-08-21に全面刷新。以前の「アーケード筐体を並べる」方式は廃止した。**
筐体は3D・陰影・パース・金属質感を持つ絵で、生成AIに同じ型を再現させられず、
新作のたびにデザインが揃わなかった。旧筐体用のPORTAL TUNER、筐体・タイトル・PLAYの画像、
位置の確定値表は削除済み。現行の携帯機レイアウト用に、2026-08-21から新しいPORTAL TUNERを再導入した。

現在の形:

- 中央に**スケルトンの横長携帯機1台**。カセットを選ぶと本体へ吸い込まれ、
  差込口が光り、内部がそのゲームの色に灯って画面へ絵が出る
- **カセットはCSSで描く**（画像ではない）。形も色も光り方もコードから変えられる
- ロゴは**CSSの文字**。画像ロゴは、それを載せる暗い板ごと背景を覆っていたため廃止
- 世界観は地下の秘密基地 `VIVI GAME BASE`（旧 VIVI GAME ARCADE）。
  `INSERT COIN` `GAME FLOOR` は筐体前提の言葉なので
  `SELECT YOUR GAME` `CARTRIDGES` へ変更済み
- カセット置き場は**折り返さず横スクロール**。PCはドラッグでも動かせる。
  10本並べても1段のままで、ページは横に伸びない（実測確認済み）
- `?portalDebug=1`ではPORTAL TUNERを表示。ロゴ、ステータス、カセット見出し／列／名前、ゲーム機、情報カウンターのX/Y/倍率を個別スライダーで調整できる。値はその端末の開発表示だけに保存され、コピーした値を本番CSSへ反映する
- 倍率の表示単位`x`をCSSの`scale()`へ渡して変形全体が無効になる不具合を修正。位置・倍率スライダーの値がCSS変数へ即時反映されることを実機ブラウザで確認済み
- 携帯の初期レイアウトは実機調整値を採用（見出しY -78px／1.09x、カセット列Y -82px、名前X -15px、本体Y -127px、情報Y +37px）。携帯の挿入カセットは本物と近い25%幅へ拡大。タグのX/Y/倍率も調整対象へ追加し、コピーが使えないHTTP携帯環境では`値をコピー`でツール内に確定文字列を表示して長押し／スクショ共有できるようにした
- `?portalDebug=1`のPORTAL TUNERへ、携帯機へ挿入済みのカセット専用のX/Y/倍率を追加。挿入アニメーション中から着地後まで同じ調整値を使うため、サイズだけを変えても差込口からずれない
- 背景はカセット名の下端まで延長し、その直後から本体の黒い展示領域へ切り替える。カセットと背景の切れ目が重ならないよう、PC/携帯ともヒーロー高さとセクションの重なり量を対で調整済み
- CSSロゴは一部の文字だけが壊れた蛍光灯のように不規則に明滅。`SELECT YOUR GAME`はゆっくり点滅する。`INFORMATION COUNTER`は外側の大きな装飾枠を廃止し、各情報ボタンだけを残す
- 蛍光灯演出は`VIVI GAME`内のIとEだけに限定し、`BASE`は常時表示。携帯ではカセット列をさらに35px上げ、ゲーム名を幅100%の中央揃え・折返し禁止へ統一して`HELL RUNNER 2`も1行で表示する

**新作を1本足すときは、絵1枚とコード数行だけ。**
手順は `docs/ポータル実機デザインの仕様.md` の「新作を1本足すときの手順」が正本。

**ゲームの絵には安全地帯がある。** 1枚を「カセット（正方形のまま全体）」と
「本体の画面（横長に切り取り）」の2か所で使うため、画面では上から10〜69%しか映らず、
その下半分にはタイトルの帯が重なる。**主役は上から10〜45%へ置くこと。**
数字と図は同仕様書に記載。

素材（すべて `images/portal/`、原本は `source/` で .gitignore 対象）:

| ファイル | 用途 | 何回作るか |
|---|---|---|
| `console-body.webp` | 携帯機本体 | 一度だけ |
| `arcade-bg-desktop/mobile.webp` | 秘密基地の背景（PC/携帯） | 一度だけ |
| `cart-label-*.webp` | ゲームの絵 | ゲームごと1枚 |

- `vivi-game-base-logo.webp` は残してあるが**未使用**。CSSロゴを採用したため
- 素材の縮小とwebp変換は `npm run portal:images`（`tools/portal-images.mjs`）
- 確認用サーバー`tests/lan-static-server.cjs`は既定でLAN公開（4174）。`VIVI_PREVIEW_HOST=127.0.0.1`と`VIVI_PREVIEW_PORT`を指定すると、そのPCだけで見られるローカル確認へ切り替えられる


### 素材と道具（2026-08-21時点）

| 道具 | 用途 |
|---|---|
| `npm run portal:images` | ポータル素材の縮小＋webp変換（`tools/portal-images.mjs`） |
| `node tools/game-images.mjs <html>` | ゲームHTMLの埋め込みPNGをWebPへ。大きい絵は非可逆、ドット絵は可逆 |
| `node tools/game-audio.mjs <html>` | 埋め込みBGMのビットレートを下げて入れ直す |
| `tools/hooks/pre-commit` | コミット前に `verify` / `test:unit` / `test:hex` を走らせる（下記の有効化が必要） |

- 依存に `sharp`（画像）と `ffmpeg-static`（音声）を追加済み。どちらも devDependency
- **PowerShellでは `npm` が実行ポリシーで弾かれる。`npm.cmd` を使う**（`git` はそのままで通る）
- `docs/ダークネオンアーケード仕様書.md` は筐体方式の記録。現行仕様ではない

## テスト環境

**巨大HTMLを変更したら、最後に必ず `npm run verify` を実行する。**
構文・`<script>`の開閉と`</html>`終端・素材参照の実在を1コマンドで検査する。
以前は `tail -c 40` などを手で叩いていたが省略されやすく、素材の欠落は目視でしか
気づけなかった（実際にポータルで参照切れを4件見落としていた）。手作業の構造確認は不要。

テストは**変更した領域だけ**に絞る。全件は複数領域にまたがる変更と公開前確認だけ。

ブラウザを使わない検査（数秒で終わる）:

| コマンド | 対象 | 件数 |
|---|---|---:|
| `npm run verify` | 構文・構造・素材参照（公開ファイル全部＋HELL RUNNER 2） | 7ファイル |
| `npm run test:unit` | tools/の通知スクリプト、HELL RUNNER 2の地形生成、背景ゾーン | 36 |
| `npm run test:hex` | HEXAMINEのソルバー | 108 |

Playwright（1件あたり十数秒かかる）:

| コマンド | 対象 | 件数 |
|---|---|---:|
| `npm run test:runner` | HELL RUNNER(debug-mode + 背景比較) | 25 |
| `npm run test:hr2` | HELL RUNNER 2 | 14 |
| `npm run test:boss` | 討伐2048 | 10 |
| `npm run test:portal` | ポータル | 10 |
| `npm run test:hex-ui` | HEXAMINEの操作 | 3 |
| `npm run test:champion` | 本日の最強決定戦 | 20 |
| `npm run test:ranking` | ランキング登録ボタンの状態遷移 | 5 |
| `npm test` | 全件（Playwright分） | **88** |

- 背景の見た目を意図的に変えた場合のみ `npm run test:update-backgrounds`。通常は基準画像を更新しない
- **背景の基準画像はOSごとに別ファイル**（`01-hell-win32.png` / `-linux.png`）。
  同じ変更でも実行したOSの分しか更新されないため、別OSで走らせた結果をそのまま信じない
- HELL RUNNER(無印)は凍結済みのため、`test:runner` と背景比較は**回帰確認としてのみ**必要。
  無印を変更していないなら実行しなくてよい
- `test:hex` と `test:unit` はPlaywrightではなくNode単体で動く
- RHYTHM LAB は制作中止。テストも検査対象も外してある（上のRHYTHM LABの項を参照）
- push/PRのたびに `.github/workflows/tests.yml` が `verify` / `test:unit` / `test:hex` を回す。
  **CIのNodeは24に固定してある。** `test:unit` はグロブをNode自身に解釈させており
  （Windowsのnpmではシェルが展開しないため）、これはNode 22以降でしか効かない。下げないこと。

### pre-commitフックの有効化（手元で1回だけ必要）

**2026-08-26にユーザーの環境で有効化済み。** ただしこの設定は各クローンの `.git/config` に
入るため、**別の場所へ取得し直すと消える**（PCを変えた、フォルダを作り直した、など）。

ユーザーはgitの細かい用語には馴染みがないため、必要になったら手順ごと案内すること。
リポジトリのフォルダでPowerShellを開いて、次の1行:

```powershell
git config core.hooksPath tools/hooks
```

確認は `git config core.hooksPath` で `tools/hooks` と出ればよい。

**再設定が必要になったことの見分け方**（ユーザー向けの説明はこれで足りる）:
コミット時に `7 ファイル / インラインスクリプト 20 件: ...OK` の行が出なくなったら、
フックが効いていない。`pre-commit: node が見つからないため検査を飛ばす` と出る場合は
PATHの問題で、フックは黙って素通りするので放置しないこと。

一時worktreeでも追加設定なしで効く（`.git/config` を共有するため）。
  Playwrightを入れていないのは、背景の基準画像がOSごとに別で、CIのLinuxと手元のWindowsで
  必ず食い違い、常に赤いCIになってしまうため

## 未完了・次にやること

### 公開前にやること（2026-08-26に全部完了。着手不要）

HEXAMINEの公開前チェックとして3項目を挙げていたが、**すべて済んでいる。**
「最優先」と書かれたまま残っていたため、次に開いた担当が着手しないよう畳んでおく。

1. ~~`hexamine_scores` の動作確認用データ（名前「接続確認」の2件）を消す~~
   → ユーザーが削除済み
2. ~~HEXAMINEを一通り遊ぶ~~ → 公開済みで、Discordのランキング通知も稼働中
3. ~~GitHubへpush~~ → 済み

### ポータル

- `vivi-game-base-logo.webp` は未使用のまま残してある。CSSロゴで確定したので削除してよい
- HELL RUNNER 2 は β として掲載済み。完成したら説明とタグを見直す
- HELL RUNNER 2 の完成後に `node tools/game-images.mjs games/hell-runner-2.html` を通す。
  無印と同じ **未使用の `UI_B64`（2.5MB）** が残っているので、まずそれを消すと効果が大きい

### HEXAMINE

- `tools/ranking-to-discord.mjs` へ追加済み。初回実行時は記録するだけで通知しない
- 盤面の `0` の直線ヒントが多いと感じたら `silentRatio` などで調整の余地あり

---

- 既存HELL RUNNER 1は凍結。ユーザーから明示的な修正指示がない限り、コード変更もHELL RUNNER 1専用テストも実行しない
- **HELL RUNNER 2も2026-08-26をもって開発終了。** プレイヤーからのバグ・不具合報告がない限り触らない。
  コイン価値1.50xなど「仮固定」と書かれている数値も、そのまま確定値として扱う
- 詳細な優先順・確認項目は `docs/HELL-RUNNER-2引き継ぎ.md` を正本とする
- ゲームオーバー時の能力アイコン・XPバー・コンボ・通知の非表示、通知がXPバーより下に出ることは専用テストで確認済み。今後は実機で配置の細部だけ確認する

- ローグライト能力版は既存HELL RUNNERへ上書きせず、ゲーム部分を流用した独立作`HELL RUNNER 2`として制作する方針に変更。新HTML・保存キー・ランキング・素材フォルダを分け、完成まではポータルへ掲載しない
- HELL RUNNER 2素材工程6の前半を完了。通常・邪悪・幸運コインと通常・高密度XPオーブの透過スプライト列を`images/hell-runner-2/collectibles/`、生成マスターを`source/collectibles/`へ保存。5素材とも四隅アルファ0・0〜255を検査済み。変換器演出とゲーム内置換は未実装
- HELL RUNNER 2素材工程5を完了。メイン3種・補助5種・レジェンド5種の全能力アイコンを`images/hell-runner-2/icons/`、生成マスターを`source/icons/`へ保存。全て1254×1254px、左上アルファ0・0〜255を検査済み。一覧は`docs/mockups/hell-runner-2-ability-icons-contact.png`
- HELL RUNNER 2素材工程6を完了。変換器演出`effect-coin-to-xp-spritesheet.png`を追加。通常コインから粒子を経てXPオーブになる8フレーム、1536×192px、四隅アルファ0・0〜255を検査済み。ゲーム内置換は未実装
- HELL RUNNER 2素材工程7の一部を完了。文字なし透過のLEVEL UP角装飾を`frames/level-up-header-ornament.png`へ追加し、選択画面へ統合。390×844・PCの専用UIテストと構文検査成功
- HELL RUNNER 2は旧版の開始前能力選択入口を非表示化。開始時は必ず2段ジャンプから始め、ラン中のXP・3択でのみ能力を得る仕様へ移行する。XP取得・抽選・実効果は次の実装段階
- HELL RUNNER 2の最小XPループを実装。通常・高密度XPオーブを足場上へ生成・描画・回収し、XPバーを更新する。しきい値到達で走行を停止してLEVEL UPへ入る。選択カードの候補抽選・取得効果・スロット反映は次段階。`window.__hellRunnerDebug.giveXp()`でdebug時のみ高速検証可能。専用UIテストと構文検査成功
- HELL RUNNER 2のメイン3種・補助5種を実候補化。メイン2枠・補助3枠の空きがある未取得能力、または上限未満の取得済み能力だけを3択へ出す。選択時に3段ジャンプ・滑空・転生は既存プレイ処理へ反映。補助の数値効果・通常プレイHUDスロットの動的反映・LEGENDは次段階。専用UIテストと構文検査成功
- HELL RUNNER 2の補助5種を実効果化。コイン倍率、コンボ倍率、コンボ持続（Lvごと+0.15秒）、経験値倍率、XP吸引を実処理へ接続。通常HUDのメイン2枠・補助3枠は現在の取得ビルドとLvを動的に表示する
- HELL RUNNER 2のLEGEND 5種を候補化。デスイーター（補助上限+1）、スロット解放（メイン+1）、修羅（固定960px/s・得点1.10倍）、変換器（コインをXPへ）、ギャンブラー（停止なしランダム取得・得点1.12倍）を実処理へ接続。レジェンド枠は1つのみで、候補出現率は5%
- ビルド内で取得可能な能力が尽きた以後は選択画面を出さず、内部の3択相当のスコア報酬（18 / 28 / 46）を抽選し`Lucky!`で通知する。数値は100000m帯の実機計測後に調整予定
- 通常のLEVEL UP選択後だけ0.65秒の静止＋`GO!`へ変更。旧3秒再開カウントは通常ポーズ専用として残し、ギャンブラー・育成完了後報酬は完全ノンストップ
- コインを既存の手描き円から、通常・邪悪・幸運の透過スプライトへ置換。変換器取得中は画面上のコインもXPオーブ表示へ即時見せ替えし、回収地点にはコイン→XPの8フレーム変換演出を出す。専用UIテスト3件・`npm run verify`・`git diff --check`成功
- `?debug=1`のROGUELITE BALANCEで、修羅速度（760〜1120px/s）、レジェンド率（1〜100%、本番初期17%）、変換器XP量（0.40〜1.20倍）、育成完了後Lucky!報酬（0.50〜2.00倍）を走行中に調整・コピー可能。通常URLには表示しない
- LEVEL UP下部の能力欄はダミー表示を廃止し、メイン→補助→LEGENDの順で現在のビルドとLvをHUDと同期して表示する
- `?debug=1`のROGUELITE BALANCEへラン統計を追加。通常コイン得点、変換器XP、通常オーブXP、能力取得数、Lucky!報酬をリアルタイム表示し、変換器とXP系ビルドの価値を比較できる
- コイン出現を地上区画の約80%から55%へ減らし、通常列も3〜5枚へ減らして広間隔化。COMMONは抽選重み1、メインRAREは0.45として、早期メイン取得を幸運なビルド分岐にした。実機確認項目は`docs/HELL-RUNNER-2実機テスト項目.md`
- XP吸引は取得範囲だけでなく、近くのコイン・XPオーブがプレイヤーへ滑らかに引き寄せられる視覚効果へ変更。XPオーブは30%を高所配置とし、吸引ビルドの回収価値を上げた。コイン／XPオーブは表示サイズと発光も強化。実機テストで吸引の分かりやすさ・高所の取りやすさ・明るさを確認する
- XP吸引の初期範囲が広すぎて全回収になる問題を修正。Lv1の開始範囲を168px相当から88px相当へ縮小し、追尾速度を上げて並走せず素早く吸収するよう変更。Lv上昇で遠方・高所まで届く設計は維持
- 次の地形・当たり判定修正: (1) 飛び越える段差と走行可能な段差を高低差で明確化、(2) 空中で段差の横壁へ衝突した際に通り抜けて落下する問題の解消、(3) コイン／XPオーブの高所・横位置を吸引Lvとジャンプ判断に見合うよう再調整。実機では高所配置がまだ十分に出ていないとの報告あり
- 段差の地形生成を小段差10〜28pxと大段差68〜120pxへ二極化し、最大高さを156pxへ拡張。走行可能かジャンプ必須かを視認しやすくした。空中の横壁判定から着地補助の通過例外を削除し、必要高度がない段差をすり抜けて落下する問題を修正。高所XPは45%・+130〜230pxへ強化
- 高所コイン配置を追加。地上弧コインを+112〜224px、ラッキーコインを+160〜230px、奈落越え報酬を最高+258pxへ上げ、ジャンプ精度またはXP吸引ビルドによる回収判断を作った
- 跳ぶ必要がある大きな上り段差だけ、横壁へ朱色の面と金色の上縁を追加。高低差だけでは見落としやすい場面でも、走行可能な小段差との判断を補助する
- `?debug=1`にWALL TESTを追加。届かない高さの横壁へ空中衝突する状況を固定再現し、押し戻されて通過しないことをHR2専用UIテストで検証。通常URLには公開しない
- ギャンブラー取得中と育成完了後は、1回のXP取得で複数しきい値を越えた余剰XPも即時に連続処理するよう改善。通常の3択だけは最初の到達時に停止し、残余XPを保持する。専用テストで200XPの連続処理を確認
- 育成完了後のLucky!報酬は上部通知に加え、プレイヤー頭上へ`Lucky! +点数`を浮かせる演出を追加。走行中の報酬取得だと直感的に分かるようにした
- `?debug=1`にCOMPLETE BUILDを追加。スロット解放＋メイン3種＋補助3種Lv5の完成ビルドへ即移行し、XPをあと1にして次の取得でLucky!報酬ループを確認できる。通常URLには公開しない。専用テストでも完成ビルド遷移とLucky!を確認済み
- PC幅1200px以上の開発者ツールはゲーム右外側へ横展開し、地点移動・録画・音量・ジャンプ・ローグライト調整を2列で配置。各操作とローグライトの全スライダーを日本語化し、用途説明も付けた。ゲーム画面と開発者ツールが重ならないことを専用PCテストで検証
- 開発者ツールを再整理。確定済みの音量・小ジャンプ調整は非表示化し、右側を地点移動・再現と録画、左側をローグライト調整専用に分割。サイズ0.60〜2.20倍、高所アイテム高さ0.50〜2.50倍、高所オーブ率0〜100%、コイン出現頻度10〜90%を走行中に調整可能
- 収集物生成を整理。コインは横列・ピラミッド・高所アーチ・高所逆ピラミッド・高所一点物へ限定し、紫（邪悪）・白金（幸運）・金を混ぜた当たり列も追加。既存のコイン／XPオーブと重なる生成を抑止し、十分な高低差の二層配置だけを残す
- XPオーブは通常42px・高密度52px基準へ拡大し、変換器表示もサイズ調整へ連動。表示サイズに応じて回収判定も変わる。高所コイン率0〜100%を開発者ツールへ追加し、XP吸引は前方の遠距離吸引を抑え、遠方でゆっくり・近くで加速する追尾へ変更
- 高所コイン・高所XPオーブは、着地できる地面上へは生成せず奈落上だけに限定。3段ジャンプなしで垂直高度を要求される不公平をなくし、強制ジャンプ中の寄り道報酬として機能させる
- 収集物の基礎出現を単体中心へ再調整。地上コインは単体62%・3連22%・ピラミッド16%、地上XPオーブは単体72%・3連18%・ピラミッド10%。混色コイン列も通常8%／高所22%へ下げた。コイン／オーブの3連とピラミッドは横幅・段の高さを同じ値に統一
- 全コイン（通常・紫・白金）と全XPオーブ（通常・高密度）の基準表示を42pxへ統一し、回収判定も共通化。横3連、ピラミッド、奈落上の3連は全て56pxグリッドで配置する
- 開発者ツールへオーブ出現頻度5〜80%、複数アイテム間隔42〜112px、コイン価値0.50〜5.00倍を追加。コイン価値の初期値は1.50倍へ上げ、距離だけでスコア差が付きにくい状態を緩和
- 複数オーブ率0〜70%を開発者ツールへ追加。高所報酬は奈落ごとに共通の単体／3連パターンを選び、最大2層まで同じ形で生成する。低層は単体ジャンプ、高層は2〜3段ジャンプの選択肢となる。XPオーブ回収には澄んだ短い上昇音を追加
- HELL RUNNER 2素材工程1を完了。COMMON・RARE・LEGENDの透過能力カード枠を`images/hell-runner-2/frames/`、生成原本を`images/hell-runner-2/source/frames/`へ保存。四隅・中央の実アルファ0を検査済み。表示初期基準は約354×128px
- HELL RUNNER 2素材工程2を完了。COMMON・RARE・LEGEND・EMPTYの小型透過スロット枠4種を同フォルダへ保存。全て1254×1254pxで四隅・中央の実アルファ0を検査済み。表示サイズは工程3のUI試作で確定する
- HELL RUNNER 2素材工程3を完了。仮アイコン3個（3段ジャンプ、コイン倍率、経験値吸引）を制作し、独立した`games/hell-runner-2.html`へXPバー・左右スロット・縦3択プレビューを実装。旧版とはランキング名・保存キーを分離
- 390×844の専用テスト`tests/hell-runner-2-ui.spec.js`成功。完成画面は`docs/mockups/hell-runner-2-ui-prototype.png`。カード内文字の重なりも実画面確認で修正済み。能力抽選・取得ロジックは未実装

- HELL RUNNERのローグライト能力システム方針を確定。メインRARE 2枠、補助COMMON 3枠、特殊LEGEND 1枠とし、枠が埋まった後は未取得能力を候補から除外する
- メインは3段ジャンプ・滑空・転生から2種類だけ取得。転生はレベル制にしない。ライバルは能力化せず、現在のギャグ系ステージギミックとして残す
- 特殊能力案はデスイーター（補助上限+1）、スロット解放（メイン枠+1）、修羅（固定高速+取得後得点倍率）、変換器（コインをXPへ変換）、ギャンブラー（停止なし自動取得+取得後得点倍率）の5種類
- レベルは無制限。全能力完成後は選択画面を出さず、内部でコイン報酬を抽選して頭上通知する。通常3択後も3秒カウントを使わず0.5～0.8秒程度の短い再開猶予を試作する
- 変換器の完成後報酬は通常の10000mあたり平均コイン得点を実測し、失う直接コイン点の70～80%程度を回収する初期目標で調整する。詳細は `docs/ゲーム企画・HELL-RUNNER次期アップデート案.md` 3.8～3.10
- 能力選択画面、通常プレイ中の6スロット、経験値バーをまとめた完成予想図を `docs/mockups/hell-runner-roguelite-ui-concept.png` に保存。選択カードは携帯向け縦3枚、通常スロットは中央視界を塞がない左右分割案

### HELL RUNNER(無印)は完成品。触らない

`games/temple-run-clone.html` は **v0.5で機能凍結**。ユーザーの明示的な指示がない限り
一切変更しない。入れてよいのは致命的なバグ修正だけ。

シンプルにスコアを狙う今のゲーム性をそのまま残したいというユーザーの判断による。
**ローグライト化はHELL RUNNER 2の機能**で、無印へは持ち込まない。
2本はコードの大半が同じため、片方だけ直すと必ず食い違う。横展開しないこと。

### 進行中

**HELL RUNNERの開発は1・2とも終了した（2026-08-26）。**
残っている作業は「本日の最強決定戦」のみ。

- HELL RUNNER 2のローグライト能力システムは実装完了。正本は `docs/HELL-RUNNER-2ローグライト仕様書.md`。
  無印向けの旧指示書 `docs/HELL-RUNNER-ローグライト能力システム指示書.md` は参照禁止
- XP、候補抽選、能力取得、XPオーブ、メイン／補助／LEGENDの実効果、完成後Lucky!、開発者ツールまで実装済み

### 参考

- 新作3案（複合型音楽ゲーム、AIストーリー生成型テキスト放置ゲーム、テトリスブラスト）は `docs/ゲーム企画・HELL-RUNNER次期アップデート案.md` に整理済み。**いずれも着手しない**。音楽ゲームとテトリスは2026-08-26に制作中止
- 「旧十倍界王拳を選択式へ戻さない」はタイトル画面の旧アビリティ選択へ戻さない意味。ラン中に停止して3択ポップアップから取得する新ローグライト方式は別仕様
- 大ジャンプ(長押し)と滑空(落下中の長押し)が同じ操作になる件はユーザー確定済み。「そのまま滑空へつながる」を仕様とし、滑空したくない時は頂点付近で指を離す。入力を分ける仕組みは追加しない
- PC・携帯のポータル位置調整値は実機確認を経て本番へ反映済み。一時LANサーバーも最終確認後に停止済み
- 音量の本番値はiPhone実機試聴でBGM 0.30、ジャンプ 2.80、コイン 1.50、ライバル 2.00に最終確定
- ポータルの主要な品質改善は実装済み。今後は実機確認で気になる細部が出た場合のみ、光量・余白・文字サイズを微調整する
- ユーザーが両ゲームを公開し、プレイヤーの感想を集める段階
- 次回は寄せられたプレイ感・ランキング推移を見て調整する

### 新しい会話で最初に進める内容

1. `AGENTS.md` とこのファイルを最後まで読む
2. `docs/ダークネオンアーケード仕様書.md` と `docs/mockups/dark-neon-arcade-concept.png` を確認する
3. 実装済みのPC・スマホワイヤーフレームを確認し、必要なら配置や文言を調整する
4. 実画面でロゴ・背景・キービジュアルの重なりとトリミングを確認する
5. PC・スマホ実機で操作盤とCOMING SOON筐体を確認し、必要な場合のみ細部を微調整する

重要: モックアップ一枚を背景として貼らない。背景、ロゴ、キービジュアル、筐体装飾、HTML文字へ分解して実装する。

## 作業履歴の追記形式

作業後はこの下へ新しい日付を上にして追記する。

### 2026-08-21 — Codex（本日の最強決定戦・企画確定）

- 新作を、近未来の路地裏で当日最速の反応速度を競う`本日の最強決定戦`として企画確定。日次1位の名前・キャラ・反応時間が次の挑戦者の敵になる、日替わりの王者戦を差別化の中心にする
- プレイヤー用の5人、各3種ボイス、リアル寄りの立ち絵＋前景ドット絵の選択カード、監視ドローンの`ENGAGE!`合図、`CAUTION! CURRENT CHAMPION`の王者戦導入、フライングと異常記録への対策を仕様として記録
- 実装・素材制作は未着手。会話での企画整理のみのためテストは未実行

### 2026-08-21 — Codex（本日の最強決定戦・キャラクター企画原画）

- レイヴン、ミカ、ブリック、ノイズ、キリの共通絵柄・衣装・アクセントカラーを揃えた5分割のキャラクター企画原画を生成し、`images/todays-champion/source/character-roster-concept.png`へ保存
- この原画を立ち絵、選択カード、2Dドット絵の人物一致の基準にする。ボイスはユーザーが用意する仕様
- 変更: `images/todays-champion/source/character-roster-concept.png`、`PROJECT-STATUS.md`。企画原画の追加のみのためテストは未実行

### 2026-08-21 — Codex（本日の最強決定戦・選択カード立ち絵とドット絵設計）

- レイヴン、ミカ、ブリック、ノイズ、キリを、共通の雨の近未来都市と各キャラ固有色で統一した選択カード用の縦長立ち絵として`images/todays-champion/portraits/`へ追加
- 同じ衣装・配色を、待機／決闘時の警戒／勝利ポーズへ落としたドット絵の企画原画`images/todays-champion/source/pixel-character-poses.png`を追加。ゲーム実装時はこの設計から小さな個別スプライトへ分割または描画する
- 変更: `images/todays-champion/portraits/{raven,mika,brick,noise,kiri}.png`、`images/todays-champion/source/pixel-character-poses.png`、`PROJECT-STATUS.md`。素材追加のみのためテストは未実行

### 2026-08-21 — Codex（本日の最強決定戦・モーション確認画面）

- `games/todays-champion-motion-preview.html`を追加。5人の選択カード、選んだキャラと相手のドット絵、待機→警戒→勝利の1.2秒ごとの自動切替を1画面で確認できる
- ドット絵企画原画をCanvasへ拡大表示し、元のドットの硬さを保つため画像補間を無効化。後からゲーム本体へ被弾・踏み込み・ヒットストップを追加する前の、人物の見分けやすさとポーズ確認に絞った
- `tests/verify.cjs`へ確認画面を登録。検証: `npm.cmd run verify`（6ファイル／インラインスクリプト9件、構文・構造・素材参照すべてOK）

### 2026-08-21 — Codex（本日の最強決定戦・携帯モーション確認）

- `tests/static-server.cjs`は通常の127.0.0.1待受を保ちつつ、`HOST`環境変数を明示した実機確認時だけLAN待受へ切り替えられるよう変更
- port 4174を`0.0.0.0`で一時起動し、携帯から確認ページを開けることをローカルHTTP 200で確認。`npm.cmd run verify`も6ファイル／インラインスクリプト9件ですべて成功

### 2026-08-21 — Codex（本日の最強決定戦・ポータル用ラベル絵）

- ポータルのカセットと携帯機画面で共用する、文字なしの正方形ラベル絵を生成。上寄りにレイヴン／ミカ／監視ドローン、下3割に雨の路面反射だけを置き、現行ポータルの画面トリミング安全地帯を守った
- 原本を`images/portal/source/cart-label-todays-champion.png`へ、公開用を`images/portal/cart-label-todays-champion.webp`へ保存。`tools/portal-images.mjs`の変換対象へ登録し、`npm.cmd run portal:images`で640×640・71KBへ変換できることを確認

### 2026-08-21 — Codex（本日の最強決定戦・ポータル導線）

- `index.html`のCOMING SOON手前へ`本日の最強決定戦`カセットを追加。説明は「今日の最強へ反応速度で挑め」、タグは`開発中`／`スマホ対応`、共通発光色は警告灯イエロー`#ffd34e`とした。稼働本数表示は5へ更新
- PLAY先の`games/todays-champion.html`を追加。キャラ選択と待機→警戒→勝利のドット絵確認を含む`IN DEVELOPMENT`ページで、`← GAME BASE`から置き場へ戻れる
- `tests/portal.spec.js`を新しいカセット数・画像取得・開発中ページのPLAY導線へ更新、`tests/verify.cjs`へ開発中ページを登録。検証: `npm.cmd run test:portal`（10件成功）、`npm.cmd run verify`（7ファイル／インラインスクリプト10件、構文・構造・素材参照すべてOK）
- ユーザー指示でコミット`a0f5a44`（`本日の最強決定戦を追加`）を`main`へpush。未使用の企画原画と音ゲーテストMP3は公開対象から除外した

### 2026-08-21 — Codex（本日の最強決定戦・公開素材の圧縮）

- 2.2〜2.8MBだった立ち絵PNG5枚を幅480px・WebP品質84へ変換し、合計11.9MB→409KBへ圧縮。画面でのカード表示より十分大きい解像度を残して、携帯の読み込みを軽くした
- ドット絵は見た目を変えない可逆WebPへ変換し、1.9MB→1.4MB。HTML参照をすべてWebPへ切り替え、原本PNGは公開対象外の`images/todays-champion/source/`へ移動
- 再現可能にするため`tools/todays-champion-images.mjs`と`npm run champion:images`を追加。検証: `npm.cmd run test:portal`（10件成功）、`npm.cmd run verify`（7ファイル／インラインスクリプト10件、構文・構造・素材参照すべてOK）

### 2026-08-21 — Codex（本日の最強決定戦・ドット絵切り取り修正）

- 元のポーズ表を一定幅・一定高で切っていたため、警戒ポーズの足元に勝利ポーズの一部、横端に隣キャラの一部が映ることを390px表示で確認
- ポーズごとの縦切り取り範囲と5人それぞれの横切り取り範囲を明示し、縮尺が変わっても足元で揃うよう下端基準で描画。開発中ページとモーション専用ページの両方へ反映
- 検証: 390px Playwright実画面で待機／警戒を目視確認、`npm.cmd run verify`（7ファイル／インラインスクリプト10件成功）

### 2026-08-21 — Codex（本日の最強決定戦・NPC練習決闘）

- 開発中ページへ`START PRACTICE`を追加。ランダムな待機時間のあと、監視ドローンの`ENGAGE!`へ決闘画面をタップすると、反応時間とNPC（310〜420ms）の反応を比較して勝敗を表示する
- 合図前に触れた時は`FEINT!`として即NPC勝利にする。決着時は勝者だけ勝利ポーズを取り、RETRYから同じキャラで再試合できる。選択カードは待機中・合図中に切り替えないため、試合の相手が途中で変わらない
- 検証: Playwrightでフライング→`FEINT!`、再試合で`ENGAGE!`後に決着することを確認。`npm.cmd run verify`（7ファイル／インラインスクリプト10件成功）

### 2026-08-21 — Codex（ポータル携帯版の位置確定）

- ユーザー実機調整値を携帯幅（599px以下）の本番初期値へ反映。挿入カセットはX=28px / Y=15px / 1.80x、タグはY=-141px、情報カウンターはY=-138pxを含む全21値を明示した
- 情報カウンターを上へ移動したことで残るフッター前の空白は、携帯限定で情報セクションの下余白と負の下マージンを対にして解消。390×844の実表示で情報カウンター末尾からフッターまで18pxに収まり、詰まりすぎないことを確認
- ポータルのPATCH NOTESへ2026.08.21の携帯レイアウト調整を追記。Discord通知はGitHubへpushした差分を自動送信する構成のため、コミット`e8d38a6`をpushして通知対象へ反映
- 変更: `index.html`、`PROJECT-STATUS.md`
- 検証: `npm.cmd run test:portal`（10件成功）、`npm.cmd run verify`（4ファイル／インラインスクリプト7件、構文・構造・素材参照すべてOK）

### 2026-08-21 — Codex（RHYTHM LAB 全モード比較試作）

- 同じ曲・共通の拍時間／判定／スコア／コンボを土台に、TAIKO・POP・DDR・ORBITの4モードを切り替えて遊べる`games/rhythm-game-test.html`を追加。PCキーと携帯タップの両方に対応
- BPMとAUDIO OFFSETを実機で詰め、コピー共有できる`DEV SYNC TUNER`を追加。譜面の作り込みより先に、各モードの操作感と同期を比較するため
- Canvasが開始前のカバーと違い150px相当の高さしか描画されない問題を実画面で発見し、ステージ内側全体を描画・操作面に修正
- 曲の再生失敗を黙殺して無音のままノーツを開始していたため、再生成功後にだけ開始するよう修正。失敗時は`AUDIO ERROR`と再試行導線を出す
- BPM不明の音源へ固定120BPMの譜面を置いていたため、`TAP BEAT`で4拍からBPMと拍の起点を取る方式を追加。専用テストも4回タップ後のSYNCED表示を確認するよう更新
- `tests/rhythm-game.spec.js`と`npm run test:rhythm`を追加。4モードの切替と390px携帯でCanvasがステージ内側を埋めることを確認
- 変更: `games/rhythm-game-test.html`、`tests/rhythm-game.spec.js`、`tests/verify.cjs`、`package.json`、`PROJECT-STATUS.md`
- 検証: `npm.cmd run test:rhythm`（1件成功）、`npm.cmd run test:hex-ui`（1件成功）、`npm.cmd run test:hex`（108件成功）、`npm.cmd run verify`（5ファイル／インラインスクリプト8件、構文・構造・素材参照すべてOK）

### 2026-08-21 — Codex（HEXAMINEデイリーHINT制限）

- デイリーだけHINTを3回までに制限。HUDへ`HINT（残り 3）`を表示し、解ける手を実際に示せた時だけ1回消費する。空振りで回数を失わせないため
- 残数をデイリー保存データへ追加。既存の同日記録は0回使用から安全に移行し、ミス・経過時間と同様に入り直しても回復しない。残り0でボタンを無効化
- `tests/hexamine-ui.spec.js`へ3回消費・0回表示・再入場後も0回の回帰テストを追加
- 変更: `games/hexamine.html`、`tests/hexamine-ui.spec.js`、`PROJECT-STATUS.md`
- 検証: `npm.cmd run test:hex-ui`（2件成功）、`npm.cmd run test:hex`（108件成功）、`npm.cmd run verify`（5ファイル／インラインスクリプト8件、構文・構造・素材参照すべてOK）

### 2026-08-21 — Codex（HEXAMINE携帯の外側ヒント操作）

- 外側ヒントの数字から盤外側へ、面に沿った透明なタップ判定帯を追加。マスの面へは重ならないため、数字を狙い損ねた操作が原子／空孔の確定へ抜けない
- 携帯では外側ヒントをタップすると対象範囲を固定表示し、同じヒントの再タップで閉じる方式へ変更。押している間だけでは推理に必要な範囲を読めなかったため
- `tests/hexamine-ui.spec.js`と`npm run test:hex-ui`を追加。判定帯端のタップ、マスの不変、固定表示と閉じる操作を確認
- 変更: `games/hexamine.html`、`tests/hexamine-ui.spec.js`、`package.json`、`PROJECT-STATUS.md`
- 検証: `npm.cmd run test:hex-ui`（1件成功）、`npm.cmd run test:hex`（108件成功）、`npm.cmd run verify`（4ファイル／インラインスクリプト7件、構文・構造・素材参照すべてOK）

### 2026-08-21 — Claude Code（ポータル刷新・HEXAMINE公開準備・軽量化）

**ポータルを筐体方式から実機＋カセット方式へ全面刷新した。**

- 中央のスケルトン携帯機1台へCSS製カセットを挿す形へ。差込口が光り、内部が
  ゲームの色に灯り、画面へ絵が出る。新作で増えるのは絵1枚だけになった
- サイト名を `VIVI GAME BASE` へ。背景は地下の秘密基地、ロゴはCSSの文字
- カセット置き場は折り返さず横スクロール。PCはドラッグ可。10本で実測確認
- HEXAMINE と HELL RUNNER 2（β）を掲載。稼働4本
- 筐体まわりのCSS71規則、デバッグコード52行、未使用画像10枚を削除

**HEXAMINEを公開できる状態にした。**

- 手順つきの誘導チュートリアル、間違えたマスを開かない仕様、長押しの誤爆対策
- ∞モードとデイリーを不定形の盤面へ。デイリーランキング（Supabase）を実装
- Discord通知へ追加。日付は日本時間で固定

**約20MB軽量化した。**

| | 前 | 後 |
|---|---|---|
| 討伐2048 | 7.2MB | 3.2MB（BGMを80kbpsへ） |
| HELL RUNNER 1 | 6.2MB | 1.8MB（未使用2.5MB削除＋WebP化） |
| ポータル素材 | 8.9MB | 359KB |

- HELL RUNNER 1 は凍結中だが、ユーザーの明示的な指示による。素材の再圧縮と
  未使用データの削除のみで、挙動は変えていない

**環境整備**

- `sharp` / `ffmpeg-static` を追加し、変換をスクリプト化
- コミット前に `npm run verify` を走らせるフックを追加（壊れたHTMLで実際に止まることを確認）
- git の `core.quotepath=false`。日本語ファイル名が読めるようになった
- 権限リストの使い捨て25件を汎用28件へ整理

**踏んだ罠（同じ事故を繰り返さないため）**

- **試作を既存ページへ持ち込むとき、クラス名の衝突が最大の事故要因。**
  `.shell` がページ幅コンテナと同名で、ヘッダーとヒーローが崩れた。`cs-` を付けて解決
- **発光の層は本体の絵より後に置く。** 同じ z-index ならDOM順が描画順になり、
  前に置くと不透明な絵に隠れて一度も見えない
- **ドラッグ追跡に `setPointerCapture` を使うと click が奪われる。** 追跡は window で行う
- CSSアニメーションはブラウザペインが非表示だと進まない。`resize_window` で復帰する

- 検証: `npm run verify` / `test:portal`(10) / `test:hex`(108) / `test:boss`(10) / `test:runner`(25) すべて成功

### 2026-08-19 — Codex（HELL RUNNER 2 収集物・開発者ツール調整）

- PCの開発者ツールを左右のゲーム外余白へ分割し、固定済みの音量・小ジャンプを非表示化。未確定のローグライト値だけを日本語説明付きで大きく調整できるようにした
- コイン／XPオーブの生成重複を防止し、整理されたコインパターン、高所コイン頻度、混色コイン列、サイズ連動の回収判定を実装。XP吸引は前方からの違和感を抑え、近距離加速へ変更
- 検証: HELL RUNNER 2専用Playwrightテスト3件、`npm run verify`、`git diff --check` 成功

### 2026-08-19 — Codex（HELL RUNNER 2 高所報酬・デバッグ移動修正）

- コンボ表示をXPバー内の小表示から、赤青のネオン枠・大きい連番・得点倍率・残り時間ゲージを持つ独立パネルへ置換
- `?debug=1`へ、コンボ本体のサイズ／X／Y、コンボゲージの幅／X／Y、実コインを待たずに表示を出す確認ボタンを追加
- メイン・補助枠が埋まった後、LEGEND抽選に外れたXP到達はLucky!コイン報酬に変更。LEGENDだけが出た時に見送った場合も同じ報酬を得て、デスイーター待ちのスコア稼ぎを成立させた
- 検証: HELL RUNNER 2専用Playwrightテスト7件、`npm run verify`、`git diff --check` 成功

### 2026-08-19 — Codex（HELL RUNNER 2 コンボ表示の初期実装）

- デスイーター取得時だけ、横方向を広げず上方向のアイテム吸引・取得判定を+210px補正。奈落上3層目を狙う用途に限定
- XPオーブを取るたびXPバーと数値を即時更新するよう修正。コイン連続取得はXPバー右端へ`COMBO x数`と残り時間ゲージを表示
- LEGENDだけが候補に残る場合は、欲しくないLEGENDを「今回は見送る」で取得せず通過可能。レジェンド抽選に外れたLEVEL UPは停止せず走行を継続
- LEVEL UPを開く際は、携帯の開発者ツールを自動で縮小してカードと見送りボタンを覆わないよう修正
- 検証: HELL RUNNER 2専用Playwrightテスト6件、`npm run verify`、`git diff --check` 成功

### 2026-08-19 — Codex（HELL RUNNER 2 高所報酬・デバッグ移動の初期修正）

- 3層目は経験値吸引の所持有無に関係なく、60,000m以降に出現するよう変更。2層目は20,000m以降のまま
- デバッグの距離移動で、能力ビルド・能力レベル・XP進行・ラン統計を引き継ぐよう修正。死神は距離到達ではなくLEGEND「デスイーター」取得中だけ追従する
- 空中でXPが満ちた場合は、その場で停止せず、次の着地を確認してからLEVEL UPを表示するよう変更
- XPバーとXP数値は内部XP自体は加算されるが、閾値到達時にしか描画更新していない未完了状態を確認。コインコンボの可視化と得点調整の工程で同時修正予定
- 検証: HELL RUNNER 2専用Playwrightテスト5件、`npm run verify`、`git diff --check` 成功

### 2026-08-19 — Codex（HELL RUNNER 2 初期高所報酬・デバッグ移動修正）

- XPオーブ取得音をコインと同じ基準音量へ合わせた。音色は魂を吸い込む上昇感を維持
- 収集物の基準を、サイズ2.20x・発光1.80x・高所高さ1.50x・コイン40%・オーブ20%・複数オーブ5%・高所コイン40%・高所オーブ20%・56pxグリッドへ更新。コイン価値1.50xは保留のまま
- 奈落上の報酬を最大3層化。2層目は20,000m、3層目は60,000mかつ経験値吸引Lv6以降だけに限定
- 地点移動時に古いLEVEL UP候補・一時停止状態を残さないよう修正し、レジェンド所持状態でも移動後に選択できる自動テストを追加
- 検証: HELL RUNNER 2専用Playwrightテスト4件、`npm run verify`、`git diff --check` 成功

### 2026-08-18 — Codex（HELL RUNNER 2 ローグライト基盤・地形判別）

- HELL RUNNER 2だけに、ラン中XP、3択、メイン／補助／LEGENDスロット、5種のLEGEND、完成後Lucky!報酬を実装。通常版HELL RUNNERは変更していない
- XP吸引を実際の追尾として見える化し、初期吸引範囲を狭めて、Lv上昇で高所アイテムに届くよう調整。コイン・XPオーブの表示、配置高、出現間隔も見直した
- 地形を小段差とジャンプ必須の大段差へ二極化。大段差の上り壁には朱色・金色の縁取りを追加し、空中横壁の通過例外をなくした
- `?debug=1`へ完成ビルド、横壁再現、ローグライト数値調整を追加。通常URLには露出しない
- 検証: HELL RUNNER 2専用Playwrightテスト3件、`npm run verify`、`git diff --check` 成功

### 2026-08-17 — Codex（討伐2048配置確定・エフェクト追従）

- ユーザー実機調整の停止ボタン、勇者、ミミック、ドラゴン、魔王のサイズ・X・Yをデバッグ初期値と通常プレイへ反映
- ボスの接地点と表示高から共通の中心座標を算出し、攻撃エフェクト、ダメージ数字、被弾・回復・撃破パーティクルを3ボスそれぞれへ追従させた
- 勇者の位置調整に足元の床、ボス位置調整に背景光も追従。攻撃画像とパーティクルの広がりは各ボスの拡大率に合わせる
- HELL RUNNERと同じ `formatHudNumber` を導入し、チャレンジ中・ゲームオーバー時のスコアとベストをK／M表記へ統一
- 検証: 討伐2048専用テスト10件成功、確定3ボスのエフェクト中心座標とK／M境界値を確認、PC目視・`npm run test:syntax`・`git diff --check` 成功

### 2026-08-17 — Codex（討伐2048サイズ調整デバッグ）

- `?debug=1`限定のSIZE TUNERを追加し、停止ボタンはサイズ/X/Y、勇者は共通サイズ/X/Y、ミミック・ドラゴン・魔王はそれぞれ独立したサイズ/X/Yをリアルタイム調整可能にした
- 全15項目のスライダーと直接数値入力を双方向同期し、COPY VALUESで対象別に整理した共有用JSONを表示。Clipboard APIが使えない携帯でも手動コピー可能
- PCではゲーム右側、携帯では初期縮小表示とし、通常URLではパネルと調整APIを非公開
- デバッグ中は全3ボスを一時解放するが、`bossDemoUnlockedStages`へ書き込まず通常プレイの進行を汚さない
- 検証: 討伐2048専用テスト9件成功、PC目視で全ボスと調整パネルを確認、`npm run test:syntax`・`git diff --check` 成功

### 2026-08-17 — Codex（討伐2048タイル移動エフェクト改善）

- `slideAndMergeGrid()` が出力タイルごとの元位置を返すようにし、合体時は遠い側の元位置を採用
- 盤面全体の移動元を `gridMoveFrom` へ保存し、描画時に各タイル固有の距離を計算。静止タイルと新規出現タイルには移動オフセットをかけない
- 既存のアニメーション速度・ease-out・合体ポップ・スコア計算は変更せず、使われなくなった全体移動方向の状態を削除
- デバッグURL限定で合体処理と方向座標変換を検査可能にし、通常URLでは引き続き非公開
- 検証: 討伐2048専用テスト4件成功、4方向の座標変換と合体元追跡を確認、`npm run test:syntax` 成功、HTML末尾・script 2/2・`git diff --check` 成功

### 2026-08-17 — Codex（討伐2048一時停止・音量設定）

- `images/stop-button-magic-blue.png` を戦闘画面左上へ配置し、HPバーと重ならない64×64pxの停止操作を追加
- PAUSE画面へPLAY／RETRY／HOMEと、BGM／効果音の0〜100音量ゲージを追加。設定は端末へ個別保存
- 一時停止中はグリッド入力、Canvas演出、合体後の攻撃・回復・勝敗タイマー、AudioContextを停止。P／Escapeとアプリ非表示時の自動停止にも対応
- PCと390×844の携帯表示を目視確認し、停止メニューが1画面に収まることを確認。ブラウザの警告・エラーなし
- 検証: 討伐2048専用テスト7件成功、`npm run test:syntax` 成功、HTML末尾・script 2/2・`git diff --check` 成功

### 2026-08-16 — Codex（HELL RUNNER一時停止機能）

- プレイ中だけ表示する48×48pxのⅡボタンと、既存START/HOME画像を流用した一時停止メニューを追加
- PLAY後は停止地点を維持したまま3秒カウントし、完了後に再開。P/Escapeも同じ経路へ統一
- タブ・アプリが非表示になった場合は自動停止し、復帰しても自動再開しないよう対応
- 停止中はBGMの再生位置を保持し、カウント開始時に同じ位置から復帰。ゲーム全体のサウンドON/OFFを保存可能にした
- `clearJumpInput()` と `jumpCanBeCut = false` を併用し、空中停止後に大ジャンプが勝手に小ジャンプへ変わらないよう保護
- `paused` / `pauseCountdown` 中は物理だけでなく粒子・コウモリ・`animT` も停止し、現在距離の背景を維持
- 正式な128×128透過ポーズボタンへ差し替え、デバッグモードに28〜96pxのサイズ調整・直接入力・コピーを追加。PC・携帯共通の本番値は96pxに確定
- PCの主操作をSpaceキーとしてタイトル画面と遊び方へ明記し、クリック操作も併用可能なまま維持。携帯にはPC向け文言を表示しない
- 検証: `tests/debug-mode.spec.js` 17件成功、ポーズサイズ調整の関連テスト成功、`npm run test:syntax` 成功

### 2026-08-16 — Codex（HELL RUNNER大小ジャンプ試遊版）

- 上昇中に入力を離すと残り上向き速度をカットする方式で、短押しの小ジャンプと長押しの従来相当大ジャンプを実装
- イベント順に依存せず毎フレーム `holdingInput` を見ることで、`pointerdown → pointerup → update()` の最速タップも小ジャンプにした
- pointerとkeyboardの押下元を分離し、携帯では最初のpointerIdだけを採用。2本目の指のpointerupで意図せずカットされないよう修正
- 2段目・レッドブルの3段目へ同じ大小判定を適用。長押しだけで次段を自動消費しない既存キーリピート対策を維持
- DEV TOOLSへ `JUMP HEIGHT` を追加。`RELEASE CUT`、実測AIRTIME/DISTANCE/HEIGHT、JUMPS、MODE、設定値コピーを実装
- LAN内HTTPで自動コピーできない携帯向けに、手動コピー用の選択済みテキスト欄へフォールバック
- 携帯では項目増加後もタイトル画面のPLAYを塞がないよう、デバッグパネルを初期縮小
- 遊び方とタップヒントを短押し／長押しへ更新し、滑空説明を「落下中の長押し」へ明確化
- `JUMP_RELEASE_CUT` はPC・携帯の実機試遊結果を反映し、0.50で確定
- 検証: `tests/debug-mode.spec.js` 14件成功、`npm run test:syntax` 成功、HTML末尾 `</html>`、script 2/2、`git diff --check` 成功

### 2026-08-16 — Claude Code（成果物の保全・指示書レビュー・キーリピート修正）

- Git管理外だったポータル刷新の成果物を取り込み。`images/portal/` の公開素材14点、`tools/build_portal_titles.py`、`tests/portal.spec.js`、一時停止機能指示書。**素材が欠けると実機で確定した位置調整値を再現できないため**
- `images/portal/source/`(42MB)は `.gitignore` へ追加。世代違いの中間生成物が大半で、一度履歴へ入れると消せないため。**原本はディスク上に残している**
- 筐体の一体型画像化で使われなくなった `.control-panel` / `.coming-screen` / `.coming-controls` を削除。これらが参照する `control-deck-*.webp` と `coming-soon-cabinet.webp` はすでに存在しないファイルだった。適用されないルールの `url()` は取得されないため実害は無かったが、素材の欠落と誤認されるため除去
- `keydown` に `e.repeat` ガードを追加し、キーリピートで2段・3段ジャンプが勝手に消費されるバグを修正。滞空約0.69秒に対しリピート開始が約0.5秒後のため、空中で実際に発火していた。タイトル・ゲームオーバーでの `startGame()` 連打も同時に防止
- 一時停止機能指示書へ「12. 実装アンカー」を追記。**パーティクルとコウモリの扱いが指示書と既存コードで矛盾していた**(既存は転生カウント中も動かす意図的な作り)。指示書側を優先し転生時の挙動は変更しない方針を明記。あわせて `animT` の加算条件も直さないと背面が静止しない点、`reviveCountdown` が変数ではなく `state` の値である点、巨大 `dt` 対策が `Math.min(0.033, ...)` で既に入っている点を記録
- 大小ジャンプ指示書へ「15. レビュー結果」を追記。**「最低カット受付時間を経過済み」をカット条件にしていたため、速いタップほど大ジャンプになる欠陥**を指摘。ジャンプは `update()` 内で次フレームに発動するため、素早いタップでは押下時間が事実上0になり必ず下限を下回る。時間で門を作らず常にカットし高さの下限で守る方式を提案
- 同指示書に小ジャンプの設計意図を確定して追記。**本質は「高さ」ではなく「滞空時間＝着地点」の調整**。用途は(1)早く着地して `jumpsUsed=0` に戻し満タンで奈落へ入る、(2)高速域で760px跳ぶ2段目を小さく撃ち、190〜430px(狭い足場は56〜102px)の足場へ降りる、の2点。これに伴い4節の調整目標の単位が誤りと判明。カット率kに対し滞空時間と水平距離はk倍だが高さはk²倍のため、「頂点55〜65%」では水平距離が74〜81%までしか縮まず用途(2)に届かない。目標を滞空時間・水平距離で50〜60%へ定義し直した
- 検証: `git diff --check` 相当の空白エラーなし、`index.html` と本体の `<script>` 対応・`</html>` 終端を確認、ポータルが参照する全画像の実在を確認。**Playwrightと構文検査は未実行**(この環境から `node` が見えないため)。次に触る担当が `npm test` / `npm run test:syntax` を実行すること

### 2026-08-16 — Codex（ポータル位置調整モード）

- 筐体へ焼き込まれていた3タイトルを空看板へ戻し、BIZ UDゴシック Bold・固定文字高の独立透過WebPへ変更
- `?portalDebug=1` 限定のPORTAL TUNERを追加。HELL・討伐2048・COMING SOONのタイトルと、2作品のPLAYをX・Y・SIZEで個別調整可能
- 調整値をJSONで取得する `COPY VALUES`、初期値へ戻す `RESET`、携帯でも使えるパネル縮小ボタンを追加
- ユーザーがPCで確定したタイトル・PLAY値を、幅600px以上に限定した本番初期値へ反映
- HELL・討伐2048・COMING SOON各筐体のX・Y・SIZE調整を追加し、COMING SOONが上へ重なる負の余白を解除
- 各調整スライダーの横を直接編集できる数値入力欄へ変更し、両方を同期
- 同一Wi-Fi上のHTTP接続ではClipboard APIが無効になるため、COPY時に選択済みJSON欄を表示する手動コピー代替を追加
- 携帯の手書き調整値をスクリーンショットと照合し、3タイトルと2つのPLAYへ幅599px以下限定で反映
- PLAY画像だけを縮小する構造へ変更し、見た目を筐体差込口へ合わせつつ56pxのタップ領域を保持
- 旧PLAY反射レイヤーを画像ボタンでは完全に無効化し、筐体外へ四角い光がはみ出さないよう修正
- ステータス行を2段構成へ変更し、NOW PLAYINGとタグが中途半端に折り返されないよう固定
- PLAYの大きなタップ領域まで発光していた問題を修正し、hover・押下演出を実画像だけへ限定
- 透過タイトル3枚は1024×192、四隅アルファ0、完全透明画素ありを検査
- 検証: ポータル専用8件成功、筐体移動・数値入力同期・Clipboard APIなしでの手動コピー表示を確認、`npm run test:syntax` 成功、差分の空白エラーなし

### 2026-08-16 — Codex（ポータルワイヤーフレーム）

- `index.html` を `VIVI GAME ARCADE` のダークネオンアーケード構成へ刷新
- PCは2筐体横並び、スマホは1列とし、素材未配置時にもゲーム名・説明・PLAYが表示されるフォールバックを実装
- NEWS、PATCH NOTES、CREDITS、ABOUTをキーボード操作可能なモーダルへ整理し、既存の更新履歴とクレジットを移行
- 公開用素材フォルダ `images/portal/` を作成し、仕様書で定めた画像ファイルを置くだけで反映される参照先を準備
- 検証: `npm run test:syntax` 成功、差分の空白エラーなし、2作品のリンク先ファイルと4モーダルの対応を確認

### 2026-08-16 — Codex（ポータル背景素材）

- ダークネオンアーケードのPC用背景と、同じ世界観のスマホ専用縦構図を画像生成
- PNG原本を `images/portal/source/`、公開用WebPを `images/portal/` へ保存
- 公開用をPC 2560×1440・182KB、スマホ1080×1920・110KBへ変換し、中央上部のロゴ用暗部と左右の筐体列を確認
- ロゴ案は正確な文字で生成できたが、透過背景が実画像化されていたため不採用。誤った素材を本番へ入れずHTML文字を継続
- 背景PC版・スマホ版はユーザー確認を経て正式採用
- ロゴの指示書を改訂し、市松模様などの疑似透過を禁止。実アルファ値と重ね表示で合否判定する工程を明文化
- 改訂した指示でロゴを再生成し、1600×600のRGBA PNG、四隅アルファ0、完全透明画素58.9%を確認して正式採用
- `images/portal/vivi-game-arcade-logo.png` をポータルへ組み込み、読み込み失敗時にはHTML文字が残る構造を維持
- ロゴ画像の読み込み完了を検知して背面のHTML文字を隠すよう修正。PC・390pxスマホの実表示で二重表示なし、横スクロールなしを確認
- PC背景は入口ヒーローの680pxまで表示して暗くフェードし、それ以降はキービジュアルと情報を主役にする現在の構成を正式採用。下部用の別背景画像は追加しない
- HELL RUNNERの既存地獄背景・死神素材を参照し、右向きの骸骨ランナーと追跡する死神のキービジュアルを制作
- 討伐2048は数字を画像へ焼き込まず、魔法タイルと青紫の巨大ボスでゲーム内容を表現
- PNG原本を `images/portal/source/`、1600×900の公開用WebPを `images/portal/` へ保存。HELL RUNNER 255KB、討伐2048 193KB

### 2026-08-16 — Codex（ポータルUI仕上げ）

- 追加画像を増やさず、筐体画面下へ金属製コントロールデッキ風の区切りをCSSで追加
- PLAYボタンへ内側フレーム、左右ランプ、hover時だけ通過する弱い反射を追加し、ゲーム固有色を維持
- INFORMATION COUNTERを一体型の金属パネルにまとめ、各項目へ統一アイコン枠・走査線・矢印・フォーカス可能な操作反応を追加
- スマホでは装飾の余白を縮め、2列配置と56px以上のPLAYタップ領域を維持
- `prefers-reduced-motion` の既存設定により、反射移動を含むアニメーションは停止可能

### 2026-08-16 — Codex（ポータル文言・テスト）

- NEWSをリニューアル告知と2作品の稼働情報へ更新し、ABOUTへCOMING SOONの扱いを追記
- フッターへ制作者表記、GitHub、クレジット入口、サイトバージョンを整理
- `tests/portal.spec.js` を追加し、PCの2筐体比較、スマホ1列・横はみ出しなし・PLAY高さ56px以上を検証
- ロゴ画像の取得失敗時にHTMLタイトルへ戻ること、情報モーダル開閉後にスクロールが戻ることを検証
- 背景2枚、ロゴ、キービジュアル2枚のHTTP取得を検証
- テスト結果: ポータル専用5件成功、`npm run test:syntax` 成功、差分の空白エラーなし

### 2026-08-16 — Codex（公開前最終検証）

- ポータル、HELL RUNNER、討伐2048、背景比較を含む全25件を実行し、すべて成功
- `npm run test:syntax` 成功、差分の空白エラーなし
- ポータル刷新のローカル実装・素材制作・検証は完了。コミット・push・公開は未実施

### 2026-08-16 — Codex（完成予想図との再比較）

- PC実画面を `docs/mockups/dark-neon-arcade-concept.png` と再比較
- 背景・ロゴ・キービジュアル・基本配色・操作性は良好だが、筐体が平面的なWebカードに見え、完成予想図の物理筐体らしさが不足
- ジョイスティックとボタンを含む操作盤、筐体脚部、専用COMING SOON筐体、画面反射・金属質感、小アイコンの統一が未完了
- 現在版はPCで全高1855pxあり、完成予想図のように1画面内でロゴ・2筐体・COMING SOON・情報カウンターを俯瞰できない
- 自己評価は総合68/100。公開完成扱いを取り消し、追加素材とレイアウト再設計を次工程とする

### 2026-08-16 — Codex（ポータル品質向上）

- 2台のゲーム筐体へ立体的な操作盤、ジョイスティック、4ボタン、脚部を追加し、完成予想図のアーケード筐体らしさへ近づけた
- 消灯したCRT画面のCOMING SOON専用素材を生成し、操作部を備えたミニ筐体として配置
- PC版のロゴ・筐体・情報カウンターの余白と寸法を再設計し、全高を約1855pxから約1063pxへ圧縮。1440×1000では情報カウンター下端まで表示可能
- スマホ版は説明文とタグを維持し、390×844で横はみ出しなし、PLAY高さ60px、操作盤表示を確認
- 公開画像の取得テストへCOMING SOON素材を追加

### 2026-08-16 — Codex（操作盤の画像素材化）

- CSSの単純な図形で描いていたジョイスティック・ボタン・操作盤を廃止し、透過WebP素材へ置換
- HELL RUNNER用は傷のある黒鉄と赤熱光、討伐2048用は青紫の魔導金属、COMING SOON用は無点灯の黒鉄として3種類を個別制作
- PNG原本を `images/portal/source/`、公開用透過WebPを `images/portal/` へ保存。全素材でアルファ0〜255と完全透明画素を確認
- 透明余白を公開用変換時に切り詰め、PCでは筐体幅いっぱい、390pxスマホでは325×108pxで細部が読める寸法へ調整
- HTMLのPLAYボタンとリンクは画像へ焼き込まず維持し、操作性・フォーカス・レスポンシブを保った
- 公開画像取得テストへ3種類の操作盤素材を追加

### 2026-08-16 — Codex（筐体全体の画像素材化）

- HELL RUNNER、討伐2048、COMING SOONの3台を、看板・画面・操作盤・下部外装・脚まで含む一体型筐体画像として制作
- ゲーム名は筐体看板へ正確に収録。COMING SOONは青紫に発光するアナログ砂嵐CRTとして制作
- 2作品のPLAYは筐体画像へ焼き込まず、ゲーム別の独立した透過ボタン画像を制作。筐体の空き枠へ割合座標で重ね、画像全体をリンク領域にした
- 生成直後に市松模様を描いた原本は不採用。HELL RUNNER筐体、討伐2048筐体、COMING SOON筐体、PLAYボタン2種の全5素材で、生成原本の実アルファ・四隅0・完全透明画素・暗色/明色背景表示を検査
- 透過生成共通ルールを仕様書へ追加し、後処理で透明化した試作を「生成時点の透過合格」と扱わないことを明文化
- スマホではPLAY画像が小さくてもリンク領域を最低56pxへ拡張し、押しやすさを維持

### 2026-08-16 — Codex（筐体・PLAY寸法の統一）

- 透過後の素材切り詰めとCOMING SOON専用CSSにより、3筐体の実表示サイズとPLAY2種の比率が不揃いだったことを確認
- 生成原本は維持したまま、公開用3筐体を同一1200×1680キャンバス・同一1080×1580外形・同じ床面へ正規化
- PLAY2種も同一1024×256キャンバス・同一960×240外形へ正規化し、リンク配置を左22%・上69.5%・幅56%・比率4:1の共通値へ変更
- COMING SOONのPC表示幅をほか2台と同じ461pxへ変更
- 1440px PCで3筐体すべて461×645.39px、PLAY2種とも258.16×64.53px、横はみ出しなしを確認
- 筐体3台とPLAY2種の表示寸法が一致することをポータル自動テストへ追加

### 2026-08-16 — Codex（共通マスターから筐体を新規制作）

- 個別生成による外形差を解消するため、空看板・空画面・操作盤・PLAY差込口・脚を固定した共通透過マスターを新規制作
- HELL RUNNER、討伐2048、COMING SOONは同じマスター形状へ各画面と発光色だけを適用し、アルファマスク・原寸・外形・床面を完全共通化
- 生成編集時に透明が失われる問題が再発したため、合格済みマスターのアルファを3台へ共通適用する正式工程へ変更。最終原本3枚は1054×1493、四隅0、同一アルファ境界
- ゲームタイトルはBIZ UDゴシック Bold、文字高76px、同一中心座標で正確に合成。HELL RUNNER、討伐2048、COMING SOONのフォントとサイズ感を統一
- PLAYは筐体側の黒い角丸差込口へ収まる、外枠・ボルト・側面ランプなしの角丸ガラス板へ変更
- PLAY2種は同一1024×256、BIZ UDゴシック Bold、文字高90px、同一中心座標。テーマごとの差は赤・紫の発光色だけ
- PC実測は3筐体すべて461×645.39px、PLAYリンク2種184.39×46.09px。390px携帯は3筐体幅343px、PLAY画像137.19×34.30px、リンク領域137.19×56px、横はみ出しなし
- 旧v2/v3/v4筐体・旧PLAY・旧CSS操作盤・旧COMING SOON画面・旧比較画像の計21ファイルを削除。削除済み素材を公開取得テストから除外

### 2026-08-16 — Codex

- ゲーム置き場を `VIVI GAME ARCADE` として刷新するデザイン・レスポンシブ・素材サイズ・制作ルールを仕様書化し、PC版コンセプトモックアップを保存
- iPhone SafariでもBGM音量が効くよう、BGMをWeb Audio経由へ統一
- HELL RUNNERのデバッグモードへ3系統の音量スライダー・現在値表示・設定値コピーを追加
- 実機試聴でBGM 0.14、ジャンプ/コイン 1.5、ライバル 2.0に確定し、デバッグモードへ落下しても確認を継続できる `NO FALL` 切替を追加
- デバッグパネルをPCではゲーム外の右余白へ移し、携帯向けの縮小切替とJUMP・COINの個別ミキサーを追加
- 携帯でデバッグパネルを縦スクロールできず音量欄へ到達できなかった問題を修正し、画面高さいっぱいまで表示可能に変更
- iPhone実機の個別ミキサーで最終調整し、本番音量をBGM 0.30、ジャンプ 2.80、コイン 1.50、ライバル 2.00に確定
- 同じ項目を反復調整する場合、毎回コードを直す前にデバッグ機能で簡易化できないか提案するルールを `AGENTS.md` に追加
- 関連テストのみ実行し、構文検査とデバッグ非公開・BGMクロスフェード・音量スライダーの確認に成功

### 2026-08-15 — Codex

- 公開先を確認したところ、GitHubには旧3ファイルしかなく `games/`・`images/`・`audio/` が未配置。次回公開時はフォルダごと反映が必要
- HELL RUNNERのモバイルHUDを折り返し禁止・均等幅へ変更し、ゲームオーバーとホームではHUD自体を非表示化
- タイトル画面を上端基準のスクロール配置へ変更し、ロゴと3ボタンをモバイル向けに縮小
- 公開入口の `index.html` はルートに残し、2本のゲーム本体を `games/` フォルダへ整理
- ポータル、画像、BGM、ホーム導線、テスト、指示書の参照先を新配置へ更新
- HELL RUNNERの公開画面をスマホ比率・固定描画密度・10%引きへ統一
- HUDのK表記、デバッグ用スクリーンショット、30000〜100000mの緩やかな加速を追加
- 討伐2048のランキング得点をゲーム終了時の盤面合計へ変更
- 構文検査を両ゲームへ拡張し、Playwrightを16件へ追加
- テスト結果: `npm run test:syntax` 成功、`npm test` 16件成功

### 2026-08-22 — Codex（本日の最強決定戦：端末共通の基準座標）

- `games/todays-champion.html` のキャラ・攻撃エフェクト・デバッグ用ガイド線を、390×844の基準座標で扱う方式へ変更。保存するX/Yとサイズ値は端末固有pxではなく基準画面の値であり、携帯・横長PC・異なるモニター比率では短辺基準で同率に拡大縮小する
- 信号はすでに背景画像の`cover`拡大・切り抜き位置に追従する実装のため、キャラ側も同じ「基準を固定して実表示だけ変換する」方針に統一した。DEV TUNERへ基準座標であることを明記し、入力名も`X (BASE)`/`Y (BASE)`へ変更
- `tests/todays-champion.spec.js`へ携帯390×844とPC1440×900で基準換算のキャラ幅・足元位置が一致するテストを追加。最強決定戦の専用Playwright 6件、`npm run verify`、`git diff --check`成功
- 他ゲームを監査。HELL RUNNER 2はPC時点で390×844の縦長フレームへ固定済み。討伐2048・音ゲーテストはCanvas実寸から全描画位置を算出し、HEXAMINEは再描画時に盤面寸法を取り直すため、今回のようなDOM画像の固定px補正を横展開する必要は現時点でない。凍結済みのHELL RUNNER無印は変更していない
- 未コミット。ユーザー実機で基準値を再確認後、明示的な`push`指示で最強決定戦HTML・専用テスト・本記録だけをコミット対象にする
- ユーザー実機で確定した信号の基準値は、RED `(2.38, 8)`、YELLOW `(3.4, 8)`、GREEN `(4.3, 8)`、共通SIZE `3.59`。キャラ・ガイド・音量・モーション速度`0.2`は上記の既存初期値と一致
- DEV TUNERへ`ALL LAMPS: ON/OFF`を追加。調整中だけ赤・黄・緑を同時点灯し、3灯の相対位置と背景への収まりを確認できる。本番の信号進行には混ぜない
- DEV TUNERのコピーを`SPRITES`、`SIGNAL`、`GUIDE`、`AUDIO`、`SPEED`単位へ分割。必要な項目だけ短いJSONで共有できる。信号のドット風デザインはサイズ感が背景と合わなかったため、従来の丸いCSSと発光へ戻した
- 全5キャラのguard/dash/attack/win/lose/effectをミカ基準から実機で個別調整。確定値は`games/todays-champion.html`の`playerSpriteTuning`が正本。以後の値共有はDEV TUNERの`COPY SPRITES`を使う
- DEV TUNERの`ATTACK + EFFECT: LINK ON`では、攻撃ポーズまたは攻撃エフェクトのX/Yを動かすと相手側も同じ差分だけ追従する。現在の相対配置を壊さず動かすためで、SIZE/FLIPは個別調整のまま
- 5キャラを再微調整。WIN/LOSEのXを中央寄りへ寄せ、ブリックLOSE倍率を0.51へ変更した。通常URLではDEV TUNERやポーズプレビューは出ず、日替わり王者の取得・対戦・勝利時の記録登録が通常フローとして有効
- キャラ・エフェクト・ガイド線も信号と同じ背景画像座標から計算する方式へ変更。横長PCでは画面の左右50%ではなく、携帯と同じ道路上の座標へ配置し、決着時の突進距離も両者の背景座標間に限定する
- PCではSpaceで開始・信号への反応が可能（入力欄・DEV TUNER操作中は無効）。CAUTION中はRETRYを必ず隠し、黄黒の面でも読める青緑・琥珀の文字へ変更
- キャラ名／プレイヤー名を各キャラの足元中央へ移動。`?debug=1`の`NAME LABELS`で共通X/Y/SIZEを細かく調整でき、`COPY NAMES`で個別に値共有できる
- 最強決定戦の専用Playwrightは、Space操作・CAUTIONのRETRY非表示・名前調整を加えた12件が成功。`npm run verify`、`git diff --check`成功。未コミット
- ポータルのPATCH NOTESへ更新漏れを追記。HEXAMINE v0.2（デイリーの通知・HINT制限）、本日の最強決定戦 v0.1、HELL RUNNER 2 v0.1 β、HELL RUNNER v0.5（軽量化・完成版固定）を掲載

### 2026-08-22 — Codex（本日の最強決定戦：初戦の素材準備）

- 初回対戦だけポーズ画像の読み込み・デコード途中に表示を切り替えることで、縮小状態や反転前の絵が一瞬出る問題を対策。背景、ポートレート、全5キャラの全ポーズ・攻撃エフェクトをページ表示直後から先読みし、準備中にSTARTした場合は`PREPARING ARENA`を表示して全素材の準備完了後にだけ試合を開始する。デコード済み画像への参照を保持するため、演出中にメモリから解放されて再度初期表示になることも避ける
- 最強決定戦の専用Playwright 12件、`npm run verify`、`git diff --check`成功。未コミット。ユーザーから明示的な`push`指示があれば、`games/todays-champion.html`と本記録のみを対象にする

### 2026-08-22 — Codex（本日の最強決定戦：対戦表示の簡素化）

- 対戦中の説明文（「赤を待て」「交差点をタップ」など）をすべて廃止。信号表示だけで状況を伝え、決着後の下部表示は反応時間の`ms`値だけにした。フライングは信号・下部表示ともに`FLYING`のみを出す
- 専用Playwright 12件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-22 — Codex（本日の最強決定戦：ボイス導線と登録画面プレビュー）

- 選択画面で、非選択カードを暗転・低彩度化し、選択中だけを明るく表示。携帯幅でキャラ名と肩書きが重ならないよう、名前・肩書きの下端と行高を分離した
- 5キャラ×選択／開始前／勝利の15台詞用の再生導線を追加。音源到着後は`voiceSources`の各`null`をファイルパスへ置き換えるだけで有効になる。別キャラを選ぶと直前の声を停止して切替え、同一キャラ連打では再生し直さない。選択画面へ戻る／START時も声を停止する。開始台詞がある場合は850msだけ信号開始前に間を置き、勝利台詞は勝利ポーズと、ランキング入力を出す同じタイミングで再生する
- DEV TUNERへ`VOICE`音量を追加。`RANKING ENTRY`で勝利ポーズ・154ms・中央の登録フォームを即時プレビュー可能。現在の登録画面は実装済みだが、勝者を中央へ寄せる専用の戴冠演出は未着手。先に実機で現行構図を確認してから判断する
- 専用Playwrightを13件へ増やし、選択状態と登録画面プレビューを確認。`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-22 — Codex（本日の最強決定戦：選択画面の帰還導線）

- `← GAME BASE`は選択画面の右上だけに表示し、試合中は非表示へ統一。背後に残っていた旧リンクも完全に非表示化したため、画面上・アクセシビリティ上ともに戻り導線は一本だけ
- ユーザー報告の信号・CAUTIONの一時的なズレを確認。直近の変更には信号の座標値、倍率、背景追従計算、信号CSSの変更はない（RED `(2.38,8)`／YELLOW `(3.4,8)`／GREEN `(4.3,8)`／SIZE `3.59`を維持）。再読込で回復したため、更新直後のブラウザキャッシュと素材読込が混在した一時状態として扱い、数値は憶測で変更していない
- 専用Playwright 13件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-22 — Codex（本日の最強決定戦：王者登録画面）

- 有効な勝利でランキング入力を出す時は、勝利キャラを中央・拡大し、`CROWN TAKEN`／`YOU ARE TODAY'S CHAMPION`／反応時間／`ENTER YOUR NAME`の専用画面へ切替える。通常の結果表示や敗者、RETRYは混ぜない
- 登録成功後は入力欄を消し、`NEXT CHALLENGER: BEAT <記録>ms →`だけをゲーム置き場へのリンクとして表示。登録前には`GAME BASE`リンクを増やさず、左右に戻る矢印が並ばないようにした
- DEV TUNERの`RANKING ENTRY`で、王者登録画面を即時確認可能。専用Playwrightは13件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-22 — Codex（本日の最強決定戦：王者画面の再構成）

- ユーザー実機画像で確認した重なりを解消。選択画面のゲーム置き場導線は文字ボタンを廃止し、HELL RUNNERと同じ左上の`◀`だけへ変更。試合中の`FIGHTER SELECT`は完全に隠し、勝敗確定後だけRETRYの上へ表示する
- 王者登録画面から`CROWN TAKEN`、中央の対戦用信号文、左右の戻り導線を除去。`YOU ARE TODAY'S CHAMPION`と記録だけを残し、記録は虹色が流れる表示にした。フォームは足元を覆う大型パネルをやめ、画面下の小型入力＋REGISTERへ変更
- 登録後はゲーム置き場へ遷移せず、`FIGHTER SELECT`だけを表示して選択画面へ戻る。ゲーム置き場へは選択画面左上の`◀`から戻る
- `?debug=1`の`CHAMPION SCREEN`でFIGHTER／TITLE／SCORE／FORMそれぞれのX・Y・SIZEを独立調整でき、`COPY CHAMPION SCREEN`で値を共有できる。専用Playwright 13件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-22 — Codex（本日の最強決定戦：王者画面PC値と信号補正）

- ユーザー確定のPC向け王者画面初期値を反映: FIGHTER `(-24,-80,1)`、TITLE `(11,84,1.36)`、SCORE `(11,71,1.8)`、FORM `(19,45,1.74)`。携帯はこの基準値で実機確認後に調整する
- 王者画面では通常ステージの中央`signal`、`VS`、信号3灯をすべて非表示化。`YOU ARE TODAY'S CHAMPION`は調整可能な王者タイトル1つだけにした
- 信号3灯の個別X/Yだけが端末ごとの基準倍率を掛けずCSS pxのまま残っていたため、キャラ・信号全体と同じ`metrics.unit`で拡大縮小するよう修正。背景画像は実寸1280×720で、`SIGNAL_IMAGE`定義とも一致を確認済み
- 専用Playwright 13件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-22 — Codex（本日の最強決定戦：決着ボタンの個別調整）

- 決着後にだけ出る`FIGHTER SELECT`と`RETRY DUEL`を、`?debug=1`の`RESULT BUTTONS`で個別に調整可能にした。各ボタンのX／Y、WIDTH／HEIGHT、角丸、文字サイズ、表示文言を変更でき、`COPY RESULT BUTTONS`で設定値だけ共有できる
- `RESULT BUTTONS`プレビューを追加し、王者ランキングを更新せずに勝敗ポーズと2ボタンの重なりを確認できる。通常の対戦中は選択ボタンを表示しない
- `FIGHTER SELECT`が画面を縦に引き伸ばしていた原因は、旧CSSの`top`と新しい`bottom`が同時に有効だったため。`top:auto`を明示し、指定高さだけのボタンへ修正
- 専用Playwright 14件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-22 — Codex（本日の最強決定戦：記録とフライング表示）

- 通常の決着時の反応時間は画面下ではなく、勝者キャラの上へ表示するようにした。プレイヤー勝利は左、NPC勝利は右のキャラ上に追従する
- フライング時は上部の合図`FLYING`だけを表示し、下部の重複表示を廃止。DEV TUNERの`RESULT BUTTONS`プレビューでも同じく上側だけに統一した
- 専用Playwright 14件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-22 — Codex（本日の最強決定戦：決着ボタン値とCAUTION導線）

- ユーザー確定の決着ボタン初期値を反映: SELECT `(67,-50,140×48,角丸0,12px)`、RETRY `(-80,0,140×48,角丸0,12px)`
- RETRYから日替わり王者との対戦へ入り直す時、`FIGHTER SELECT`の`hidden`を戻していなかったためCAUTION中に残っていた。CAUTIONへ移る前にRETRYとSELECTを両方必ず隠すよう修正
- 専用Playwright 14件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-22 — Codex（本日の最強決定戦：プレイヤー上の結果スコア）

- 通常の反応時間とフライング表記を、勝敗に関係なくプレイヤー（左側）キャラの頭上へ統一。上部の合図は`YOU WIN!`または`NPC WINS`だけとし、フライング時も`NPC WINS`を維持する
- `?debug=1`に`RESULT SCORE`（X／Y／SIZE、`COPY RESULT SCORE`）を追加。`RESULT BUTTONS`のプレビューは上部`NPC WINS`＋プレイヤー上`FLYING`で本番同様に確認できる
- 調整済みの`ALIGNMENT LINE`と`COPY GUIDE`をDEV TUNERから削除し、不要な基準線を出さないようにした
- 専用Playwright 14件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-23 — Codex（本日の最強決定戦：ブリック選択ボイスの試聴版）

- 受領したブリック選択ボイスを、元音源を残したまま`audio/todays-champion/voices/brick-select-preview.mp3`へ試聴用として出力。頭尾の不要な無音を整理し、低域ノイズ除去、声の輪郭補正、ゲーム用の音量正規化（48kHzモノラル／128kbps MP3）を適用した
- 現時点ではゲームへは未接続。ユーザーが試聴して音作りの方向性を決めた後、確定名で`voiceSources.brick.select`へ接続する
- 出力ファイルをデコード検査済み。未コミット

### 2026-08-23 — Codex（本日の最強決定戦：ブリック選択ボイスの仮接続）

- `voiceSources.brick.select`へ`brick-select-preview.mp3`を接続。ブリックを選択した時だけ再生し、同一キャラの連打では再生し直さない既存ルールを維持する
- DEV TUNERの`AUDIO MIX → VOICE`でボイス再生中も音量を即時調整できるように変更。`PLAY SELECT VOICE`を追加し、現在選択中キャラの選択ボイスを試合に入らず何度でも確認できる
- ブリック選択クリック時に試聴版MP3への実際の読み込み要求が出ることを専用Playwrightへ追加して確認。専用Playwright 15件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-23 — Codex（本日の最強決定戦：ブリックボイスの空間音抑制）

- 同じ元M4Aから試聴版を再出力。発話前の無音を強めに削り、低域カット、適応ノイズ低減、ゲート処理で空間音を抑えた。途中の発話まで切れてしまう設定は出力時間1.12秒の時点で破棄し、話し始めだけを詰める方式へ修正済み
- 現在の試聴版は5.38秒でデコード検査済み。ゲーム接続先は同じファイルのため、次の公開で自動的にこの版へ置き換わる。未コミット

### 2026-08-23 — Codex（本日の最強決定戦：選択BGMとCAUTION重ね再生）

- `kk_scene28`素材の72秒ループM4Aを`audio/todays-champion/select-bgm.m4a`として追加。最初のキャラ選択タップからループ再生し、通常対戦・王者戦とも信号開始前に約0.56秒でフェードアウトする
- 王者戦のCAUTION中は選択BGMを残したまま既存のCAUTION BGMを重ね、信号前に両方を止めて信号音を明瞭にした。`AUDIO MIX → SELECT BGM`で選択BGMの音量を実機調整できる
- フェード中も早押しはフライングとして判定する。専用Playwright 15件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-23 — Codex（本日の最強決定戦：開始ボイス前提の信号導線）

- 戦闘画面に切り替えた後、開始ボイスがあるキャラは1.2秒の発話枠を確保してからBGMを0.56秒でフェードアウトし、完全無音で信号へ入る。音源未登録キャラも短い間を置くため、後から開始ボイスを追加しても信号タイミングを組み直さずに済む
- 選択BGMはページ表示直後から再生を試行し、自動再生が禁止される携帯ブラウザでは最初の画面タップを起点に開始する。START／キャラ選択のタップでも開始を保証する
- 専用Playwright 15件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-23 — Codex（本日の最強決定戦：音量調整と選択ボイス確認）

- `AUDIO MIX`の各スライダー／数値入力は、変更中と変更確定後の両方で対象音源へ即時反映する共通処理に統一。`PLAY / STOP SELECT BGM`と`PLAY / STOP CAUTION BGM`を追加し、ゲーム進行を待たずBGM音量を確認できる
- 選択ボイスは同じキャラを連続クリックしても再読込・再生しない。一度別キャラを選択してから戻った場合だけ、再度そのキャラの選択ボイスを再生する。ブリック連打時にMP3読み込みが1回だけであることを専用Playwrightで検証した
- 専用Playwright 15件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-23 — Codex（本日の最強決定戦：携帯で効く音量ミキサー）

- iPhone Safariでは`HTMLAudioElement.volume`が実質変更できず、PC用の音量スライダーが携帯で効かなかった。BGM・信号・衝突音・ボイスをWeb Audioの`GainNode`経由へ変更し、`AUDIO MIX`の値を携帯でも即時反映するよう修正
- 最初の画面タップでAudioContextを解除してから選択BGMを開始するため、携帯の自動再生制限下でも以後のツール調整が有効になる
- 専用Playwright 15件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-23 — Codex（本日の最強決定戦：Safari向けバッファ音声ミキサー）

- SafariでMediaElement経由のGainNode音量が効かなかったため、選択BGM・CAUTION BGM・キャラボイスをWeb Audioのデコード済みAudioBufferとして直接再生する方式へ変更。ツールの`SELECT BGM`／`CHAMPION`／`VOICE`はSafariでもGainNodeの値だけで音量を制御する
- ブリック選択ボイスは再生完了後に同じキャラでもツールから再試聴でき、別キャラを挟んで戻った時も再生する。再生中の同一キャラ連打だけを止める
- 選択BGMを128kbpsから64kbps AACへ圧縮し、`1,174,668`Bから`597,015`Bへ軽量化。専用Playwright 15件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-23 — Codex（作業フォルダの整理）

- HELL RUNNER 2の自動生成UI確認スクショ4枚は、テスト実行による差分だったため追跡済みの状態へ戻した。ゲーム本体・公開素材には変更なし
- ユーザー指示により未追跡の`音ゲーテスト作成用サンプル.mp3`を削除。以後、音ゲー関連は明示指示があるまで対象外
- 削除済み試作曲を参照していた未公開の音ゲーサンプルからも参照だけを外した。開発・公開は行わず、全体検査が止まらない状態だけを保つ

### 2026-08-23 — Codex（本日の最強決定戦：キリ選択ボイスの試聴版）

- 受領したキリの選択ボイスを、発話前後の無音整理、低域の響きの軽減、声の輪郭を保つ圧縮、ゲーム用音量化を施した`audio/todays-champion/voices/kiri-select-preview.wav`として追加
- `voiceSources.kiri.select`へ接続。キリを選んだ時にだけ再生し、選択中の別キャラへ切り替えた時は前の台詞を止める既存ルールをそのまま使う
- 専用Playwright 16件が成功。全体構造検査は、試作曲参照を外した後に実行する

### 2026-08-23 — Codex（本日の最強決定戦：個別ボイス音量と信号音同期）

- 決着ボタンのSELECT初期値を`(76,-85)`へ更新。音量初期値はSELECT BGM`0.2`、CHAMPION`0.97`、VOICE`0.5`へ反映
- DEV TUNERの`AUDIO MIX`へRAVEN／MIKA／BRICK／NOISE／KIRIの個別VOICE倍率を追加。全体VOICEとキャラ別VOICEは掛け合わせるため、全体バランスを保ったまま各録音だけ補正できる。`COPY AUDIO`にも個別値を含める
- 信号1/2・3と衝突音も、BGM／ボイスと同じAudioBuffer直接再生に統一。選択時の最初のタップで事前デコードし、Safariで音だけが灯り表示より遅れる余地をなくした
- 履歴確認では、選択BGM追加・ランキング入力画面追加は信号の座標／倍率／CSSを変更していない。最後の信号見た目変更はドット風を元の丸い灯りへ戻した時点。専用Playwright 16件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-23 — Codex（本日の最強決定戦：信号の一括位置調整）

- 実機スクリーンショットで確認した「3灯がまとめて左上へずれる」ケースのため、`SIGNAL LAMPS`へ`GROUP X`／`GROUP Y`を追加。RED／YELLOW／GREENの既存微調整を壊さず、灯りグループ全体を右下へ動かせる
- 履歴上、選択BGM追加とランキング入力画面追加は信号の座標・倍率・CSSを変更していない。背景`cover`基準へ移行した信号グループの基準点を実機で補正できるようにした
- 王者戦のランダム信号待機が最大値寄りの時に専用テスト8秒上限を越える可能性があったため、ゲーム仕様に合わせ10秒へ変更。専用Playwright 16件、`npm run verify`、`git diff --check`成功。未コミット
- ユーザー実機で確定した携帯向け初期値: GROUP `(4,6)`、RED `(1.5,8)`、YELLOW `(3.4,8)`、GREEN `(4.3,8)`、SIZE `3.59`。PCも同じ背景基準の値として扱う
- 個別VOICEの初期値はRAVEN`1`、MIKA`1`、BRICK`1.6`、NOISE`1`、KIRI`1.01`。全体VOICE`0.5`と掛け合わせて再生する

### 2026-08-24 — Codex（本日の最強決定戦：キリ開始・勝利ボイスと調整画面整理）

- 受領したキリの開始・勝利ボイスを、前後無音の整理、低域・空間ノイズの軽減、声の輪郭を保つ圧縮、ゲーム用音量化を施し、`audio/todays-champion/voices/kiri-start-preview.wav`（2.48秒）と`kiri-win-preview.wav`（5.55秒）として追加。既存の開始待機・勝利モーション時のボイス導線へ接続した
- DEV TUNERに`VOICE LINES`を追加。全体VOICE×キャラ別VOICEに加え、BRICK SELECT／KIRI SELECT／KIRI START／KIRI WINを録音ごとに試聴・個別調整できる。`COPY VOICE LINES`で値も個別に共有できる
- 確定済みのキャラ位置・サイズ、信号、名前、王者入力画面の座標ツールはDEBUG表示から隠し、音声・結果画面の確認を優先。実行時の設定値は変更していない
- ゲーム置き場のクレジットへ「本日の最強決定戦　音楽：かねこかずき（https://kazuki-kaneko-music.com/）」を追加
- 専用Playwright 17件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-24 — Codex（本日の最強決定戦：レイブン3種ボイス仮接続）

- 受領したレイブンの選択・開始・勝利台詞を、前後無音の整理、低域ノイズ軽減、声の輪郭を保つ圧縮、ゲーム用音量化を施し、`raven-select-preview.wav`／`raven-start-preview.wav`／`raven-win-preview.wav`として追加
- 選択時、戦闘前、勝利時の既存導線へそれぞれ接続。DEBUGの`VOICE LINES`にはRAVEN SELECT／RAVEN START／RAVEN WINとして独立した音量スライダー・試聴ボタン・コピー値が出る
- 専用Playwright 18件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-25 — Codex（本日の最強決定戦：0msからのランキング登録）

- 従来の`90ms以上`制限を撤廃。0msを含むすべてのプレイヤー勝利をランキング入力画面へ進める
- `0`をJavaScriptの未設定値として扱わないよう、勝利記録の判定・登録処理を`null`判定へ変更
- 専用Playwright 18件、`npm run verify`、`git diff --check`成功。未コミット

## PC環境再開までの詳細引き継ぎ（2026-08-25）

### 公開状態と作業フォルダ

- 公開済みの`origin/main`最新コミットは`55e6c29`（最強決定戦の0msランキング登録対応）。ランキング通知によりリモートが自動更新されることがあるため、次に公開する時は必ず`git fetch origin main`を先に実行する
- 元作業フォルダには、過去に公開済みの素材・ゲーム本体が差分として残る。これは一時worktreeで選択公開しているためで、**`git add .`、reset、checkoutで片付けないこと**。公開時は最新`origin/main`から一時worktreeを作り、対象ファイルだけをコピーしてcommit/pushする
- HELL RUNNER（`games/temple-run-clone.html`）は完成・凍結。引き続き触らない。音ゲー試作（`games/rhythm-game-test.html`）も明示指示があるまで対象外

### 本日の最強決定戦：現在の仕様

- 入口はキャラ選択画面。ゲーム置き場へ戻る導線はここだけにある。PCはクリックに加えSpaceで開始・信号への反応が可能
- キャラの表示位置・倍率と信号灯の座標は、実機調整済みの値を維持している。DEBUGでは信号・キャラ・名前・王者入力画面の座標ツールを非表示にしているため、通常は触らない
- 信号は背景画像の座標基準へ追従する。ただし灯りはPCと携帯で見え方の差が残る可能性があり、PC実機で確認する時はユーザーが確定した携帯値（GROUP `(4,6)`、RED `(1.5,8)`、YELLOW `(3.4,8)`、GREEN `(4.3,8)`、SIZE `3.59`）を基準にする
- モーション初期速度は`0.2`。結果画面・ランキング入力画面の配置はDEBUGのFORCE RESULT PREVIEWで確認できる

### 最強決定戦：音と台詞

- 選択BGMは画面開始から再生を試行し、キャラ選択〜信号開始まで流れる。Safari対策としてAudioBuffer/GainNode再生になっており、DEBUGの音量変更も実機で反映する
- 王者戦だけCAUTION画面を`2.2秒`表示。CAUTION BGMは画面終了と同時に停止する
- 戦闘画面では開始台詞後に約`3秒`待ち、選択BGMを`0.56秒`で消して完全無音から信号1/2/3へ入る。決着ポーズでは選択BGMを`0.56秒`で戻し、勝者の勝利台詞と重ねる
- 接続済み台詞：RAVEN（選択・開始・勝利）、KIRI（選択・開始・勝利）、BRICK（選択のみ）。MIKA／NOISEは未到着。DEBUGの`VOICE LINES`で録音ごとの試聴・音量調整・値コピーができる
- ブリック選択ボイスは一部つぶれがあるため、録り直し待ち。キリ勝利ボイスは高域ノイズを強く抑えた仮版だが、元録音由来のジリつきが残る報告あり。PC再開後に再試聴し、必要なら録り直し素材を優先する
- クレジットはゲーム置き場に「本日の最強決定戦　音楽：かねこかずき（https://kazuki-kaneko-music.com/）」を追加済み

### 最強決定戦：ランキング

- Supabaseテーブルは`todays_champion_scores`。当日最速かつ`280ms未満`の記録を王者としてNPCへ反映する
- プレイヤー勝利は`0ms`を含め、すべてランキング入力へ進む。`0`を未設定として扱わないよう`lastWinMs === null`で判定している
- 決着時の勝利台詞は勝者側のキャラを再生する。プレイヤー敗北時はNPC側の勝利台詞（登録済みであれば）を鳴らす

### HEXAMINE

- PCの盤面判定は左クリックだけ。右クリックとホイールクリックは何も判定せず、誤答・誤タップを防止済み
- スクロールホイールの拡大縮小は盤面の閲覧操作として残している。停止したのはホイールの押し込みクリックのみ

### 再開時の確認手順

- 最強決定戦を触るなら`npx playwright test tests/todays-champion.spec.js`、HEXAMINEなら`npx playwright test tests/hexamine-ui.spec.js`を優先。巨大HTMLを編集した後は必ず`npm run verify`と`git diff --check`
- PC実機で音量・信号位置・結果画面を確認する時は`games/todays-champion.html?debug=1`を使う。座標項目は意図的に隠しているので、どうしても再調整が必要になった場合だけ一時的に表示を戻す

### 2026-08-25 — Codex（本日の最強決定戦：素材先読みの軽量化）

- 初回表示から全5キャラ・全ポーズ・エフェクトを先読みしていた処理（約`4.6MB`）を廃止。START DUEL時にプレイヤーと対戦相手の2キャラ分だけを読み込むよう変更
- 画像のdecode完了を待ってから開始するため、初回対戦で縮小・反転前の画像が一瞬出る既知問題は再発させない。選んでいないキャラの通信・メモリ消費を避ける
- 専用Playwright 18件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-25 — Codex（パッチノート更新）

- `index.html`のPATCH NOTESにHEXAMINE `v0.3`（PCの右・ホイールクリック誤答防止）と、本日の最強決定戦 `v0.2`（キャラボイス、BGM導線、0msランキング、素材先読み軽量化）を追記
- ポータル専用Playwright 10件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-24 — Codex（HEXAMINE：PCの誤クリック防止）

- 盤面の判定操作をPCでは左クリックだけに限定。右クリック・ホイールクリックは盤面上でも何も判定せず、ブラウザのコンテキストメニュー／自動スクロールも抑止する
- 右クリック・ホイールクリック後に未判定マス数が変化しないPC操作テストを追加。既存の携帯ヒント・デイリーHINTも含めPlaywright 3件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-24 — Codex（本日の最強決定戦：決着後BGMとキリ勝利ボイス再調整）

- キリ勝利ボイスを再度元録音から処理。高域の帯域を大きく絞り、圧縮によるノイズの持ち上がりを抑えた。台詞の明瞭さよりジリつき低減を優先した仮版
- CAUTION画面終了時にCAUTION BGMを即停止し、開始セリフ中へ持ち越さないよう修正
- 決着ポーズに移るタイミングで選択BGMを約`0.56秒`でフェードイン。勝者の台詞と重ねつつ、通常の選択BGM音量まで戻す
- 専用Playwright 17件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-24 — Codex（本日の最強決定戦：敗北時の勝者ボイス）

- 決着時の`win`台詞は、プレイヤー勝利時はプレイヤー、NPC勝利時は敵キャラを再生するよう統一。勝者側の台詞だけが鳴る
- 専用Playwright 17件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-24 — Codex（本日の最強決定戦：キリ勝利ボイス再整音と開始間隔）

- キリ勝利ボイスを元録音から再処理。高域のジリつきを抑えるローパス、低域ノイズの整理、圧縮を強め、音量のピークも余裕を持たせた
- CAUTIONは`3.0秒`から`2.2秒`へ短縮。戦闘画面の開始台詞後は、BGMのフェードを含め約`3.56秒`後に信号開始となるよう余韻を確保した
- 専用Playwright 17件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-26 — Codex（本日の最強決定戦：ランキング登録画面の再調整）

- `?debug=1`の`CHAMPION SCREEN`を再表示。FIGHTER／TITLE／SCORE／FORMをそれぞれX・Y・SIZEで再調整でき、`COPY CHAMPION SCREEN`で確定値だけを共有できる
- `RANKING ENTRY`プレビューでは、`YOU ARE TODAY'S CHAMPION`と虹色の`154ms`、入力フォームを同時に表示したまま調整できる。信号・VSは本番どおり非表示
- 専用Playwright 18件、`npm run verify`、`git diff --check`成功。未コミット

### 2026-08-26 — Codex（本日の最強決定戦：ランキング登録画面の初期値）

- ユーザー実機調整値を初期値へ反映: FIGHTER `(-24,-80,1.00)`、TITLE `(11,84,0.63)`、SCORE `(11,71,0.61)`、FORM `(0,45,1.22)`

### 2026-08-26 — Codex（本日の最強決定戦：ランキング登録失敗の診断）

- ゲームは0ms登録へ変更済みだったが、Supabase作成SQLは`ms between 90 and 3000`のまま残っていた。90ms未満の好記録がDB制約`23514`で拒否され、画面上では通信失敗と誤表示される不整合
- 作成SQLの制約・RLSチェックを0〜3000msへ修正。本番DBにはユーザーが同等の`ALTER TABLE`を実行済み
- 登録失敗時はDBの値制限・権限設定・通信などを区別し、エラー番号を表示する。続いて`42501`を確認し、公開INSERT権限・採番権限もユーザーが実行済み

### 2026-08-26 — Codex（本日の最強決定戦：PC実機調整の再開）

- 結果ボタンのSELECTを`(76,-50)`へ変更。RETRYは既存`(-80,0)`を維持
- 個別VOICEのKIRIを`1.01`から`0.74`へ変更。その他の音量初期値は維持
- PCで信号位置を再調整できるよう、DEBUGの`SIGNAL LAMPS`と`COPY SIGNAL`を再表示。GROUP X/Y、各灯X/Y、SIZEを利用可能

### 2026-08-26 — Codex（本日の最強決定戦：PC／携帯別の調整値）

- 信号、結果ボタン、AUDIO MIX、キャラ別VOICEをPC／携帯で別プロファイル化。片方でDEBUG調整しても、もう片方の確定値を上書きしない
- PCは結果SELECT`(76,-50)`・KIRI VOICE`0.74`、携帯は従来のSELECT`(76,-85)`・KIRI VOICE`1.01`を初期値として保持。`COPY SIGNAL`／`COPY AUDIO`には現在の端末種別も含める
- PC信号の確定値: GROUP `(4,1)`、RED `(1.5,8)`、YELLOW `(2.2,8)`、GREEN `(3,8)`、SIZE `2.68`。携帯信号値は従来のまま

### 2026-08-26 — Codex（本日の最強決定戦：公開画面からDEV導線を除去）

- キャラ選択画面にあった`OPEN DEV TUNER`の表示・リンクを削除。PC／携帯とも通常URLでは開発ツールを一切出さず、調整時だけ`?debug=1`を付けたURLを直接開く
- 本章までの最強決定戦専用Playwrightは現行20件成功。PCのSpace操作テストは当日王者データへ依存しないよう固定済み

### 2026-08-26 — Claude Code（テスト基盤の穴を塞ぐ）

**`npm run verify` が `games/hell-runner-2.html` を検査していなかった。**
`tests/verify.cjs` の対象一覧に入っておらず、08-18〜08-21のHELL RUNNER 2作業で
記録した「verify 成功」は、この4.0MBのファイルを一度も開いていない成功だった。
`tetris.html` も同様だったため一度は対象へ加えたが、tetris は後述のとおり同日に削除した。

追加しただけでは14件の誤検知で常に落ちるため、素材参照の判定を直した。

- `icon:'ability-main-glide.png'` のように、コード側でフォルダと連結される素材名は
  リポジトリ内に同名ファイルがあるかで確認する
- `slot-frame-${def.rarity}.png` のような差し込み付きの参照は、候補が1つも無い時だけ落とす
- `link.download` に渡す保存名（`hell-runner-${stamp}.png`）は素材ではないので対象外にする
- 背景画像・能力アイコン・枠素材をそれぞれ退避させて、3経路とも実際に検出することを確認済み

**ブラウザ不要の単体テストを追加**（`tests/unit/`、`npm run test:unit`、36件）。
正本から関数を取り出して動かす方式で、`tools/hex/solver.test.mjs` と同じ考え方。
`tools/` のスクリプトは import した時点で本番のDBへ繋ぐため、写しを持たずに取り出す。

- `sanitizeName`：プレイヤー名をDiscordへ流す前の唯一の関門。制御文字・メンション・
  装飾記法・20文字切り・NGワードを検証。5分ごとのcronで本番稼働しているのに無検証だった
- `jstDay`：**HEXAMINE本体の `dailySeed()` と同じ日付になることを縛った。**
  別実装が2箇所にあり、ズレると前日の王者を通知してしまう
- `parseEntry` / `decodeEntities`：`&amp;` を最後に戻す順序（意図的なもの）を固定。
  実際の `index.html` のパッチノート全件を解釈できることも確認する
- `generateTerrain`：**跳べない奈落を生成しないこと**を検証。ゲーム側の見積りではなく、
  `GRAVITY` と `JUMP_VELOCITY` から2段ジャンプの滞空時間を解いた物理上限で判定する。
  速度4段階×乱数8種で5052件を検査し、最も余裕がない奈落でも限界の70%（幅を1.42倍に
  すると破綻する）。ジャンプ力や重力を変えれば判定も自動で追随する
- 背景ゾーン：境界の噛み合い、3000mのクロスフェード、どの距離でも合計が1になること、
  同時に重なるのは2枚までを検証。**2本のランナーで区間が一致していることも確認する**
  （HELL RUNNER 2には背景の自動確認が1件も無かった）

**Playwrightテストを2つ追加。**

- HELL RUNNER 2の通常URLでデバッグ機能を公開しないこと。無印と討伐2048にはあった検査で、
  HR2だけ無く、14件すべてが `?debug=1` で開いていたため本番の起動経路を誰も通っていなかった。
  `?debug=1` に変えると実際に落ちることも確認済み
- `tests/ranking-submit.spec.js`：登録ボタンの状態遷移（5件）。既存テストは全て
  「必ず成功するスタブ」だったため、失敗時に押し直せる道と二重送信の防止が未検証だった

**運用まわり。**

- `.github/workflows/tests.yml` を追加。push/PRのたびに `verify` / `test:unit` / `test:hex`
  を回す。3つで4秒、依存インストールも不要。Playwrightを入れない理由は上のテスト環境を参照
- `tools/hooks/pre-commit` も同じ3つを見るようにした（従来は verify のみ）
- `npm run test:hr2` / `test:champion` / `test:ranking` / `test:unit` を追加。
  HELL RUNNER 2(14件)と本日の最強決定戦(18件)は、どの絞り込みコマンドにも
  `AGENTS.md` のコマンド一覧にも無く、全体の4割が引き継ぎから見えていなかった
- `playwright.config.js` に `testMatch: '**/*.spec.js'` を明示。`tests/unit/` のNodeテストを
  Playwrightが拾わないようにするため
- テスト環境の表を実測値へ修正（`npm test` は45件ではなく87件、`test:hex-ui` は2件ではなく3件）

**テスト結果。** `verify`(9ファイル) / `test:unit`(36) / `test:hex`(108) 成功。
Playwright 87件のうち、Linuxコンテナで 74成功・8失敗。失敗の内訳は次のとおりで、
**いずれも今回の変更が原因ではない**。

- 背景比較6件：基準画像がOSごとに別ファイルで、Linux用が無い（生成されたものは削除した）
- `デバッグ録画を停止するとWebMを保存する`：並行実行の負荷によるもの。単独では成功

### 2026-08-26 — Claude Code（RHYTHM LAB の制作中止に伴う除外）

ユーザー判断で音ゲー（RHYTHM LAB）の制作を取りやめたため、テストと検査の対象から外した。
現在の開発環境・プレイ環境では満足のいくものが作れる見込みが立たない、との理由による。

- `tests/rhythm-game.spec.js` を削除
- `package.json` から `test:rhythm` を削除
- `tests/verify.cjs` の対象から `games/rhythm-game-test.html` を削除（検査は8ファイルへ）
- `AGENTS.md` のコマンド一覧と、このファイルのテスト表から該当行を削除

このコミットの時点では `games/rhythm-game-test.html` 本体を残していたが、
同日、次の作業で `tetris.html` とあわせて削除した。

これで **Playwright 86件のうち、Linuxコンテナで失敗するのは背景比較6件のみ**になった。
背景比較はOSごとに基準画像が別で、Linux用が無いことによるもの。

### 2026-08-26 — Claude Code（RHYTHM LAB と テトリス の削除）

ユーザー判断で、音ゲーとテトリスの試作を本体ごと削除した。

- `games/rhythm-game-test.html` を削除（前の作業でテストと検査からは既に外してあった）
- `tetris.html` を削除。あわせて `tests/verify.cjs` の対象からも外した（検査は7ファイルへ）
- `CLAUDE.md` と `AGENTS.md` のファイル表から `tetris.html` の行を削除
- `docs/ダークネオンアーケード仕様書.md` の「テトリスは公開可能になるまでゲーム名を
  確定表示しない」を、制作中止として書き換え。3本目以降の予告枠の扱いは変えていない
- `docs/ゲーム企画・HELL-RUNNER次期アップデート案.md` の**テトリスブラスト案は、
  「既存のテトリスを土台に」という前提を失った**。企画書自体は残してあるが、
  やるなら一から作る前提になる旨をこのファイルの参考欄へ書いた

どちらもポータル（`index.html`）からリンクしておらず、専用素材も持っていなかったため、
削除による影響は無い。`npm run verify`(7ファイル / インラインスクリプト20件) /
`npm run test:unit`(36) / `npm run test:hex`(108) 成功。Playwrightは86件のまま
（音ゲーのテストは前の作業で削除済み）。

### 2026-08-26 — Claude Code（HELL RUNNER 1・2 の開発終了を反映）

ユーザーから「無印は完成、2もほぼ完成。プレイヤーからのバグ・不具合報告がない限り
開発も変更もしない」との判断があったため、引き継ぎの記述を現状へ合わせた。

- HELL RUNNER 2 を「実装中」から**開発終了**へ。`CLAUDE.md` のファイル表、`AGENTS.md`、
  このファイルの現況・進行中・凍結ルールをすべて更新
- 「次の主作業はコイン価値・変換器・Lucky!報酬の数値調整」「コイン価値1.50xは仮固定」を撤回。
  **仮固定と書かれていた数値はそのまま確定値として扱う**
- `AGENTS.md` の「新しい開発はすべて HELL RUNNER 2 で行う」を、1・2とも終了した旨へ書き換え
- 新作3案（音楽ゲーム／AIストーリー放置ゲーム／テトリスブラスト）は**いずれも着手しない**。
  テトリスを土台にする前提の注記も不要になったため削除した
- `tests/unit/hell-runner-2-terrain.test.mjs` の意図の説明を、
  「数値調整のため」から「バグ報告対応で1箇所だけ直す時の巻き添えを防ぐため」へ修正

残っている開発は**「本日の最強決定戦」のみ**。未到着のボイス（MIKA／NOISE）、
ブリック選択ボイスの録り直し、キリ勝利ボイスの再確認が待ち。

テスト構成に変更は無い。`npm run verify`(7ファイル) / `npm run test:unit`(36) /
`npm run test:hex`(108) 成功。

### 2026-08-26 — Claude Code（CIの起動失敗を修正し、mainを取り込む）

**追加したCIは3回とも失敗していた。** ジョブIDに日本語（`検査:`）を使ったため、
GitHub Actions がワークフローを起動できず、ジョブが1件も無いまま終わっていた。
既存の2本と同じく英数字のID（`checks:`）にし、日本語は `name:` へ移した。
**ワークフロー名とステップ名は日本語で問題ない。ジョブIDだけが英数字限定。**

手元で `npm run verify` などが通っても、ワークフローYAML自体が妥当かは
GitHubへpushするまで分からない。CIを足した時は実際の実行結果まで見ること。

あわせて `origin/main` を取り込んだ。作業中にmainが10コミット進んでいた
（最強決定戦のランキング登録画面調整・0ms登録のDB制約修正・DEV導線の除去など）。

- 衝突したのは `PROJECT-STATUS.md` の末尾だけ。双方が作業履歴を追記したためで、
  内容は競合しないので**両方残した**
- `tests/todays-champion.spec.js` は18件から20件へ増えていた。テスト表を更新
  （最強決定戦 18→20、Playwright全体 86→88）
- マージ後に `verify`(7ファイル) / `test:unit`(36) / `test:hex`(108) 成功。
  最強決定戦20件 + ランキング登録5件も成功

### 2026-08-26 — Claude Code（CIを実際に緑にするまで）

CIの追加は、pushしてGitHubで動かすまで2つの不具合が見つからなかった。

1. **ジョブIDに日本語（`検査:`）** — ワークフローが起動できず、ジョブ0件のまま失敗。
   4回とも同じ。英数字のID（`checks:`）にし、日本語は `name:` へ移した
2. **`node-version: '20'`** — `npm run test:unit` は `node --test "tests/unit/*.test.mjs"` と
   書いてグロブをNode自身に解釈させている（Windowsのnpmではシェルが展開しないため）。
   **この解釈はNode 22以降でしか効かず**、20では `Could not find ...` で落ちた。
   手元の環境に合わせて24へ変更

いずれも手元では再現しない。`npm run verify` などが通ることと、ワークフローが
正しく動くことは別問題で、**CIを足した時は実際の実行結果まで確認すること**。

### 2026-08-28 — Codex（Remaster：共通透過ゲート枠とRAVEN／VIVI先行アート）

- 選択画面は、舞台専用背景・共通の透過ゲート枠・枠なしキャラアートをPhaser同一座標で重ねる構成へ変更。背景やキャラアートにゲート・文字・UIを焼かないため、端末別CSS座標のずれや二重枠を作らない
- 通常枠の透明内寸は`700×4740`、選択中は`1140×4740`。RAVEN／VIVIの通常・選択中4枚をこの寸法で接続し、選択中だけがその場で太枠・少し拡大・強調され、周囲の6人は個別の絵を維持することを390×844相当で確認済み
- 残る5人の通常・選択中アート10枚は、同じ寸法と枠なしの条件で制作する。詳しい正本は`docs/最強決定戦Remaster_ネオンゲート選択画面_素材指示書.md`、進捗は`docs/最強決定戦Remaster_実装状況.md`

### 2026-08-28 — Codex（Remaster：固定ゲート構図とタイトル素材）

- 採用モックに合わせ、上：透過タイトル画像、中：固定位置の7ゲート、下：`START DUEL`の三分割へ再設計。選択キャラを中央へ移動する処理は廃止し、7人が常に選択肢として読める明るさを維持する
- タイトルは背景へ焼かず、透明PNG原画／軽量WebPとして追加。四隅alpha=0を確認済み。狭いiPhone Safariで左右が少し切れるCanvasでも、RAVEN／TΩ9を含む全7ゲートが残る安全域へ配置した
- 型チェック、Remasterビルド、ルート`npm run verify`、`git diff --check`成功。公開用ビルドへ反映済み

PR: https://github.com/overvivi/vivigames/pull/1

### 2026-08-29 — Codex（Remaster：選択中キャラの路面反射・確認用）

- 最初のカード画像をそのまま切り取る反射は、背景まで含むため画面上で確認できなかった。選択中の立ち絵だけを透過切り抜き素材から読み込み、枠の足元から下へ、上下反転・短く圧縮して描く構成へ置き換えた
- 現在は携帯実機で見え方を判断するため、選択中だけを`0.70`の強さで一時表示している。明るいMIKAではピンクの短い反射を確認済み。暗いRAVEN／TΩ9は色付き輪郭などの個別調整を次段階で行う
- 非選択カードはカード全体を透過せず、立ち絵だけを暗くするまま維持。背景が枠内に抜けて空のゲートに見える問題を避ける
- 携帯実機確認の結果、この切り抜き反転方式は不採用。キャラの足元に落ちる路面反射ではなく、上下反転した別の立ち絵に見えるため。次に反射を再検討する場合は、背景側へ各ゲートの色だけをぼかして置く表現から始める

### 2026-08-29 — Codex（Remaster：路面へ接続する簡素なゲート枠）

- 新規の`championship-re-frame-layout-master-v2.png`からNORMAL／SELECT枠を作り直した。下端の大きな脚・張り出し・中央装飾を廃止し、細い金属の敷居と小さな斜めカットだけで地面に接する意匠へ変更
- 枠の表示位置・外寸（`110×850`）・キャラ窓・入力範囲は維持。背景を描き直す時はこの下端を街と濡れた路面の境界線に合わせる
- 不採用になった切り抜き反射の読込・表示処理を撤去。旧v1枠の原画・配信用素材も削除し、新枠を唯一の参照元にした
- 型チェック、Remasterビルド、ルート`npm run verify`成功。公開はユーザーの明示`push`待ち

### 2026-08-29 — Codex（Remaster：接地量の追加確認）

- `SELECT_GATE_Y`を`808`から`822`へ追加で14px下げた。道路境界に対して枠下端が合計21px、携帯表示で約3mm路面へ入る確認候補
- 型チェック、Remasterビルド、ルート`npm run verify`成功。公開はユーザーの明示`push`待ち

### 2026-08-29 — ユーザー確定（Remaster：ゲート内外の世界設定）

- キャラクター選択パネルは、雨の街の地面に立つ「異次元ゲート」。共通の街背景は**ゲート外側だけ**に存在し、ゲート内へ街並みや路面を連続させない
- ゲート内はキャラごとの独立した異次元空間。通常／選択の透過キャラ2パターンと、ゲート内専用背景を別レイヤーで構成する
- 現在の完成カード画像は、共通街背景の切り抜きをキャラと焼き込んでいるため完成仕様では不採用。全7キャラを描き直す段階で、街背景を枠内へ合成する処理・素材を廃止する
- 制作順は、(1) 枠内専用の異次元背景について複数案を提示して1案を確定、(2) 同一構図・色違いの7背景を枠内規格へ収める、(3) その上へ通常／選択の透過キャラ2パターンずつを制作、(4) 最後に足元・頭位置・距離感を全員同一基準で揃える。キャラ制作を先に始めない

### 2026-08-29 — Codex（Remaster：選択中ゲートのキャラ色点灯）

- 各ゲートの左右にあるキャラ固有色レールを、未選択時`0.34`、選択時`1.00`の明度へ分離。選択中だけがキャラ色で明確に点灯し、他の6枠の色情報は抑えて残す
- 枠座標、サイズ、背景、キャラ絵には変更なし。MIKA選択でピンクのレールと正面立ち絵が主役として読めることを実画面で確認
- 型チェック、Remasterビルド、ルート`npm run verify`成功。公開はユーザーの明示`push`待ち

### 2026-08-29 — Codex（Remaster：枠下端と道路境界を揃えた背景）

- `select-bg-groundline-master-v2.png`を新規原画として追加。街並みから濡れた路面へ切り替わる境界を、現在の枠下端と一致するY=`1226`へ固定した
- 路面だけに7枠の位置と同じシアン／ピンク／橙／紫／青紫／銀紫／赤の、人物を写さないぼかした色だまりを焼き込んだ。各ゲートの窓内背景も同じv2背景の該当領域から再生成し、枠内外の路面が切れないようにした
- 背景・14枚のゲート完成アートはv2ファイル名へ切り替え済み。実画面で枠下端が道路境界に接続していることを確認。型チェック、Remasterビルド、ルート`npm run verify`成功。公開はユーザーの明示`push`待ち

### 2026-08-29 — Codex（Remaster：ゲートの接地量）

- `SELECT_GATE_Y`を`801`から`808`へ変更。背景の道路境界はY=`1226`のままにし、枠下端だけを7px（携帯表示で約1mm）路面側へ入れた
- 型チェック、Remasterビルド、ルート`npm run verify`成功。公開はユーザーの明示`push`待ち

### 2026-08-29 — Codex（Remaster：ゲート内・オーラ空間の7色背景）

- ユーザー採用の「薄い床＋控えめなオーラ＋端だけのデータノイズ」案を、無彩色マスターからRAVEN／MIKA／BRICK／NOISE／KIRI／VIVI／TΩ9の7色へ同一構図で展開した。個別生成せず色相だけを変え、全員で床・オーラ・ノイズの位置がずれないようにした
- 背景は枠の透明窓`370×3137`と同寸で保存し、ゲーム側で引き伸ばさない。既存の仮キャラ完成アートの背後を、街の切り抜きからこの異次元空間へ差し替えた
- キャラの通常／選択中立ち絵は未完成の仮素材。次工程でこの背景を基準に全員を同一の足元・頭位置・距離感で描き直す
- Remaster公開用ビルド成功。公開はユーザーの明示`push`待ち

### 2026-08-29 — Codex（Remaster：ゲート背景とキャラのレイヤー分離）

- 背景と立ち絵を焼き込んだ1枚を非選択時に暗くしていたため、透明になった分から街背景が見えていた。ゲート内背景を常時不透明の別レイヤーにし、通常／選択中の透明キャラだけを切り替える構成へ変更
- 枠内背景と透明キャラはともに透明窓と同寸`370×3137`。同じ倍率で重ねるため、枠の上下に別背景や隙間が出ない
- VIVIはKIRIの銀青と近かったため、二面性の金具を拾った淡い琥珀`#f0d59b`へ変更。ゲート内背景・レール・ゲーム内の固有色を同色に統一
- 旧「街背景焼き込み」の14枚は参照を外したうえで削除。Remasterの型チェック・公開用ビルド成功。公開はユーザーの明示`push`待ち

### 2026-08-29 — Codex（Remaster：ゲート背景の外寸・床位置を是正）

- 直前の背景は透明窓内寸`370×3137`で作っていたため、枠の上下に抜けが残っていた。背景だけを枠外寸`450×3477`へ作り直し、枠と同じ比率・同じ倍率で全面に敷く構成へ変更
- 無彩色マスターを上側から切り出し直し、床の明るい境界をキャラの足元直前まで下げた。非選択でキャラを薄くしても、背景の床線が膝や胴を横切らない
- キャラ透明アートは従来どおり透明窓内寸のまま維持。背景の全面敷きと役割を混同しない
- 型チェック・Remaster公開用ビルド成功。公開はユーザーの明示`push`待ち

### 2026-08-29 — Codex（Remaster：ゲート内背景マスターの描き直し）

- 外寸だけを合わせても、従来マスターは上部が黒く空き、床の境界も人物の脚へ近すぎて見た目上の問題を解決できていなかった。背景マスターを描き直し、上端から細いデータノイズが続き、床・オーラは最下部だけにある構図へ変更
- 新マスターを同一構図の7色へ展開。旧v1マスターは参照を外して削除した
- Remaster公開用ビルド、ルート`npm run verify`成功。公開はユーザーの明示`push`待ち

### 2026-08-29 — Codex（Remaster：ゲート下端と路面の接地・背景床面の復元）

- 当初はユーザーの黒囲みを外側の接地隙間と誤認して`SELECT_GATE_Y`を20px下げたが、実際は枠内の黒い内張りだった。ゲート位置は元の`822`へ戻した
- 非選択キャラを`alpha 0.58`にしていたため、背景の床線が体を透けて見えていた。キャラは不透明のまま暗くする方式へ変更し、背景だけは常に奥に残す
- 新しいゲート内背景は床が小さすぎたため、上部データ感を維持しつつ下部約22%へ広い床面を戻した。床への遷移は煙でぼかし、明瞭な水平線をキャラの脚へ通さない
- 旧v2マスターは参照を外して削除。型チェック・Remaster公開用ビルド・ルート`npm run verify`成功。公開はユーザーの明示`push`待ち

### 2026-08-29 — Codex（Remaster：枠内背景の上下余白を原画実測で修正）

- 枠原画の透明窓を、見た目の推測ではなく中心列の金属終端で実測した。旧マスクは原画`72〜1392px`だけを透明化しており、黒い内張りが上に約22px・下に約48px残っていた
- 金属を残せる実寸`50〜1440px`へ透明窓を広げ、ゲーム規格では`top: 120 / height: 3300`へ変更。背景は枠の内側で0〜100%まで届き、上下の黒帯を残さない
- ゲート全体のY座標や共通街背景の境界は変更していない。型チェック・Remaster公開用ビルド・ルート`npm run verify`成功。公開はユーザーの明示`push`待ち

### 2026-08-29 — Codex（Remaster：選択枠上端の張り出し内張りを除去）

- 選択枠は中央装飾が原画上`0〜51px`まで深く張り出し、通常枠と同じ透明窓上端では背景の前に黒い帯が残った。選択枠だけ透明窓を原画`20〜1440px`相当へ広げた
- 張り出しの内側は背景が通り、上端は細い金属ラインだけが残る。下端と同じく背景が途中で止まらない、簡素な選択枠にした
- 型チェック・Remaster公開用ビルド・ルート`npm run verify`成功。公開はユーザーの明示`push`待ち

### 2026-08-29 — Codex（Remaster：選択枠の上端を下端と統一）

- 上端の張り出し内側だけを抜く案は形が中途半端だったため不採用。選択枠の下端で採用済みの簡素な敷居を反転して上端へ再構成した
- 中央の大きな張り出しを廃止し、上端も下端と同じ細い金属ラインだけに統一。背景が上端で止まらず、選択時だけ装飾が過剰にならない
- 型チェック・Remaster公開用ビルド・ルート`npm run verify`成功。公開はユーザーの明示`push`待ち
