# AGENTS.md

自作ブラウザゲーム集。**このファイルを最後まで読んでから作業を始めること。**
ここに書かれた制約を知らずに触ると、ほぼ確実にファイルを壊す。

---

## 最重要: ファイルを丸ごと読んではいけない

`temple-run-clone.html` は **6.5MB**、`boss-battle-demo.html` は **7.3MB** ある。
巨大なのは、ドット絵スプライト・UI画像・BGMを **base64で埋め込んでいる**ため。

- **500文字を超える行が base64 のかたまり**。1行が数百KBある
- コード自体は HELL RUNNER 約2,270行、討伐2048 約1,740行しかない
- 全体を読もうとすると、それだけでコンテキストが尽きて作業できなくなる

**必ずこうすること:**

```bash
# 構造の把握(base64を除外して関数一覧を出す)
grep -nE "^\s*(function |const [A-Z]|// ---)" temple-run-clone.html | cut -c1-120

# 目的の箇所を特定してから、その周辺だけを読む
sed -n '1740,1810p' temple-run-clone.html
```

**やってはいけないこと:**

- ファイル全体の読み込み(`cat` / 引数なしの Read)
- 整形ツール・prettier・自動フォーマッタをかける(base64の行が折り返されて壊れる)
- ファイル全体を書き直す形の編集。**必ず部分置換で編集する**

---

## テストが無い。実行して確かめることもできない

- ビルド工程なし、npmなし、テストなし。HTMLをブラウザで開けば動く
- **Node.js も Python もこの環境に入っていない**。JSを実行して検証する手段が無い
- したがって「動くはず」で終わらせず、**変更箇所を目視で追い切ること**

変更後は最低限これを確認する:

```bash
# 構造が壊れていないか
tail -c 40 temple-run-clone.html          # </html> で終わること
grep -c '<script' temple-run-clone.html   # 開きタグと閉じタグの数が一致すること
grep -c '</script>' temple-run-clone.html
```

---

## 絶対に復活させてはいけない機能

過去に**意図して廃止した**もの。良かれと思って戻さないこと。

| 廃止したもの | 理由 |
|---|---|
| 修羅モード(速度1.5倍) | 難易度が高すぎた |
| 十倍界王拳の「選択式能力」化 | 20000m到達で全員に自動発動するステージギミックへ変更済み |

---

## Supabase: RLSポリシーだけでは足りない

ランキング機能でここに実際にハマった。テーブルにRLSポリシーを設定しても、
**GRANTを忘れると `permission denied` になる**。ポリシーは「行へのアクセス可否」、
GRANTは「テーブルへのアクセス権」で、**両方**必要。

```sql
grant select, insert on runner_scores to anon, authenticated;
grant usage on all sequences in schema public to anon, authenticated;
```

`id` 列が `generated always as identity` なので、シーケンスへの `usage` も要る。
詳細は `supabase-fix-permissions.sql`。

---

## 背景画像システム(HELL RUNNER)

背景は `images/` に置いた画像を距離に応じて切り替える。**HTMLには埋め込まない。**
枚数が増えるほど起動時に全部読み終わるまで待たされ、コード修正のたびに
全画像を再取得することになるため。

実装は `temple-run-clone.html` の `BG_ZONES` 周辺(1743行目あたり)。

- `rangeAlpha` で前後の背景を **3,000mかけて重ねながら**入れ替える
- **画像は1枚おきに左右反転して並べる**。継ぎ目が必ず鏡写しになるので、
  素材がシームレスでなくても境目が出ない(画像生成AIはシームレス化が苦手なため、この方式にした)
- 画像が出ている間、手描きの星・月・城・枯れ木は `procA` で薄くする。
  背景画像にも城やコウモリが描かれており、二重に見えるため
- 画像は**原寸のまま**使う。縦長画面では約3.5倍に拡大されるので、
  縮小すると粗が出る(1/2に縮小すると7倍拡大になる)

### 背景ゾーンを追加する手順

`images/` に画像が置かれたら、`BG_ZONES` に1行足すだけでよい。**それ以外は触らない。**

```js
const BG_ZONES = [
  { key:'hell', src:'images/bg1-hell.png', start:-1, peak1:0, peak2:15000, end:18000 }
  // ここに追記する
];
```

`start` で現れ始め、`peak1` で完全表示、`peak2` から薄くなり、`end` で消える。
区間の値は `docs/画像素材の仕様.md` の「5. 区間の割り当て」表に従うこと。**自分で決めない。**

| # | key | ファイル名 | start / peak1 / peak2 / end |
|---|---|---|---|
| ① | hell | `bg1-hell.png` | -1 / 0 / 15,000 / 18,000 |
| ② | graveyard | `bg2-graveyard.png` | 15,000 / 18,000 / 31,500 / 34,500 |
| ③ | forest | `bg3-forest.png` | 31,500 / 34,500 / 53,500 / 56,500 |
| ④ | ruins | `bg4-ruins.png` | 53,500 / 56,500 / 76,500 / 79,500 |
| ⑤ | cliff | `bg5-cliff.png` | 76,500 / 79,500 / 98,500 / 101,500 |
| ⑥ | hope | `bg6-hope.png` | 98,500 / 101,500 / 99,999,999 / 100,000,000 |

**隣り合うゾーンは値を共有している。** 前のゾーンの `peak2`/`end` が、
次のゾーンの `start`/`peak1` と一致する。こうすると重なりの合計が常に1になり、
入れ替わりの途中で背景が薄くなったり濃くなったりしない。**この対応を崩さないこと。**

②③には対応する手描き演出(`drawGraveyard` / `drawCursedForest`)があり、
**同じ値に揃えてある**。背景を足すときも、この演出の区間は変更しないこと。

---

## git は触らない

**コミットもpushもしない。** git操作は人間側が管理している。
変更は作業ツリーに残すだけでよい。

このリポジトリは GitHub(overvivi/vivigames)に公開しているが、
**作りかけのものは反映しない方針**。勝手にpushすると公開されてしまう。

---

## コードの慣習

- コメントは**日本語**。「何をしているか」ではなく **「なぜそうしたか」** を書く。
  特に、過去のバグを踏まえた実装意図は必ず残す
- 既存コードの書き方に合わせる。命名も密度も周囲に揃える
- パッチノートは `index.html` の `<details>` 内に、ゲームごと・バージョンごとに追記する
- ゲームは単一HTMLで完結させる(背景画像だけが例外)。
  `file://` で直接開いても動く状態を保つこと

---

## ファイル構成

| ファイル | 内容 |
|---|---|
| `index.html` | ゲーム置き場(ポータル)。パッチノートもここ |
| `temple-run-clone.html` | HELL RUNNER 本体。横スクロールランナー |
| `boss-battle-demo.html` | 討伐2048 本体。2048+ボス撃破RPG |
| `tetris.html` | テトリス。**土台のみ。ポータル未掲載**(まだ遊べる状態にしない) |
| `images/` | 背景画像 |
| `docs/画像素材の仕様.md` | 背景・足場の発注仕様と区間の割り当て |
| `supabase-*.sql` | ランキング用テーブルのSQL |

より詳しい背景は `CLAUDE.md` にもある(内容はこのファイルとほぼ同じ)。
